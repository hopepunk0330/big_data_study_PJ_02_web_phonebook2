# brand-designer 메모리

이 파일은 brand-designer의 최근 작업 로그(휘발성, 5개 캡)만 남깁니다. **브랜드 결정사항 자체(현재 확정 상태)는 여기 없습니다 — `docs/design/brand-guide.md`가 그 소스 오브 트루스입니다.** 작업 시작 시 이 로그가 아니라 그 문서를 먼저 읽으세요. (단, mercari 프로젝트는 별도 fileKey를 쓰는 다른 제품이라 그 소스 오브 트루스는 `docs/design/mercari/brand-guide.md`다.)

## 작업 로그

### 2026-08-24 (4차) — 사용자 중간 피드백: 페이지 리네임 + Concept B 트렌드 전면 교체(팔레트는 그대로)
- 배경: 1차 3안이 아직 최종 확정 전인 상태에서 사용자가 Figma를 직접 보고 중간 피드백 3건 전달 — (1) 페이지 `8:2` 이름을 관례 표기 "브랜드컨셉"으로, (2) Concept B의 5색 팔레트(`#FFF8EF`/`#FF5A36`/`#FFD400`/`#7B5CFF`/`#16171A`)는 검증된 값이라 유지하되 "네오팝·뉴브루탈리즘" 트렌드만 A/C와 안 겹치는 제3의 실제 트렌드로 교체, (3) A/C는 절대 건드리지 않음.
- 절차: 페이지 `8:2` → "브랜드컨셉"으로 리네임, 기존 `8:4`는 내용 무수정 상태로 이름만 `❌ 미채택 — Concept B — 네오팝 브루탈형`으로 라벨링. 레퍼런스 페이지 `1:58`(무드보드 42장)을 `get_screenshot`으로 재열람해 스탬프·쿠폰·티켓 다꾸(다이어리 꾸미기) 클러스터(와디즈 펀딩데이, 리미티드 쿠폰샵, hwahae awards 도장 포스터, Conversation 말풍선 스티커시트)를 근거로 **콜라주 저널링(Collage/Scrapbook Journaling)**을 신규 트렌드로 확정 — A(1px 보더+무그림자)·C(벤토 셀+네온 stroke+glow) 어느 쪽과도 겹치지 않는 제3의 조형 원리로 "다스트 ticket 대시보더 + 소프트 디퓨즈 그림자(blur16, ink14%) + washi tape 오버랩 + 잉크 도장 배지"를 채택.
- 신규 프레임 `28:2`("Concept B — 콜라주 저널링 (Collage Journaling)")를 `8:4` 계열 옆(x=2620, `8:5` 우측)에 신규 생성 — Color Palette(5 hex 스와치 + washi tape 액센트로 실측 대조 가능하게 원색 유지)/Typography(Hero Price를 대시보더 티켓 스텁 + 코랄 잉크스탬프 "OK!" 배지로 표현, Inter Black 56)/Card Components(선택됨=코랄 솔리드+소프트섀도, 미선택=대시 아웃라인, 신뢰배지=영수증 스트립, 스텝=잉크 스탬프 원)/Illustration Direction(washi tape·잉크도장·영수증컷·폴라로이드 선화) 5개 필수 요소 전부 포함. 팔레트 5개 hex를 read-only로 재조회해 브리프 표와 **완전 일치**(FF5A36/FFD400/7B5CFF/FFF8EF/16171A) 확인.
- A(`8:3`/`20:2`/`20:3`)·C(`8:5`/`23:2`/`23:3`) 6개 프레임은 이름·자식 수 재조회로 무변경 확인(직접 수정 없음). SCR-002/003 적용화면(`21:2`/`21:3`)은 손대지 않음(다음 단계 ui-designer 몫). design-pl에게 신규 프레임 nodeId(`28:2`)와 트렌드 선택 근거, 팔레트 hex 동일성 확인 결과 보고.

### 2026-08-24 (5차) — B 계보 병합(신규 "브루탈 콜라주") + A·C 확장 프레임 추가
- 배경: `8:2`("브랜드컨셉") 페이지에 이미 존재하던 A(`8:3`)/네오팝 B(`8:4`, 이미 미채택 라벨)/콜라주 B(`28:2`, 라벨 없음)/C(`8:5`) 4개를 `get_metadata`+`get_screenshot`(base64)으로 먼저 직접 재확인한 뒤 진행. 신규 3개 프레임 제작: 병합 B `43:2`("Concept B — 브루탈 콜라주") x=3480, A 확장 `48:2`("Concept A — 확장 (정보 위계 강화)") x=0·y=2200, C 확장 `51:2`("Concept C — 확장 (히어로→시트 전환, 펄스·칩)") x=1720·y=2200 — 모두 기존 프레임과 좌표 겹침 없음.
- **병합 B**: 팔레트 5색(`#FF5A36`/`#FFD400`/`#7B5CFF`/`#FFF8EF`/`#16171A`) 절대 불변, 구조 레이어(잉크 3px 보더+하드 오프셋 그림자 `offset 6~7px blur0`, 8bit 별/다이아몬드 컨페티는 콘텐츠 안 가리게 모서리에만)와 장식 레이어(washi tape 회전 오버랩, 잉크 스탬프 배지, 대시 보더 미선택 버튼, 영수증 스트립 신뢰배지 — 전부 소프트 디퓨즈 그림자 `blur16 ink14%`만 적용)를 명확히 분리 적용. 빌드 중 스와치 라벨 줄바꿈·상품카드 텍스트-스탬프 겹침·스텝뱃지 텍스트 잘림 버그 3건을 스크린샷 재확인 후 자체 수정.
- **A 확장**: 팔레트 5색 불변. "콘텐츠 > 배지 > 콜아웃" 위계를 크기·채도·대비로 구현 — 배지(작은 필 칩, Accent Tint 배경+Primary 텍스트, 카드 코너에 얹힘)와 신뢰 콜아웃 박스(넓은 면적이지만 Accent Tint 60% 저채도 배경)를 신규 추가. 빌드 중 CARD COMPONENTS 섹션이 Typography 섹션 caption과 겹치는 버그 1건을 스크린샷 재확인 후 자체 수정(세로 간격 재조정, 프레임 높이 확장).
- **C 확장**: 팔레트 5색 불변. 다크 히어로 블록(Surface bg)이 라운드 코너로 화이트 바텀시트 위에 슬라이드하는 전환 데모, 동심원 3단(외곽 링/중간 링/도트+글로우)+"실시간 분석 중" 라벨의 펄스 인디케이터, 민트/바이올렛 스트로크 알약형 칩 4개(±5,000/±1,000)를 신규 추가.
- 완료 후 `28:2` 이름 앞에 `❌ 미채택 — ` 라벨 추가(시각 내용 무변경, 재조회로 확인) — `8:4`(이미 미채택)는 손대지 않음. `8:3`/`8:4`/`8:5`는 `get_metadata` 전체 재조회로 자식 수·이름·좌표 완전 동일함을 확인(무변경). `docs/design/brand-guide.md`/`confirmed/`는 이번 라운드가 중간 보강 단계라 손대지 않음(브리프 명시). design-pl에게 신규 노드 3개 ID + 무변경 확인 근거 + 병합 레이어 분리/A·C 인사이트 반영 자체 점검 결과 보고.

### 2026-08-24 (6차) — `43:2` 미채택 라벨링 + A확장 기조 Concept A(신규)·B(신규) 신규 톤 프레임 제작
- 배경: 사용자가 Figma를 직접 보고 네오팝 계보(`8:4`/`21:2`/`21:3`, 이미 미채택 라벨) 완전 제외를 재확인하고, 병합 B(`43:2`, 지난 5차분)에 미채택 라벨을 요청. 동시에 "Concept A — 확장"(`48:2`)의 컬러·포인트 언어를 기조로 A·B 신규 톤 프레임 제작을 요청.
- `43:2`("Concept B — 브루탈 콜라주")를 `❌ 미채택 — Concept B — 브루탈 콜라주 (Brutal x Collage Merge)`로 리네임(시각 내용 무수정, 자식 수 12개 그대로 확인). 화면 쪽 `61:28`은 ui-designer 몫이라 건드리지 않음.
- `48:2`를 `get_screenshot`+`get_design_context`로 재실측 — 브리프 제공값(Primary `#3182F6`/Accent Tint `#E8F3FF`/배경 `#FFFFFF`/Surface `#F7F8FA`/Ink `#1A1D29`)과 완전 일치 확인. 추가로 구조 보더/디바이더 색 `#E2E6EC`(48:2 자체에서 실측된 값)도 함께 확인 — 이 6번째 색은 새 팔레트 도입이 아니라 48:2 원본에 이미 존재하는 구조용 중립색이라 자체 점검상 "A확장 고정값만 사용" 원칙 위반이 아님.
- 신규 프레임 2개를 같은 페이지에 제작(기존 프레임과 좌표 겹침 없음, x=4340/5200·y=0):
  - **Concept A(신규) — 소프트 트러스트 정제형**(`72:2`, 높이 1528): 48:2의 자연스러운 정제판. Color Palette·신규 TRUST BADGE SYSTEM(✓/🔥/🔍 배지 3종, Accent Tint 배경+Primary 텍스트 pill)·Typography(Hero Price 44)·Card Components(선택=Accent Tint+Primary 2px 보더, 미선택=흰색+연회색 보더, 트러스트 콜아웃=Accent Tint 60% 통투명 박스)·신규 ICON DIRECTION SAMPLE(Surface 배경 위 1.5px Primary 아웃라인 원/라운드사각/라인샘플 3종)·Illustration Direction 순.
  - **Concept B(신규) — 액티브 컨피던스형**(`72:69`, 높이 1544): 팔레트 hex 완전 동일, 표현 강도만 상향 — 배지는 Primary 솔리드 배경+흰 텍스트(A 대비 확대), Hero Price 52·Primary 컬러(A는 44·Ink), 선택 버튼은 Primary 솔리드 채움+흰 텍스트(A는 tint+outline), 트러스트 콜아웃은 좌측 4px Primary 액센트 바 + 흰 카드(A는 전체 tint 박스), 아이콘 타일은 Primary 솔리드 배경 위 흰 라인(A는 반대로 연한 배경 위 컬러 라인).
  - 두 프레임 모두 `node.screenshot()`으로 즉시 시각 검증(겹침·잘림 없음 확인), 이어 `get_screenshot`으로 재확인.
- 자체 점검: 두 프레임에 실제로 쓰인 hex는 `#3182F6`/`#E8F3FF`/`#FFFFFF`/`#F7F8FA`/`#1A1D29`(A확장 5색) + `#E2E6EC`(A확장 자체의 구조 보더색) 뿐 — 네오팝(`8:4`)·콜라주(`28:2`/`43:2`) 계보의 색은 전혀 섞지 않음. Concept C(`8:5`/`51:2`), A 원본(`8:3`), A 확장(`48:2`) 자체는 열람만 하고 수정하지 않음. `docs/design/brand-guide.md`/`confirmed/`는 이번 라운드가 중간 보강 단계라 손대지 않음(브리프 명시).

### 2026-08-25 (7차) — 확정 디자인(`126:2601`) 7개 화면 전수 실측(브랜드/톤 판단 아님, 순수 측정)
- 배경: design-pl이 "사용자 직접 확정 완료, design-systems 컴포넌트 추출 전 실측 근거 확보" 목적으로 브리프 지정. 브리프는 8개 화면을 나열했으나 `get_metadata(126:2601)`로 재확인 결과 실제로는 Concept A_01~04·B_01~03 **7개만 존재**(오기로 판단, 존재하는 7개 전수 확인).
- 7개 전부 `get_design_context`+`get_screenshot` 양쪽으로 열람(nodeId: A_01 `110:103`/A_02 `124:890`/A_03 `124:1149`/A_04 `124:991`/B_01 `124:1455`/B_02 `126:1771`/B_03 `126:1951`). 색상·타이포·보더/radius/그림자·spacing 그리드·반복 모티프 5개 카테고리를 표로 실측해 design-pl에게 전량 반환(문서 미작성, 응답 본문에 결과 직접 기재).
- **핵심 결론**: Concept A/B는 컬러 hex·폰트 역할·radius·보더·그림자(전체 부재)·spacing 리듬 전부 완전 동일 — 서로 다른 톤이 아니라 하나의 확정 시스템의 변형. 실제 차이는 스타일이 아니라 상태/스텝 차이 3가지뿐: (1) HeroPriceCard 단일가(A) vs 범위표시(B) variant, (2) Price Input 컴포넌트가 B_02/03에만 존재(A 플로우엔 없음), (3) A_02~04만 딤+peek 바텀시트 전환 연출을 쓰고 B는 전부 정착 풀시트 상태.
- 불일치 2건 발견해 design-systems에 보고: Eyebrow 배지 배경색이 코드상 미상(스크린샷엔 옅은 반투명 보임, fill 직접 재조회 필요), B_03만 상태 배지 보더(`#dedede`) 클래스 누락.
- 확정 프레임 7개 전부 읽기 전용(이름 변경 없음), `docs/design/brand-guide.md`/`confirmed/`는 이번 라운드 범위 밖이라 손대지 않음(브리프 명시 — 실측만, 문서화는 design-prompter 몫).

### 2026-08-25 (8차) — mercari 정식 Brand Guide 문서화(`docs/design/mercari/brand-guide.md`) 최초 작성
- 배경: design-pl이 "브랜드 컨셉서도 정리하는 거 맞지?"라는 사용자 확인을 거쳐 7차의 "정식화는 이번 범위 아님" 유보를 취소하고 정식 Brand Guide 작성을 지시. 저장 위치는 이 프로젝트(fileKey `SIBLz4S4IZbjabzhMSAgdo`) 전용 신규 폴더 `docs/design/mercari/`로 확정(연락처 관리 앱 fileKey `zgGlMBwFglaDlaeyP4CkgR` 소유 문서들과 완전 분리).
- 확정 화면 7개(A_01~04/B_01~03, nodeId는 7차와 동일)를 `get_design_context`로 전부 재열람 — 색상/타이포/보더·radius/그림자/간격은 7차 실측값을 재관찰 없이 그대로 채택, 아이콘·모티프(chevron-right 24px, star-01 12px, shield-check 32px — 선택된 Choice Card에만 절대위치로 얹히는 신뢰 신호, Choice Card 전용 컬러 일러스트 아이콘 2종 — 썸업류/파란 원+회전 연필, 드래그 핸들 40×4px, 검색 이모지 🔍)와 카드 유형별 padding 구체값(Choice Card 14/HeroPriceCard 18·24/CTA 20·16/Price Input 14·12/배지류 6종)은 이번에 신규로 재확인.
- 문서에 Concept A/B를 "하나의 통합 시스템"으로 서술(별도 A/B 섹션 분리 안 함), 차이점은 HeroPriceCard variant·Price Input 존재 여부·전환 연출 3가지만 명시. 그림자 전무를 의도된 스타일 언어로, White Sheet 상단만 radius 24(하단 각짐)를 의도된 비대칭으로 명시.
- B_03(`126:1951`)의 상태배지 보더 누락 재확인(7차 보고 건과 동일) — 확정 화면은 무수정으로 그대로 기록만 함. 7개 화면 전부 이름 변경 없음(read-only) 확인.
