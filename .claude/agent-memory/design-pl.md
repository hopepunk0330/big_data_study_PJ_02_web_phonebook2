# design-pl 메모리

이 파일은 design-pl이 작업 시작 시 읽고, 작업 종료 시 기록을 남기는 메모리/로그입니다.

## 팀 로스터 (참고용)
- design-prompter, design-scanner, brand-designer, graphic-designer, design-systems, ux-designer, ui-designer, interaction-designer, motion-designer, content-designer, design-qa

## 정책 메모 (세션 기록 수준 — 하네스 문서는 아직 미수정)

**2026-07-14, 레거시 정리 작업 경계 변경**: 사용자가 "레거시 및 사용안해서 컴포넌트 기능 끊어놓은 것 내가 직접 지울게, 지금 너무 헷갈려서"라고 밝힘. 이후:
- 레거시/미사용 컴포넌트를 실제로 해제(COMPONENT/COMPONENT_SET→FRAME 전환)하거나 삭제하는 **적극적 정리 작업은 팀이 먼저 나서서 하지 않는다** — 사용자가 Figma에서 직접 처리하기로 함. 이후 사용자가 명시적으로 "팀이 직접 처리하라"고 지시하면 이 유보는 해제되고 표준 절차(2-4번, 분리 후 전환)를 그대로 따라 처리한다.
- `[Legacy ...]`/`❌ 미채택 —`/`❌ 폐기 —` 같은 **이름 라벨링(구분 목적)은 계속 유지**.
- `docs/harness/design-team/figma-file-organization.md` 등 하네스 규칙 문서는 순수 프로세스/에이전트 정의 문서 편집이면 design-pl이 직접 Read 후 Write로 처리해도 된다. **이 원칙은 `.claude/agents/*.md`(에이전트 정의)와 `docs/harness/reset-checklist.md` 같은 절차 문서 전체에 동일하게 적용된다** — design-pl에게는 Edit 도구가 없어 항상 전체 Read 후 Write(전체 재작성)로 처리하되, 기존 내용을 빠짐없이 보존하고 필요한 부분만 삽입/수정한다. **단 `docs/design/design-system.md`처럼 "Edit-only, Write 금지"가 명시적으로 요구되는 문서는 design-pl이 손대지 않고 소유 에이전트(design-systems)에게 위임한다.**

**도구 제약 메모**: design-pl에게는 백그라운드 실행 중인 하위 에이전트에게 메시지를 이어붙이는 SendMessage 도구가 실제로는 제공되지 않는다 — 완료를 기다렸다가 새 Agent 호출로 후속 지시를 보낸다. design-pl 자체 도구셋에는 Edit이 없다(Read/Glob/Agent/Write/Skill만). 백그라운드 호출은 완료 통지 없이 조용히 무반영될 수 있어 실제 반영 여부를 재확인해야 한다(65차) — 이후 후속 위임 작업은 포그라운드로 호출한다(66~69차). design-pl/content-designer 둘 다 Bash가 없어 실제 파일 업로드(curl POST)를 못한다 — "필요한 nodeId·파일 매핑 조사→메인 세션이 Bash로 업로드 실행→배치 확인 후 다음 단계"의 2단계 핸드오프로 처리한다(68차 확인). Figma를 쓰는 워커(content-designer, graphic-designer)와 Figma를 안 쓰는 워커(ai-image-prompt-writer)는 서로 독립적인 작업이면 한 메시지에 병렬로 호출해도 된다 — "Figma 쓰는 에이전트는 동시에 하나만" 제약은 Figma를 실제로 쓰는 에이전트끼리에만 적용된다(68차 확인). **단, graphic-designer(오브제 원화)→content-designer(배치)처럼 한쪽 산출물이 다른쪽 입력이 되는 경우는 반드시 순차 호출**(68차 재확인). **69차 재확인**: design-prompter(브리프 작성, Figma 미사용)와 content-designer(조사·정리 작업, Figma 사용) 조합도 서로 독립적이면 한 메시지에 병렬 호출 가능함을 재확인. **70차 신규**: design-prompter를 `subagent_type: "fork"`로 잘못 부르면 Figma MCP 도구가 전혀 배정되지 않는다(fork는 design-pl 자신의 도구셋을 상속하므로 Figma 도구 없음) — Figma 조사가 필요한 작업은 반드시 `subagent_type: "design-scanner"`(또는 해당 워커) 그대로 불러야 한다. 또한 **design-prompter가 "브리프를 위(자신의 이전 응답)에 출력했다"고 요약만 반환하고 실제 본문이 design-pl에게 전달 안 되는 사례가 반복됨** — design-prompter 호출 시 "최종 응답 메시지 자체에 브리프 본문 전체를 그대로 써라, 참조만 하지 마라"를 명시적으로 요구해야 실제 본문을 받을 수 있었다(2회 연속 요약만 반환 후 3번째 명시적 재요청에서 성공). **72차 재확인·강화**: 같은 실패가 또 반복됨(design-prompter가 "위 응답에 전문 포함"이라고만 답함) — 이때 design-pl 자신을 `fork`로 재호출해 "이전 서브에이전트의 출력을 복원해달라"고 시도했으나, **fork는 다른 서브에이전트(design-prompter)의 트랜스크립트에 접근할 방법이 없어 실패한다**(fork는 상위 design-pl의 대화 맥락만 상속받을 뿐, design-prompter가 실제로 만든 출력 그 자체는 design-prompter 자신의 트랜스크립트에만 있음) — 복구 방법은 fork가 아니라 **design-prompter를 처음부터 다시 호출**하되, 지시문 맨 앞과 맨 끝 두 곳에 "본문 전체를 출력해라, 요약 금지, 이 응답=산출물"을 강조 배치하는 것뿐이다. 이번엔 이 방식으로 3번째 시도에서 성공. **74차 재확인(이번 라운드)**: design-prompter가 "요약만 반환"하는 실패가 또 발생했고(design-qa 브리프 작성 시), 이땐 재요청 대신 design-pl이 자신의 원본 지시문(design-prompter에게 보낸 입력)에 design-prompter가 언급한 소폭 보강 포인트만 직접 병합해 design-qa를 바로 호출하는 방식으로 처리해 라운드를 지연시키지 않았다 — 브리프가 이미 충분히 구체적이고 design-prompter의 추가 기여가 요약상 소폭(3가지 포인트)일 때는 재요청보다 이 방식이 더 효율적임을 확인.

**2026-08-24, mercari 대안 디자인 — 1차 확정 게이트 직전 4개 대표화면 완성도 폴리싱**: 사용자가 항목 미지정으로 "판단하기 더 좋게 완성도를 올려달라"고 요청(실사 상품사진 적용 직후 `75:2`/`79:2`/`85:2`/`86:2`, Figma `SIBLz4S4IZbjabzhMSAgdo`). 창작 판단이 필요한 애매한 지시라 design-prompter를 거쳐 7개 체크리스트(간격 그리드/그림자 스펙/타이포 위계/이모지 아이콘 발견-only/다크 히어로 디테일/선택상태 복수신호/실측데이터 대조)로 구체화한 뒤 ui-designer 1회 호출로 4개 화면을 순차 처리시킴. ui-designer는 실제 렌더링 버그 1건, 데이터 부정합 1건, 이모지 아이콘 1건을 찾아 수정. 톤/구조/팔레트 무변화 확인. 다크 히어로 텍스트 명도대비 AA 미달(4.4:1) 발견했으나 고정 hex라 범위 밖으로 판단, 기록만 남김 — 톤 확정 단계 재검토 필요 항목으로 이연. 시안 단계(design-systems 미개입)라 design-qa/harness-auditor 모두 미호출.

**2026-08-25, mercari 확정 디자인(`126:2601`) 컴포넌트 추출·Brand Guide 정식화 라운드**: 사용자가 Figma에서 직접 만든 최종 디자인을 확정으로 지정, "[메인 세션 확인] 사용자가 실제로 승인함" 형식으로 재개 지시받음 — design-concept-round 스킬 3단계(A) 경로 중 "파일럿 승인 완료 → design-systems 추출" 지점부터 진행. 순서: (1) ui-designer가 "브랜드컨셉" 페이지(`8:2`) 내 미채택 시안 22개 노드 전부에 `❌ 미채택 —` 라벨 확인/보강(15개 신규, `36:2` "Bottom Sheet"는 사전 정보와 달리 라벨 누락이 발견돼 함께 처리) — (2) brand-designer가 확정 화면 7개(브리프의 "8개"는 오기로 확인됨, 실제 7개: A_01~04/B_01~03) 전수 실측, Concept A/B는 톤이 다른 게 아니라 **하나의 통합 시스템**(팔레트/타이포/보더/그림자/spacing 전부 동일, 차이는 Hero Price Card variant·Price Input 유무·전환연출 3가지 상태차이뿐)임을 데이터로 확정 — 그림자 전체 없음(`effects:[]`), 2px 단위 고유 그리드(8px 배수 아님)도 확인 — (3) design-systems가 Price Input/Progress Row/Product Row/Badge류 신규 등록, 기존 "check box"가 실제로는 "Choice Card" 역할임을 발견해 이름 정정, 자체 재대조 중 프로�터티 병합 버그·확정화면 인스턴스명 연쇄변경 부수사고를 스스로 발견해 즉시 복구 — (4) 메인 세션 경유 추가 지시("브랜드 컨셉서도 정리하는 거 맞지?" 확인)로 brand-designer가 정식 Brand Guide를 신규 작성(`docs/design/mercari/brand-guide.md` — 이 프로젝트 최초의 mercari 전용 docs/design 문서, 기존 `docs/design/*.md` 4개는 전부 다른 프로젝트(fileKey `zgGlMBwFglaDlaeyP4CkgR`, 연락처 앱) 소유임을 확인하고 오염 방지) — (5) design-qa 감사에서 **HIGH 4건**(White Sheet 영역 텍스트 대비 미달 3계열 — 신규 발견, 다크히어로와 별개; 아이콘이 등록된 Icon 컴포넌트의 INSTANCE가 아니라 raw FRAME으로 조립됨; 신규/보강 컴포넌트 7종 전부 Variable 바인딩 없이 완전 하드코딩; 스펙 시트가 Choice Card/Hero Price Card/CTA 3종 누락)과 **MEDIUM 2건**(variant 축 네이밍이 `State` 아닌 `Property 1` — 기존 관례라 이번 신규 사고 아님; 신규 4종이 확정 화면 안에서 INSTANCE로 치환 안 됨) 발견 — B_03 보더 누락 예외 재검증은 PASS, 값/네이밍 원본 대조도 전부 PASS. **HIGH 4건은 이번 라운드에서 수정하지 않고 그대로 메인 세션에 보고** — 근거: 사용자의 원래 지시가 "문제만 보고, 직접 고치지 않는다"였고, 대비 미달 1건은 확정 원본(읽기전용) 소관이라 애초에 팀이 못 고치며, 나머지는 "국소 보강" 범위를 넘어서는 스코프 확장(변수 인프라 신설, 아이콘 컴포넌트 재등록)이라 사용자 결정 필요로 판단. (6) `docs/design/mercari/brand-guide.md` 신규 생성(B범위)이라 harness-auditor 89차 호출 — **MEDIUM 3건**(Brand Guide가 사실상 2-3번 "확정 스펙 문서" 역할까지 겸하고 있어 `docs/design/confirmed/`에 mercari 항목이 없는 문제, `.claude/agents/brand-designer.md` line 40의 "소스오브트루스는 `docs/design/brand-guide.md`" 단수 경로 서술이 새 다중 프로젝트 서브폴더 구조를 반영 못함, 문서 내 수치 목록 2건 자체 누락)와 **LOW 1건**(근거 절이 캡 대상 로그를 인용) 발견, 전부 미수정 상태로 보고만 함. **다음 라운드 결정 필요 항목**: (a) HIGH 4건 수정 여부·범위(특히 변수 인프라를 이 파일에 신설할지), (b) Brand Guide vs 확정 스펙 문서 역할 통합을 정식 관례로 인정할지 아니면 분리할지, (c) `brand-designer.md` line 40 경로 서술을 다중 프로젝트 대응으로 갱신할지.
