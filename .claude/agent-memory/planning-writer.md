# planning-writer 메모리

이 파일은 planning-writer의 최근 작업 로그(휘발성, 5개 캡)만 남깁니다. 각 기획 문서의 확정 내용 자체는 여기 없습니다 — `docs/planning/*.md`(현재 버전)와 `docs/planning/old/`(과거 버전 실물)가 소스입니다. 컨셉/BM 판단은 `docs/planning/service-concept.md`(service-planner 관리), 기술 판단은 `docs/planning/tech-architecture.md`(tech-architect 관리)를 따릅니다.

## 작업 로그

### 2026-07-18 — 02 화면정의서 §4 SCR-002 범례 주석 잘못된 절 인용 정정 v1.17→v1.18 + 06 테스트계획서 TC-E2E-SCR003-09~12 신설 v1.12→v1.13 + 인용 동기화 연쇄(01 v1.13→v1.14, 04 v1.13→v1.14)
- 메인 세션이 이미 근거·문구까지 확정한 순수 정정·보완 2건을 한 라운드에 처리(새 컨셉·스코프 판단 없음).
- **02 v1.18**: §4 SCR-002 와이어프레임 범례 주석(242~243행)의 "GET /contacts?category_id=(§5 ⑤)" 인용이 틀렸음을 정정 — §5(517행 이하)는 페이지 로드 시퀀스만 다루고 클릭 인터랙션을 다루지 않으므로, 실제로 정확히 서술된 §4 SCR-002 본문 "카테고리 nav 목록" 문단(257행)을 가리키도록 "(§4 SCR-002 본문 "카테고리 nav 목록" 문단 참고)"로 교체. 같은 줄의 "채워짐(§5 ④)"(카테고리 목록 자체를 채우는 GET /categories 호출)은 정확한 인용이라 그대로 둠. **함정 주의**: python 스크립트로 legend 줄을 먼저 cp 전 원본(v1.17) 파일에 직접 edit했다가, 그 파일을 그대로 v1.18로 cp한 것을 뒤늦게 발견 — v1.17(구버전, old/로 이동될 파일)에 신규 수정 내용이 섞여 들어가는 사고. 즉시 v1.17 파일의 legend 줄만 원래 문구로 되돌리고 diff로 v1.17/v1.18 차이가 의도한 4곳(버전 헤더, changelog, SCR-004 반영완료 문단, legend 줄)만 남았는지 재확인 후 v1.17을 old/로 이동 — **cp 전에 원본 파일을 절대 먼저 건드리지 않는다**는 원칙을 재확인.
- **06 v1.13**: qa-planner 브리프(`qa-planner-scr003-filter-gap-brief.md`) §2 표를 그대로 옮겨 §5-4 SCR-003 표에 `TC-E2E-SCR003-09`(P0)~`12`(P1×3) 4개 신설(케이스ID·근거·시나리오·예상 반응·우선순위 문구 그대로, 판단은 하지 않음). "E2E 그룹 소계" SCR-003 8→12, 합계 38→42로 정정. §10 총계 표 "화면 흐름 E2E 시나리오" 38→42, 합계 130→134(단위 제외 118→122)로 정정. §9 AC 커버리지 매트릭스는 이 4개가 AC가 아니라 SCR-002/003 자체 근거 케이스라(기존 TC-E2E-SCR002-06 등과 동일 패턴) 변경 안 함.
- **인용 동기화 연쇄**: 01(§3-5 "배경" 문단 "화면정의서 v1.3(현재 v1.17)"→"(현재 v1.18)", v1.13→v1.14), 04(§5 UC-09 문단 02 경로 `_v1.17.md`→`_v1.18.md`, v1.13→v1.14), 06 자신의 상단 근거 문서 표 + §1 역할 안내 문단의 01/02/04 인용 3곳(v1.14/v1.18/v1.14로 작성 시점에 맞춰 반영). 02 v1.18 자신의 §4 SCR-004 "반영 완료" 문단의 04·01 인용도 두 문서가 함께 v1.14로 개정됨에 따라 `_v1.14.md`로 함께 정정(매 라운드 반복되는 동일 자기 소비 인용 패턴).
- 최종 grep 스윕(`화면정의서_v1\.17`, `구현요구사항_v1\.13`, `PRD_v1\.13`, `테스트계획서_v1\.12`) 결과 04 v1.14 changelog 항목(역사적 인용, 그대로 둠) 1곳과 `tech-architecture.md` §4 "배경"(지시대로 건드리지 않음, 여전히 "화면정의서 v1.17" 인용 중이라 이번 v1.18로 다시 stale — planning-pl에게 보고 필요, 과거 v1.14~v1.17 라운드에서도 동일하게 반복 보고된 패턴) 외 라이브 본문 잔존 0건.
- 구버전 4개 `docs/planning/old/`로 이동: `01_..._v1.13.md`, `02_..._v1.17.md`, `04_..._v1.13.md`, `06_..._v1.12.md`.

### 2026-07-18 — 02 화면정의서 SCR-002 카테고리 nav 필터링 "표/Mermaid/범례" 정합화 v1.16→v1.17 + 인용 동기화 연쇄(01 v1.12→v1.13, 04 v1.12→v1.13, 06 v1.11→v1.12)
- 배경: v1.16 changelog가 "다음 라운드 후보"로 자기신고했던 갭 — §4 SCR-002 본문 256행(카테고리 nav 클릭 필터링 서술)은 이미 정확했지만, 같은 절의 "구성 요소와 동작" 표·Mermaid 플로우차트·와이어프레임 범례 주석 3곳에는 아직 반영이 안 돼 본문과 모순 상태였음. tech-architect 브리프(`scr002-filter-brief.md`)를 정확히 그대로 따라 반영하는 정합화 라운드(새 아키텍처 판단 없음, API 계약(`GET /contacts?category_id=`)은 01 FR-06에 최초부터 confirmed 상태였음을 브리프가 확인).
- **02 v1.17**: ① 표(기존 303~314행) — 기존 "[전체]" 행을 "[전체] 버튼 / 사이드바 nav '전체' 항목"으로 확장(검색창 비움 + active 표시 추가), 바로 아래 "사이드바 카테고리 nav 항목 클릭" 신규 행 삽입(`GET /contacts?category_id={id}`, 필터링+총N건 갱신+검색창 비움+active 표시, **재클릭해도 해제 안 됨 — 해제는 오직 "전체" 클릭으로만 가능**을 표에 직접 명문화). ② Mermaid(기존 318~373행) — `D`("[전체]") 라벨 확장, `D2 --> A` 다음에 신규 분기 `L`(`GET /contacts?category_id={id}`)→`L1`(401→`Z`, 성공→`L2`)→`L2`(필터링+active 표시+재클릭해도 해제 안 됨)→`A` 삽입, `classDef`의 `class B1,C1,D1,G1,I1,J1 decision`에 `L1` 추가(`L`/`L2`는 기존 `B2`/`C2`/`D2`와 동일하게 별도 classDef 없음). ③ 범례 주석(기존 241~242행) — "표시 전용" 삭제, "GET /categories로 채워짐(유지) + 클릭 시 GET /contacts?category_id=로 필터링" 병기(완전 교체 아니라 두 사실 보완). 브리프의 "대안 A(2줄 축약)" 적용, ASCII 박스 본문(좌측 그림)은 손대지 않고 "←" 오른쪽 텍스트만 교체.
- **발견만 하고 처리 안 함(다음 라운드 후보, changelog에 명시)**: qa-planner 브리프(`qa-planner-scr003-filter-gap-brief.md`)가 확인 — 06 문서 §5-4 SCR-003 표에 실제 구현·통과된 pytest 4건(`test_tc_e2e_scr003_09~12`)에 대응하는 계획 케이스가 없음(qa-planner가 `TC-E2E-SCR003-09~12` 초안 4개 이미 작성해 둠, 다음 라운드에서 사용자 승인 하에 반영 가능). 이번 라운드 범위(02의 표/Mermaid/범례) 밖이라 06 자체 케이스 신설은 하지 않음(06은 인용 버전만 동기화).
- **인용 동기화 연쇄**: 01(§3-5 "화면정의서 v1.3(현재 v1.16)"→"(현재 v1.17)", v1.12→v1.13), 04(§5 UC-09 문단 02 경로 `_v1.16.md`→`_v1.17.md`, v1.12→v1.13), 06(상단 근거 문서 표 + §1 역할 안내 문단의 01/02/04 인용 3곳, v1.11→v1.12 — changelog에 "SCR-003 케이스 갭은 이번에 채우지 않고 순수 인용 동기화만" 명시) 순으로 정정. 02 자신의 §4 SCR-004 "반영 완료" 문단이 04·01을 구버전(`_v1.12.md`)으로 인용하던 것도 01/04가 함께 v1.13으로 개정됨에 따라 `_v1.13.md`로 함께 정정(매 라운드 반복되는 동일 자기 소비 인용 패턴).
- **문자열 치환 함정 재확인**: v1.17 changelog 신규 항목 자체가 "class B1,C1,D1,G1,I1,J1 decision" 문자열을 설명 목적으로 인용하고 있어, classDef 라이브 코드 줄을 `replace_once`로 바꾸려 하자 count==2(라이브 코드 1 + 방금 추가한 changelog 설명문 1)로 assert 실패 — classDef 한 줄 대신 앞뒤 3줄(`classDef force ...`~`class Z force`)을 통째로 매칭 대상으로 넓혀 유일성을 확보해 재실행. 이전 라운드들의 "count>1이면 각 occurrence 문맥 확인" 원칙과 같은 패턴(이번엔 changelog가 원인이라는 변형).
- **버전 관리 절차**: `cp`로 새 버전 파일명 먼저 만든 뒤 python 스크립트로 문자열 치환(assert count==1, 실패 시 컨텍스트 넓혀 재시도) → `diff`로 지정 지점만 남았는지 확인 → 구버전 원본은 `mv`로 `old/`(01/02/04/06 4개 파일).
- 자체 점검(`### SCR-` 헤딩 전수): SCR-001/002/003/004/900 5개 헤딩 그대로, mermaid 블록 수(5개) 변경 전후 동일, 신규 노드(`L`,`L1`,`L2`) ID 중복 없음 확인.
- 최종 grep 스윕(`화면정의서_v1\.16\.md`, `구현요구사항_v1\.12\.md`, `PRD_v1\.12\.md`, `테스트계획서_v1\.11\.md`) 결과 04 v1.12 changelog 항목(역사적 인용, 그대로 둠) 1곳 외 라이브 본문 잔존 0건.
- `tech-architecture.md`는 지시 대상 아니라 건드리지 않았으나, §4 "배경" 문단(73행)과 하단 이력(104행)이 여전히 "화면정의서 v1.16"을 인용 중이라 이번 v1.17로 다시 stale해진 상태 확인 — planning-pl에게 보고 필요(tech-architect가 별도 라운드에서 처리해야 함, 과거 v1.14/v1.15 라운드에서도 동일하게 보고했던 반복 패턴).
- 구버전 4개 `docs/planning/old/`로 이동: `01_..._v1.12.md`, `02_..._v1.16.md`, `04_..._v1.12.md`, `06_..._v1.11.md`.

### 2026-07-18 — 02 화면정의서 SCR-002 카테고리 nav 필터링 기능 버전/changelog 정합화 v1.15→v1.16 + 인용 동기화 연쇄(01 v1.11→v1.12, 04 v1.11→v1.12, 06 v1.10→v1.11)
- 배경: dev-pl 팀 프론트엔드 구현 라운드(2026-07-17~18)에서 사이드바 카테고리 nav 클릭 필터링(`GET /contacts?category_id=`, "전체" 클릭 시 해제)이 사용자 결정으로 신규 확장·구현·qa-engineer 테스트 4건까지 완료됐으나, 02 문서는 버전 승격 없이 §4 SCR-002 본문에 인라인 태그 `**(2026-07-18 개정, 사용자 결정으로 기능 확장)**`만 삽입된 채 baseline(v1.15)에 방치 — harness-auditor 감사로 발견. 이번 라운드는 순수 버전/changelog 정합화만(스펙 자체는 이미 확정, 재판단·표/Mermaid 내용 확장 없음).
- **02 v1.16**: ① 헤더 버전 v1.16, 상태는 그대로 확정(Baseline). ② changelog 최상단에 배경·처리 내용·발견한 갭·인용 동기화를 서술하는 신규 항목 추가. ③ §4 SCR-002 "카테고리 nav 목록" 문단에서 인라인 태그와 "클릭 시 필터링이 없다는 이전 서술은 폐기됨"이라는 자기지시적 문구를 제거하고, v1.5 라운드("비밀번호 찾기→재설정" 명칭 확정 전례)와 동일하게 확정 상태만 서술하는 자연스러운 문장으로 정리(필터링 동작 자체 — `GET /contacts?category_id=`, "전체" 클릭 시 해제 — 는 그대로 유지). ④ §4 SCR-004 "반영 완료" 문단의 04·01 인용도 두 문서가 이번 라운드에서 함께 v1.12로 개정됨에 따라 `_v1.12.md`로 동기화.
- **발견만 하고 처리 안 함(다음 라운드 후보, changelog에 명시)**: SCR-002의 "구성 요소와 동작" 표와 화면 흐름 Mermaid 플로우차트에 카테고리 nav 클릭 필터링에 대응하는 행/분기가 없음. 와이어프레임 옆 범례 주석도 여전히 "카테고리 nav 목록: 표시 전용, GET /categories로 채워짐"이라고 남아 있어 본문 서술(필터링 있음)과 모순 — 이번 라운드 범위(버전 정합성) 밖이라 손대지 않음. (→ 위 v1.17 라운드에서 이 갭을 처리함.)
- **인용 동기화 연쇄**: 01(§3-5 "배경" 문단 "화면정의서 v1.3(현재 v1.15)"→"(현재 v1.16)", v1.11→v1.12 — 작업 전 필수 grep 사전 점검 중 발견한 stale 라이브 인용, 지시대로 함께 정정·보고), 04(§5 UC-09 문단 02 경로 `_v1.15.md`→`_v1.16.md`, v1.11→v1.12), 06(상단 근거 문서 표 + §1 역할 안내 문단의 01/02/04 인용 3곳, v1.10→v1.11) 순으로 정정.
- 구버전 4개 `docs/planning/old/`로 이동: `01_..._v1.11.md`, `02_..._v1.15.md`, `04_..._v1.11.md`, `06_..._v1.10.md`.

### 2026-07-17 — 02 화면정의서 SCR-900 Mermaid Q1 401 분기 구조적 공백 정정 + SCR-003 "목적" 불릿 신설 v1.14→v1.15 + 인용 동기화 연쇄(01 v1.10→v1.11, 04 v1.10→v1.11, 06 v1.9→v1.10)
- 배경: v1.11 라운드(2026-07-16)에서 "발견만 하고 처리 안 함"으로 changelog에 남겨뒀던 두 갭을 이번에 처리 — 둘 다 사용자가 이미 정확한 방향까지 확정한 지시(새 컨셉 판단 아님).
- **02 v1.15**: ① SCR-900 §4 Mermaid `Q1{"401인가? (단, SCR-001 로그인 시도 자체의 401은 예외)"}` 라벨이 괄호로 예외를 적어놓고도 그 예외로 가는 명시적 edge가 없어, 문자 그대로 따라가면 "로그인 시도 자체의 401"이 `Q1` Yes/No 어디에도 안 걸리고 `Q2`·`Q3`도 통과해 `End4`(해당 없음)로 흘러가는 구조적 공백을 정정 — `Q1`을 이진→3방향 분기로 바꿔 "Yes, 세션 만료(일반 401)"(기존 `R1`, 그대로)와 "Yes, 로그인 시도 자체의 401"(신규 `R1b`→"메시지 영역에 detail 표시, 현재 화면(SCR-001) 유지", 신규 종료 노드 `End1b`)을 분리. ② SCR-003만 다른 SCR-* 절과 달리 "- **목적**:" 불릿이 없이 "**레이아웃**:" 문장으로 바로 시작하던 것을, SCR-003 절 안에 이미 있는 내용만 요약해 신설(새 사실 지어내지 않음).
- **인용 동기화 연쇄**: 01(§3-5 "화면정의서 v1.3(현재 v1.14)"→"(현재 v1.15)", v1.10→v1.11), 04(§5 UC-09 문단 02 경로 `_v1.14.md`→`_v1.15.md`, v1.10→v1.11), 06(상단 근거 문서 표 + §1 역할 안내 문단의 01/02/04 인용 3곳, v1.9→v1.10) 순으로 정정.
- `tech-architecture.md`는 지시대로 건드리지 않음 — §4 "배경" 문단이 이미 "화면정의서 v1.14"를 인용 중이라 이번 v1.15로 다시 stale해짐, planning-pl에게 보고 필요(tech-architect가 별도 라운드에서 처리해야 함).
- 구버전 4개 `docs/planning/old/`로 이동: `01_..._v1.10.md`, `02_..._v1.14.md`, `04_..._v1.10.md`, `06_..._v1.9.md`.

### 2026-07-17 — 02 화면정의서 §8 "파괴적 동작 확인" 행 "1차 과제 대응" 칸 화면별 구분 v1.13→v1.14 + 인용 동기화 연쇄(01 v1.9→v1.10, 04 v1.9→v1.10, 06 v1.8→v1.9)
- 배경: harness-auditor가 v1.13(같은 행 "내용" 칸을 SCR-002/SCR-003 화면별로 구분) 재검증 중, "1차 과제 대응" 칸이 예전 문구 "(신규 — 확정 디자인 커스텀 모달)" 그대로 남아 SCR-002에만 맞고 SCR-003(네이티브 confirm)에는 안 맞는다고 지적 — 새 판단 아니라 v1.13에서 "내용" 칸에 적용한 구분을 "1차 과제 대응" 칸에도 마저 적용하는 지시.
- **02 v1.14**: §8 표 "파괴적 동작 확인" 행 "1차 과제 대응" 칸 1곳만 정정(같은 행 "내용" 칸은 v1.13에서 이미 맞아 손대지 않음).
- **인용 동기화 연쇄**: 01(v1.9→v1.10), 04(v1.9→v1.10), 06(v1.8→v1.9) 순으로 정정.
- 구버전 4개 `docs/planning/old/`로 이동: `01_..._v1.9.md`, `02_..._v1.13.md`, `04_..._v1.9.md`, `06_..._v1.8.md`.
</content>
