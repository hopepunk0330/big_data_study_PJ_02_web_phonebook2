# planning-writer 메모리

이 파일은 planning-writer의 최근 작업 로그(휘발성, 5개 캡)만 남깁니다. 각 기획 문서의 확정 내용 자체는 여기 없습니다 — `docs/planning/*.md`(현재 버전)와 `docs/planning/old/`(과거 버전 실물)가 소스입니다. 컨셉/BM 판단은 `docs/planning/service-concept.md`(service-planner 관리), 기술 판단은 `docs/planning/tech-architecture.md`(tech-architect 관리)를 따릅니다.

## 작업 로그

### 2026-07-16 — harness-auditor 감사 발견 4건 정정 (04 v1.4 / 01 v1.3 / 06 v1.1 / 05 v1.3)
- 배경: harness-auditor가 직전 라운드(TRD v1.2/02 v1.6/06 v1.0)를 감사해 발견한 결함 4건. 메인 세션이 사용자 승인("수정진행해") 확인, planning-pl이 파일을 직접 읽어 각 결함을 재확인한 뒤 지시. 모두 surgical(지적된 부분만) + 새 버전 파일 + 구버전 `old/` 이동.
- ① `04_PRD` v1.3→v1.4: §5 UC-09 상세 단계의 02 문서 인용이 이미 `old/`로 이동된 구버전(`_v1.5.md`)을 가리키던 것을 최신(`_v1.6.md`)으로 정정(160행). ② `01_구현요구사항` v1.2→v1.3: §3-5 "배경" 문단의 화면정의서 "현재 버전" 라벨이 stale(v1.5)하던 것을 v1.6으로 정정(293행). 둘 다 grep으로 문서 전체에서 해당 stale 인용이 정확히 1곳뿐임을 사전 확인 후 처리.
- ③ `06_테스트계획서` v1.0→v1.1: §4-4 카테고리 API 케이스가 실제 17개(01~16+07b)인데 16개로 집계돼 있던 것을 정정 — §4-4 소계(CATEGORY 16→17, 69개→70개), §10 총계(69→70, 129→130, 117→118) 3곳. 예외적으로 v1.0 changelog 항목 본문의 숫자(69/129/117)도 직접 고침(최초 집계 오류를 남기지 않기 위해, 새 v1.1 로그에 정정 사실을 별도 기록). 케이스 내용(ID·근거·시나리오) 자체는 전혀 손대지 않음 — 숫자만 수정.
- ④ `05_TRD` v1.2→v1.3: tech-architect 브리프(`scratchpad/trd-logout-footnote-brief.md`)를 그대로 반영 — §6 API 표 354행 로그아웃 행 "인증 필요" 칸에 각주 마커 `[^logout-auth]` 추가 + 표 바로 아래 각주 정의 1줄 삽입(01 FR-03 근거로 "인증 필요"가 401을 뜻하지 않음을 명확화, 06 문서 작성 중 실제 오독 전례가 재발 방지 근거).
- 자가 검증: 4개 문서 모두 `diff`(구버전 vs 신버전)로 지적된 부분 외 100% 동일 확인. 05 TRD 편집 중 sed 멀티라인 삽입이 changelog 헤더 앞에 잘못 꽂히는 실수가 있었으나 Python 스크립트로 재정렬해 바로잡음(원본 blank line 포맷도 복원). mermaid 블록 개수(04: 3, 01: 3, 05: 4) 변경 전후 동일 확인.
- 구버전 4개 전부 `docs/planning/old/`로 이동: `04_..._v1.3.md`, `01_..._v1.2.md`, `06_..._v1.0.md`, `05_..._v1.2.md`.

### 2026-07-16 — 06 테스트계획서 신규 작성 v1.0 (qa-planner 브리프 전체 반영, 129개 케이스)
- 근거: qa-planner 브리프(`scratchpad/06-test-plan-brief.md`) — 신규 문서. API 테스트 케이스 69(AUTH 15/PW 9/CONTACT 28/CATEGORY 16/SCREEN 1) + E2E 시나리오 38(SCR-001 7/SCR-004 6/SCR-002 12/SCR-003 8/SCR-900 5) + 데이터 격리 6 + 영속성 4 + 단위 테스트 후보 12(포함 여부 별도 확인 필요, 표시만 유지) = 합계 129(단위 제외 117). 표 내용(ID·근거·레벨·사전조건·입력·기대결과·우선순위)은 브리프 그대로 옮김(케이스 설계 자체는 qa-planner 몫).
- **브리프 §0 미해결 확인 사항 해소**: `POST /auth/logout`을 세션 쿠키 없이 호출할 때의 응답(TRD §6 "인증 필요" 표기 vs 01 FR-03 "이미 로그아웃 상태여도 200")을 planning-pl 지시대로 01 문서 FR-03 기준(멱등 로그아웃, 항상 200)으로 확정 — 근거는 PRD §10 "문서 간 충돌 시 01 우선" 규칙 + TRD §6 로그아웃 행의 실패 코드 칸이 원래 "-"로 비어 실제 텍스트 충돌이 없다는 점 2가지. `TC-API-AUTH-11`을 단일 결론(200)으로 반영하고 각주로 근거 표기, 두 갈래 대안은 문서에 남기지 않음.
- §7(단위 테스트 후보)의 "TRD §10-1 파일 구조 확장 여부는 tech-architect/planning-pl 확인 필요" 캐비어트는 이번 라운드 지시 범위 밖(§0과 별개 사안)이라 브리프 문구 그대로 보존(임의로 해소하지 않음).
- 자가 검증: 브리프-문서 간 `TC-*` ID를 grep으로 전수 대조(양쪽 137개 unique 토큰, diff 0), AC-01~18 매트릭스 18행 전부 확인.
- 01/02/03/04/05 인용 버전은 실제 `docs/planning/` 파일명 확인 후 표기(01 v1.2, 02 v1.6, 03은 본문 직접 인용 없음, 04 v1.3, 05 v1.2). 신규 문서라 `old/` 이동 없음.

### 2026-07-16 — 02 화면정의서 v1.5 → v1.6 (§4 SCR-004 "반영 완료" 문단의 03 문서 인용 stale 버전 정정)
- 근거: 사용자 지시(승인 완료) — 03 기능정의서가 2026-07-15에 v1.3으로 개정되었는데, 02 문서 §4 SCR-004 "반영 완료(v1.4)" 안내 문단 안의 03 문서 인용이 `..._v1.2.md`로 남아 stale해진 것을 정정. 같은 문단의 01/04 문서 인용은 이미 정확해 손대지 않음.
- 변경 지점은 정확히 3곳: ① 상단 메타 표 버전 v1.5→v1.6, ② 변경 이력 최상단에 v1.6 항목 추가(기존 v1.1~v1.5 로그는 그대로 보존), ③ 215번째 줄(§4 SCR-004 반영 완료 문단)의 `03_연락처관리_웹서비스_기능정의서_v1.2.md` 문자열 1곳을 `_v1.3.md`로 교체. Edit 도구가 없어 Bash `sed`로 라인 지정 치환(215번째 줄만 대상) — grep으로 해당 인용이 문서 전체에서 정확히 1곳뿐임을 사전 확인 후 처리.
- `diff`(old v1.5 vs 신규 v1.6) 결과 위 3곳 외 100% 동일 확인. §4의 다른 화면(SCR-001~003, SCR-900) 내용·Mermaid 코드블록 5개·와이어프레임은 전혀 건드리지 않음(mermaid 블록 개수 5개로 변경 전후 동일 확인).
- 구 v1.5 파일은 `docs/planning/old/02_연락처관리_웹서비스_화면정의서_v1.5.md`로 이동 보존.

### 2026-07-16 — TRD v1.1 → v1.2 (FR-14/15 비밀번호 재설정을 §6/§10/§11에 반영)
- 근거: tech-architect 브리프(`scratchpad/trd-v1.2-brief.md`) — 01 문서 §2/§3-5(FR-14/15)와 04 문서 §9(AC-18)가 이미 확정돼 있었음에도 TRD §6/§10/§11 세 곳에서 누락돼 있던 갭을 해소. 브리프 지시를 그대로 옮겨 씀(판단 없이 문서화만).
- 4곳 변경: ① §6 API 설계 요약 표에 `POST /auth/find-password`(FR-14)·`PATCH /auth/password`(FR-15) 행 추가(인증 불필요, 성공 200, 실패 404/422), ② §10-1 `tests/` 구조의 `test_auth.py` 주석에 "FR-14~15 → AC-18" 추가 + 파일을 신설하지 않는 근거 문장 1개 삽입(브리프가 "각주나 본문에 한 줄 권장"이라 명시), §10-2 "API 계약 (FR-01~13)" → "(FR-01~15)" 정정, ③ §11 요구사항 추적표에 FR-14~15 행 추가, ④ 변경 이력에 v1.2 항목 추가(브리프 문구 그대로) + 상단 메타 버전 v1.1→v1.2.
- `diff`(old v1.1 vs 신규 v1.2) 결과 위 4곳(버전값 1줄, changelog 1줄, §6 2행, §10-1 주석 수정+근거문장 1개, §10-2 1줄, §11 1행) 외 100% 동일 확인 — §1~§5, §7~§9, §12는 전혀 안 건드림. mermaid 블록 4개 개수 변경 전후 동일 확인(§6/§10/§11에는 애초 mermaid 없음).
- 구 v1.1 파일은 `docs/planning/old/05_연락처관리_웹서비스_TRD_v1.1.md`로 이동 보존. tech-architecture.md는 이미 tech-architect가 갱신 완료해 이번엔 건드리지 않음.

### 2026-07-15 — 03 기능정의서 v1.2 → v1.3 (§1-1 폴더/파일 구성 이름 체계를 canonical 구조로 통일)
- 근거: tech-architect 브리프(`scratchpad/03-structure-brief.md`) — 옛 `contact_app/` 단일 루트(래퍼 없이 평평한 배치)를 `docs/planning/tech-architecture.md` §1-1·05 TRD §3이 이미 확정한 `backend/`·`static/`·`frontend/` 형제 폴더 구조로 교체. 사용자가 이번 라운드 명시 승인.
- 범위: **오직 §1-1**(제목 바로 아래 도입 문장 신설 + 트리 교체 + 각주 신설) 3곳. 03 원본 파일별 설명 주석(예: "DB 연결: 엔진, DB세션 공장, get_db")은 전부 보존(Surgical — 05식 주석으로 교체하지 않음), 원본에 없던 `requirements.txt`·`frontend/`는 canonical 구조 실재 항목이라 신규 반영(각주는 05 TRD §3 문구 그대로 재사용).
- §1-1 직후 §1-2 "다이어그램 변환 안내(v1.1)" 문구는 §1-2 소속임을 v1.2 원문 재확인 후 손대지 않음(경계 착오 방지).
- **전수 확인 재검증**: 브리프가 "`contact_app` 문자열은 §1-1 트리 1곳뿐"이라 주장했지만, grep으로 직접 재확인(신뢰하되 검증) — 실제로 v1.2 전문에서 정확히 1곳(트리 내부)만 확인되어 브리프 판단이 맞음을 검증. §1-2~§7(FN-001~015 포함)은 전혀 손대지 않음.
- `diff`(old v1.2 vs 신규 v1.3) 결과, 버전 값 1줄·changelog 1줄·§1-1 도입문장 2줄·트리 전체(교체)·각주 2줄 외 100% 동일 확인.
- tech-architecture.md 갱신(§1-1 각주 "해소" 표기, §5 결정 이력 추가)은 브리프에 따라 tech-architect가 이미 직접 완료한 것으로 확인 — planning-writer는 03 md만 처리.
- mermaid 코드블록은 이번 변경(트리)에는 없어 해당 없음(§1-2/§3의 기존 mermaid 2개는 건드리지 않아 회귀 검증 불필요로 판단, 별도 mermaid-cli 실행 안 함).
- 구 v1.2 파일은 `docs/planning/old/03_연락처관리_웹서비스_기능정의서_v1.2.md`로 이동 보존.
