# planning-writer 메모리

이 파일은 planning-writer의 최근 작업 로그(휘발성, 5개 캡)만 남깁니다. 각 기획 문서의 확정 내용 자체는 여기 없습니다 — `docs/planning/*.md`(현재 버전)와 `docs/planning/old/`(과거 버전 실물)가 소스입니다. 컨셉/BM 판단은 `docs/planning/service-concept.md`(service-planner 관리), 기술 판단은 `docs/planning/tech-architecture.md`(tech-architect 관리)를 따릅니다.

## 작업 로그

### 2026-07-17 — 02 화면정의서 SCR-900 Mermaid Q1 401 분기 구조적 공백 정정 + SCR-003 "목적" 불릿 신설 v1.14→v1.15 + 인용 동기화 연쇄(01 v1.10→v1.11, 04 v1.10→v1.11, 06 v1.9→v1.10)
- 배경: v1.11 라운드(2026-07-16)에서 "발견만 하고 처리 안 함"으로 changelog에 남겨뒀던 두 갭을 이번에 처리 — 둘 다 사용자가 이미 정확한 방향까지 확정한 지시(새 컨셉 판단 아님).
- **02 v1.15**: ① SCR-900 §4 Mermaid `Q1{"401인가? (단, SCR-001 로그인 시도 자체의 401은 예외)"}` 라벨이 괄호로 예외를 적어놓고도 그 예외로 가는 명시적 edge가 없어, 문자 그대로 따라가면 "로그인 시도 자체의 401"이 `Q1` Yes/No 어디에도 안 걸리고 `Q2`·`Q3`도 통과해 `End4`(해당 없음)로 흘러가는 구조적 공백을 정정 — `Q1`을 이진→3방향 분기로 바꿔 "Yes, 세션 만료(일반 401)"(기존 `R1`, 그대로)와 "Yes, 로그인 시도 자체의 401"(신규 `R1b`→"메시지 영역에 detail 표시, 현재 화면(SCR-001) 유지", 신규 종료 노드 `End1b`)을 분리. `R1b`는 화면 유지이므로 `R2`처럼 별도 classDef 없이 기본 스타일(force 클래스는 `R1`에만 유지). `Q1` 라벨은 분기가 명시적으로 생겨 중복된 괄호 문구를 빼고 "401인가?"로 정리. 2열(상황/화면동작) 표는 이미 정확히 스코프가 맞아 있어 손대지 않음. ② SCR-003만 다른 SCR-* 절과 달리 "- **목적**:" 불릿이 없이 "**레이아웃**:" 문장으로 바로 시작하던 것을, SCR-003 절 안에 이미 있는 내용(카테고리 추가/이름수정/삭제, SCR-002 사이드바 레이아웃)만 요약해 SCR-002 목적 불릿과 같은 형식으로 신설(새 사실 지어내지 않음).
- **버전 관리 절차**: `cp`로 새 버전 파일명 먼저 만든 뒤 python 스크립트로 정확한 문자열 치환(assert count==1로 매치 유일성 검증) → `diff`로 지정 지점만 남았는지 확인 → 구버전 원본은 그대로 `mv`로 `old/`(01/02/04/06 4개 파일 모두 이 순서로 진행, 사고 없음).
- **인용 동기화 연쇄**: 01(§3-5 "화면정의서 v1.3(현재 v1.14)"→"(현재 v1.15)", v1.10→v1.11), 04(§5 UC-09 문단 02 경로 `_v1.14.md`→`_v1.15.md`, v1.10→v1.11), 06(상단 근거 문서 표 + §1 역할 안내 문단의 01/02/04 인용 3곳 `_v1.10/v1.14/v1.10`→`_v1.11/v1.15/v1.11`, v1.9→v1.10) 순으로 정정. 02 자신의 §4 SCR-004 "반영 완료" 문단이 04·01을 구버전(`_v1.10.md`)으로 인용하던 것도 미리 예측해 v1.15 최초 작성 시점에 `_v1.11.md`로 함께 반영(매 라운드 반복되는 동일 자기 소비 인용 패턴, 이번엔 최종 grep 전에 선반영해서 잡음).
- 자체 점검(`### SCR-` 헤딩 전수): SCR-001/002/003/004/900 5개 헤딩 전부 "- **목적**:" 불릿 보유 확인(SCR-003도 이제 완비), mermaid 블록 수(5개) 변경 전후 동일, SCR-900 Mermaid 신규 노드(`R1b`, `End1b`) ID 중복 없음 확인.
- 최종 grep 스윕(`화면정의서_v1\.(9|1[0-4])`, `구현요구사항_v1\.10\.md`, `PRD_v1\.10\.md`, `테스트계획서_v1\.9\.md`) 결과 changelog 줄 외 라이브 본문 잔존 0건(exit code 1 확인).
- `tech-architecture.md`는 지시대로 건드리지 않음 — §4 "배경" 문단이 이미 "화면정의서 v1.14"를 인용 중(직전 라운드에 tech-architect가 자체 동기화한 상태)이라 이번 v1.15로 다시 stale해짐, planning-pl에게 보고 필요(tech-architect가 별도 라운드에서 처리해야 함).
- 구버전 4개 `docs/planning/old/`로 이동: `01_..._v1.10.md`, `02_..._v1.14.md`, `04_..._v1.10.md`, `06_..._v1.9.md`.

### 2026-07-17 — 02 화면정의서 §8 "파괴적 동작 확인" 행 "1차 과제 대응" 칸 화면별 구분 v1.13→v1.14 + 인용 동기화 연쇄(01 v1.9→v1.10, 04 v1.9→v1.10, 06 v1.8→v1.9)
- 배경: harness-auditor가 v1.13(같은 행 "내용" 칸을 SCR-002/SCR-003 화면별로 구분) 재검증 중, "1차 과제 대응" 칸이 예전 문구 "(신규 — 확정 디자인 커스텀 모달)" 그대로 남아 SCR-002에만 맞고 SCR-003(네이티브 confirm)에는 안 맞는다고 지적 — 새 판단 아니라 v1.13에서 "내용" 칸에 적용한 구분을 "1차 과제 대응" 칸에도 마저 적용하는 지시.
- **02 v1.14**: §8 표 "파괴적 동작 확인" 행 "1차 과제 대응" 칸 1곳만 "(신규 — 확정 디자인 커스텀 모달)"→"(신규 — SCR-002는 확정 디자인 커스텀 모달, SCR-003은 웹 표준 관례)"로 정정(같은 행 다른 셀 "(신규 — 웹 표준 관례)" 패턴과 스타일 통일). "내용" 칸은 v1.13에서 이미 맞아 손대지 않음.
- **인용 동기화 연쇄**: 01(§3-5 "화면정의서 v1.3(현재 v1.13)"→"(현재 v1.14)", v1.9→v1.10), 04(§5 UC-09 문단 02 경로 `_v1.13.md`→`_v1.14.md`, v1.9→v1.10), 06(상단 근거 문서 표 + §1 역할 안내 문단의 01/02/04 인용 3곳, v1.8→v1.9) 순으로 정정. 최종 grep 재확인 중 02 자신의 §4 SCR-004 "반영 완료" 문단이 04·01을 구버전(`_v1.9.md`)으로 인용하던 것을 추가 발견해 같은 라운드 안에서 `_v1.10.md`로 함께 정정(v1.14 changelog에 추가 문장으로 반영, 새 버전 재발급 없이 amend — 매 라운드 반복되는 동일 자기 소비 인용 패턴 재발, 이번에도 최종 grep 전에 잡음).
- 자체 점검(`### SCR-` 헤딩 전수): SCR-001/002/003/004/900 5개 헤딩 그대로, mermaid 블록 수(5개) 변경 전후 동일. 최종 grep 스윕(`화면정의서_v1\.13`, `화면정의서_v1\.9\.md`, `구현요구사항_v1\.9\.md`, `PRD_v1\.9\.md`, `테스트계획서_v1\.8\.md`) 결과 changelog 줄 외 라이브 본문 잔존 0건(재확인 명령 exit code 1 = grep 무매치 확인).
- `tech-architecture.md`는 지시대로 건드리지 않음 — §4 "배경" 문단이 여전히 "화면정의서 v1.13"을 인용해 이번 라운드로 다시 stale해진 상태, planning-pl에게 보고 필요(tech-architect가 별도 라운드에서 처리해야 함).
- 구버전 4개 `docs/planning/old/`로 이동: `01_..._v1.9.md`, `02_..._v1.13.md`, `04_..._v1.9.md`, `06_..._v1.8.md`.

### 2026-07-17 — 02 화면정의서 SCR-002 "삭제" confirm→ConfirmModal 잔여 2곳 정정 v1.12→v1.13 + 인용 동기화 연쇄(01 v1.8→v1.9, 04 v1.8→v1.9, 06 v1.7→v1.8)
- 배경: harness-auditor가 v1.12(SCR-002 삭제 confirm→ConfirmModal 용어 정정) 재검증 중, 그 정정이 놓친 2곳을 추가 발견 — 이미 승인된 동일 원칙("확정 디자인 우선", 확정 디자인 `main-삭제` nodeId `501:3636`)을 마저 적용하는 지시였고 새 판단은 없었음.
- **02 v1.13**: 지정된 정확히 2곳만 정정 — ① §4 SCR-002 "레이아웃(와이어프레임)" ASCII 그림 옆 범례 주석(그림 자체는 그대로) "행의 [삭제]: confirm → DELETE /contacts/{id}"를 "행의 [삭제]: 삭제 확인 모달(ConfirmModal) → DELETE /contacts/{id}"로. ② §8 "화면 공통 규칙" 표 "파괴적 동작 확인" 행의 "내용" 칸 — v1.12에서 화면 전체에 일반화됐던 "삭제는 반드시 확인 모달(ConfirmModal)을 거침"이 SCR-002(커스텀 모달)·SCR-003(네이티브 confirm) 차이를 감추고 있어 "삭제는 반드시 확인 절차를 거침 — SCR-002(연락처)는 확인 모달(ConfirmModal), SCR-003(카테고리)은 확인 대화상자(confirm)"로 화면별 구분 명시("1차 과제 대응" 칸은 지시 범위 밖이라 미변경 — 이게 v1.14 라운드에서 harness-auditor가 지적한 잔여 갭이 됨). SCR-003 자체 표기·06의 `TC-E2E-SCR003-*`·Mermaid `H` 노드(이미 v1.12에서 정정 완료)는 손대지 않음.
- **인용 동기화 연쇄**: 01(§3-5 "화면정의서 v1.3(현재 v1.12)"→"(현재 v1.13)", v1.8→v1.9), 04(§5 UC-09 문단 02 경로 `_v1.12.md`→`_v1.13.md`, v1.8→v1.9), 06(상단 근거 문서 표 + §1 역할 안내 문단의 01/02/04 인용 3곳, v1.7→v1.8) 순으로 정정. 최종 grep 재확인 중 02 자신의 §4 SCR-004 "반영 완료" 문단이 04·01을 구버전(`_v1.8.md`)으로 인용하던 것을 추가 발견해 같은 라운드 안에서 `_v1.9.md`로 함께 정정(v1.13 changelog에 추가 문장으로 반영, 새 버전 재발급 없이 amend — v1.12 라운드와 동일한 연쇄 패턴 재발).
- 자체 점검(`### SCR-` 헤딩 전수): SCR-001/002/004/900 4종 세트 여전히 완비(내용 미변경 확인), mermaid 블록 수(5개) 변경 전후 동일. 최종 grep 스윕(`화면정의서_v1\.(9|1[0-2])`, `구현요구사항_v1\.[1-8]`, `PRD_v1\.[1-8]`) 결과 changelog 줄과 `service-concept.md`(작업 범위 제외, 역사적 서술)·05 TRD(changelog 내 역사적 인용) 외 라이브 본문 잔존 0건.
- `tech-architecture.md`는 지시대로 건드리지 않음(같은 시기 tech-architect가 별도로 처리 중이던 변경이 있어 그 diff와 섞이지 않게 손대지 않았음, 확인만).
- 구버전 4개 `docs/planning/old/`로 이동: `01_..._v1.8.md`, `02_..._v1.12.md`, `04_..._v1.8.md`, `06_..._v1.7.md`.

### 2026-07-16 — 02 화면정의서 SCR-002 "삭제" confirm→ConfirmModal 용어 정정 v1.11→v1.12 + 인용 동기화 연쇄(01 v1.7→v1.8, 04 v1.7→v1.8, 06 v1.6→v1.7)
- 배경: harness-auditor가 직전 라운드(SCR-002 삭제 확인 모달 와이어프레임 신설, v1.11)를 감사해 논리 모순 발견 — 사용자 승인: "삭제" 케이스도 "수정" 케이스(v1.1 전례)와 동일하게 확정 디자인 우선 원칙 적용, 문서 내 "확인 대화상자(confirm)" 문구를 확정 디자인(main-삭제 `501:3636`, 이미 v1.11에서 정확히 그려짐)에 맞춰 "삭제 확인 모달(ConfirmModal) 오픈"으로 정정하라는 지시.
- **02 v1.12**: 브리프가 지정한 정확히 3곳만 정정 — ① §4 SCR-002 구성요소-동작 표 "행의 [삭제]" 행(사용자 동작 칸 + 성공 시 반응 칸, 수정 모달 행 패턴과 통일). ② 같은 절 Mermaid `H` 노드를 `{"confirm"}`(decision)에서 `[["삭제 확인 모달(ConfirmModal) 오픈"]]`(F 노드와 동일 이중 대괄호)로 변경 + classDef를 decision→screen으로 이동(`class A,F,H,K screen`) + 진입 엣지에 "(호출 없음)" 주석 추가. ③ §8 화면 공통 규칙 표 "파괴적 동작 확인" 행(내용 칸 + 1차 과제 대응 칸). SCR-003(카테고리 삭제)의 "확인 대화상자"는 지시대로 건드리지 않음(확정 디자인에 카테고리 전용 모달 없음, `docs/design/missing-screens.md` 확인 근거).
- **인용 동기화 연쇄**: 01(§3-5 "현재 v1.11"→"v1.12", v1.7→v1.8), 04(§5 UC-09 문단 02 경로 `_v1.11.md`→`_v1.12.md`, v1.7→v1.8), 06(상단 표+§1 역할 안내의 01/02/04 인용 3곳, v1.6→v1.7) 순으로 정정. 최종 grep 재확인 중 02 자신의 §4 SCR-004 "반영 완료" 문단이 04·01을 구버전(`_v1.7.md`)으로 인용하던 것을 추가 발견해 같은 라운드 안에서 `_v1.8.md`로 함께 정정(v1.12 changelog에 추가 서술로 반영, 새 버전 재발급 없이 amend).
- **06 부수 처리(지시된 판단 범위)**: `TC-E2E-SCR002-09/10`의 "confirm에서 취소/확인" 문구를 02의 확정 용어에 맞춰 "삭제 확인 모달에서 취소/확인"으로 순수 용어 동기화(새 테스트 설계 아님, 케이스 ID·근거·우선순위 불변). `TC-E2E-SCR003-05/06/07`의 "confirm"(카테고리 삭제, 네이티브 confirm 맞음)은 그대로 둠.
- 자체 점검(`### SCR-` 헤딩 전수): SCR-001/002/004/900 4종 세트 여전히 완비, mermaid 블록 수(5개) 변경 전후 동일. 최종 grep 스윕(`화면정의서_v1\.(9|10|11)`, `구현요구사항_v1\.7`, `PRD_v1\.7`, `테스트계획서_v1\.6`) 결과 changelog 줄 외 라이브 본문 잔존 0건.
- 구버전 4개 `docs/planning/old/`로 이동: `01_..._v1.7.md`, `02_..._v1.11.md`, `04_..._v1.7.md`, `06_..._v1.6.md`.
