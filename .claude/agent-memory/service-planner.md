# service-planner 메모리

이 파일은 service-planner의 최근 작업 로그(휘발성, 5개 캡)만 남깁니다. **BM/서비스 컨셉 결정사항 자체(현재 확정 상태)는 여기 없습니다 — `docs/planning/service-concept.md`가 그 소스 오브 트루스입니다.** 작업 시작 시 이 로그가 아니라 그 문서를 먼저 읽으세요.

## 작업 로그

### 2026-07-14 — 신설
- design-prompter 패턴을 참고해 신설. 아직 실제 브리프 작업을 수행한 적 없음 — `docs/planning/service-concept.md`도 아직 생성 전(첫 실제 작업 때 이 역할이 만든다).

### 2026-07-14 — 화면정의서 v1.3 Mermaid 플로우차트 보완 브리프 + service-concept.md 최초 생성
- planning-pl 요청: SCR-001/002/003/004/900 5개 화면에 빠진 Mermaid 플로우차트 4종 세트 보완용 상태 분기 브리프 작성(문서는 안 씀, planning-writer가 씀).
- 화면정의서 v1.2 §4의 API 응답 코드(200/201/401/404/409/422)와 성공/실패 결과를 분기 흐름으로 재구성해 브리프 제공. SCR-900은 "모든 화면이 따르는 401/404·409·422/200·201·204 공통 결정 흐름"으로 정리.
- `docs/planning/service-concept.md` 최초 생성(brand-guide.md와 동일한 living-document 패턴). PRD PDF(04)를 읽고 SCR-004 "비밀번호 찾기" 스코프 결정(N1 비목표 대비 포함 결정, 3안 중 ③ "아이디만 확인" 채택 근거·트레이드오프)을 canonical화. 01/03/PRD에 API 2종이 아직 미반영이라는 불일치도 명시.
- 새로 발견한 정합성 이슈: PRD AC-04(로그인 실패 시 "아이디 존재 여부를 알 수 없는 문구"여야 함)와 SCR-004 1단계(404 "존재하지 않는 아이디입니다"로 존재 여부 노출)가 서로 다른 보안 원칙을 씀 — 결정을 뒤집을 필요는 없다고 판단했지만 service-concept.md §3에 기록해 둠.

### 2026-07-14 — PRD v1.0→v1.1 개정 브리프 (사용자 승인된 정합화 작업)
- planning-pl 요청: PRD를 PDF(v1.0)에서 md(v1.1)로 최초 전환하며 화면정의서 v1.3의 SCR-004(비밀번호 찾기)를 PRD에 정식 반영. PRD v1.0 PDF 전체를 직접 재확인했다(G1~G6, N1~N5, UC-01~08, PR-01~12, AC-01~17 전부 실제 존재 확인).
- 확정 판단 4건을 service-concept.md §3-1에 정리: (1) AC-04를 "로그인 실패 응답(POST /auth/login)에 한정"으로 범위 명시하는 문구로 개정(삭제·완화 아님), (2) N1을 분리해 "이메일 인증"만 남기고 "비밀번호 찾기/변경"은 비목표에서 제거, (3) §11 표의 "비밀번호 변경/찾기" 행을 "본인 확인 강화(이메일/보안질문, 향후 확장)"로 갱신, (4) UC-09/PR-13/AC-18 신설 문구 확정.
- 번호 검증 결과: UC-09/PR-13/AC-18 전부 PRD 원문 기준 실제 빈 번호 맞음. PRD는 원문에 "FR-" 인용 관례가 없음(§10처럼 문서 단위로만 참조) — 따라서 FR-14/15(tech-architect가 01 문서에 반영 예정)를 PRD에 직접 인용하지 말고 "01 문서 참조"로만 남기도록 권고.
- PR-13의 목표(G) 매핑은 G1으로 제안(비밀번호 찾기가 로그인 가능 상태로 복귀시키는 기능이라는 논리) — G7 신설도 가능하나 이번 브리프 범위 밖이라 언급만 하고 G1 편입을 기본안으로 제시.
- service-concept.md §4(구 "미해결" 절)를 갱신해, PRD 쪽은 이번에 해결되지만 01/03 문서와 화면정의서의 "FR-13" 임시 표기는 후속 라운드 필요함을 명시.

### 2026-07-14 — PRD v1.1→v1.2 개정 브리프: §4/§7 사용자 여정 다이어그램에 비밀번호 찾기 흐름 반영 (사용자가 v1.1 라운드의 "안 넣는다" 결정을 뒤집고 승인)
- planning-pl 요청: PRD §4(제품 구성, 정적 구조도)와 §7(전체 워크플로우, 사용자 여정)에 비밀번호 찾기 흐름을 추가. PRD v1.1·화면정의서 v1.4(SCR-004 Mermaid) 원문을 직접 재확인했다.
- 확정 판단: (1) §4는 "로그인 이후 데이터 격리"만 보여주는 정적 구조도라는 이유로 mermaid 코드는 그대로 두고 본문에 한 줄 각주만 추가(최종 권고, 근거 명시), (2) §7은 U2(로그인) 노드에서 분기하는 요약 노드 PW(노드 텍스트·화살표 레이블까지 확정: "비밀번호를 잊음" → PW → "재설정 완료 → 로그인 화면 복귀" → U2) 1개 + 화살표 2개만 추가, SCR-004가 완료 후 SCR-001(로그인 화면)로 복귀하는 지점과 논리적으로 같은 U2로 재합류시키고 실패 케이스(404)는 그리지 않기로 확정(SCR-004 전체 상태 분기 복제 방지), (3) 기존 v1.1 "다이어그램 변환 안내" 블록은 지우지 않고 그 아래에 v1.2 블록을 추가하는 정확한 문구 제시(§4/§7/PRD 최상단 변경 이력 3곳), (4) 01/03/tech-architecture.md/화면정의서 점검 결과 새 ID 신설·추가 갱신 불필요 확인(오히려 화면정의서 §3/SCR-001의 기존 분기 구조와 정합성이 개선됨) — 단 향후 SCR-004 번호가 바뀌면 PW 노드 텍스트도 함께 갱신해야 한다는 유지보수 주의사항만 기록.
- service-concept.md §3-1 (5)(원래 "안 넣는다" 결정)는 삭제하지 않고 상단에 "갱신됨 — §3-2 참고" 표시만 추가, 새 §3-2에 이번 판단 전체를 confirmed 브리프로 정리.

### 2026-07-14 — 명칭 통일 브리프: "비밀번호 찾기" → "비밀번호 재설정" (01/02/03/04 전체)
- planning-pl 요청: SCR-004로 이동하는 흐름 전체의 명칭을 "비밀번호 찾기"에서 "비밀번호 재설정"으로 통일(Figma는 사용자가 이미 반영 완료, 이 브리프는 기획 문서 4종 전용). 01/02/03/04 문서 원문을 직접 재확인해 "비밀번호 찾기" 문자열이 나오는 모든 위치를 확인했다(스냅샷을 신뢰하지 않고 재검증 — 예: 스냅샷이 "§3 화면 전이도"라고 지목한 문구가 실제로는 §2 화면 목록 표에 있었음).
- 두 층위로 판단: (층위 1) SCR-004 화면명·로그인 링크·UC-09·AC-18·화면 목록/전이도/매핑 표·Mermaid 노드 라벨 등 흐름 전체를 가리키는 표기는 전부 "비밀번호 재설정"으로 1:1 치환(이 흐름이 원래 "찾아주는" 게 아니라 "재설정하는" 기능이므로 정확성도 개선). (층위 2) FR-14/FN-014(1단계, 아이디 존재 확인만 함)는 "찾기"를 기계적으로 "재설정"으로 바꾸지 않고 "아이디 확인 (비밀번호 재설정 1단계)" 패턴으로 재구성(1단계에서 아직 재설정이 일어나지 않으므로). FN-015 소제목의 "비밀번호 재설정 — 비밀번호 찾기 2단계"라는 기존 병기도 "비밀번호 재설정 2단계 — 새 비밀번호 저장"으로 정리하도록 포함.
- API 경로(`POST /auth/find-password`)·스키마 클래스명(`FindPasswordIn/Out`)은 코드 식별자라 이번 브리프 범위 밖(tech-architect 소관)으로 명시. 각 문서의 "원문" 인용 블록·과거 changelog 로그·service-concept.md §3-1/§3-2의 감사 기록 블록은 역사적 사실 보존을 위해 고치지 않기로 결정(§3-2 상단에 안내 추가).
- 4개 문서 마이너 버전업 권고(01: v1.1→v1.2, 02: v1.4→v1.5, 03: v1.1→v1.2, 04: v1.2→v1.3) + 상호 버전 인용 스테일 방지 지점(01 §3-5 배경 문단, 02 SCR-004 "반영 완료" 문단)을 §3-3에 명시. service-concept.md §3 헤딩·본문은 새 명칭으로 직접 갱신, §3-1/§3-2는 원문 그대로 보존, 신규 §3-3에 문서별·위치별 변경 목록 전체를 브리프로 작성.
