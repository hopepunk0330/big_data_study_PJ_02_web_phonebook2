# qa-planner 메모리

이 파일은 qa-planner의 최근 작업 로그(휘발성, 5개 캡)만 남깁니다. 테스트 케이스 설계 결과 자체는 `docs/planning/06_연락처관리_웹서비스_테스트계획서_*.md`(planning-writer가 작성)를 확인하세요.

## 작업 로그

### 2026-07-14 — 신설
- 기획팀 3번째 브리프 담당으로 신설(service-planner=BM/컨셉, tech-architect=시스템/API, qa-planner=테스트 케이스). 아직 실제 작업 수행한 적 없음.

### 2026-07-16 — 06 테스트계획서 브리프 최초 작성
- PRD v1.3 §9(AC-01~18), TRD v1.2 §6/§10/§11, 01문서 v1.2 §3/§5-1, 02문서(화면정의서) v1.6 §4(SCR-001~004,900 Mermaid)를 종합해 테스트 케이스 브리프 작성. 스크래치패드: `06-test-plan-brief.md`.
- ID 체계: `TC-API-*`(API 15개 엔드포인트 통합), `TC-E2E-SCR0xx-*`(화면 흐름), `TC-ISO-*`(AC-13/14 데이터격리), `TC-PERSIST-*`(AC-16 영속성), `TC-UNIT-*`(단위, TRD §10-1 골격 밖이라 포함 여부 별도 확인 필요로 플래그).
- 총 129개 케이스 설계(단위 12개 제외 시 117개). AC-01~18 전부 최소 1개 이상 케이스로 커버 — 갭 없음.
- 문서 모순 1건 발견해 §0에 미해결 확인사항으로 기재(임의 확정 안 함): `POST /auth/logout`이 세션 쿠키 없이 호출될 때 TRD §6("인증 필요")와 01문서 FR-03("이미 로그아웃 상태여도 200") 문면이 긴장 관계 — planning-pl 확인 필요.

### 2026-07-18 — SCR-003 카테고리 nav 필터링 06 문서 갭 브리프
- 02 문서(화면정의서) v1.16이 SCR-002 카테고리 nav 클릭 필터링을 정식 반영했는데, 06 문서 §5-4 SCR-003 표는 `TC-E2E-SCR003-08`까지만 있어 실제 존재하는 pytest 4건(`tests/test_categories.py::test_tc_e2e_scr003_09~12` — nav 클릭 필터/active 상태(aria-pressed)/전체 클릭 해제/검색과 상호배타)에 대응하는 계획 케이스가 없는 걸 확인.
- `TC-E2E-SCR003-09~12` 4개 케이스(근거 SCR-002+SCR-003, 우선순위 P0/P1/P1/P1 — 필터 적용은 검색 적용(SCR002-04, P0)과 같은 급, 나머지는 "전체 클릭 복귀"(SCR002-05, P1)와 같은 급으로 맞춤) 설계해 브리프 작성. 스크래치패드: `qa-planner-scr003-filter-gap-brief.md`.
- PRD(04)·tech-architecture.md 확인 결과: `GET /contacts?category_id=` API 계약은 01문서 FR-06에 최초부터 있었고 `TC-API-CONTACT-16`(API 레벨)도 이미 존재 — 이번 갭은 프론트 UI 트리거(nav 클릭)에 대한 E2E 계획 케이스 부재일 뿐, PRD AC 신설이나 tech-architecture API 계약 추가는 불필요하다고 판단.
- 06 문서 §5-4 소계(SCR-003 8→12)·§5 총소계(38→42)·§10 총계(130→134, 단위제외 118→122)도 다음 라운드에 함께 갱신 필요하다고 브리프에 명시(과거 v1.1 집계 누락 재발 방지).
