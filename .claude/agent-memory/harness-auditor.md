# harness-auditor 감사 로그

로그가 5개를 넘으면 가장 오래된 항목부터 삭제(git history에 전체 보존됨).

---

## 2026-07-16 (5차) — docs/planning 최종 재검증 (B 범위: 버전 인용/AC 추적/산술/스키마)

배경: planning-pl 라운드가 stall로 실패해 메인 세션이 직접 02 화면정의서(v1.6→v1.7) stale 인용 정정 + 그걸 인용하는 문서 갱신을 마무리. "이번이 마지막 확인"이라는 전제로 6개 확정 문서(01 v1.4/02 v1.7/03 v1.3/04 v1.4/05 v1.4/06 v1.2) + tech-architecture.md + service-concept.md 전수 재검증.

발견 2건 (심각도순, 둘 다 "02 화면정의서가 v1.6→v1.7로 재차 개정됐는데 그걸 인용하던 문서 일부가 못 따라간" 동일 패턴의 재발 — 2차 감사 때 지적한 것과 완전히 같은 클래스):

1. **[중간] 01 구현요구사항서 v1.4 §3-5 "배경" 문단이 02 문서를 stale v1.6으로 인용** — `docs/planning/01_연락처관리_웹서비스_구현요구사항_v1.4.md` line 295: `"화면정의서 v1.3(현재 v1.6) SCR-004가 이미 정의한..."`. 02 문서는 이번 라운드에서 v1.7로 재차 개정됐는데(자체 changelog에 "harness-auditor가 발견"이라고 적혀 있을 정도로 이미 알려진 이슈였음) 01의 이 인용은 갱신되지 않음. 참고로 01 자신의 v1.3 changelog(line 19)는 "v1.5→v1.6 정정 완료"라고 기록했지만, 그 이후 02가 다시 올라가면서 재차 stale해진 것. → 확인 필요.
2. **[중간] tech-architecture.md §4 "배경" 문단도 02 문서를 stale v1.6으로 인용** — `docs/planning/tech-architecture.md` line 73: `"화면정의서 v1.6 SCR-004가 이미 정의한..."`. §4는 문서 자체가 "5. 결정 이력"과 달리 "여전히 유효한 canonical 절"이라고 스스로 명시한 살아있는 절(§4 code 확정 표 등이 계속 인용되는 곳)이라, 과거 판단 보존이 아니라 실제 stale 참조임. 2026-07-16 결정 이력(line 101)에 "v1.3→v1.6 정정 완료"라고 남아 있지만 그 이후 02가 v1.7로 다시 올라가며 재차 stale해짐. → 확인 필요.

이번 라운드에서 main session이 "02를 인용하는 3개 문서"를 갱신했다고 했는데, 실제로 v1.7까지 정확히 반영된 곳은 04 PRD(§5 UC-09, line 161)와 06 테스트계획서(근거 문서 표·§1, line 11/13) 2곳뿐이었고, 01과 tech-architecture.md는 누락됨(둘 다 v1.6에 멈춤). "3개 문서"가 정확히 어느 3개를 가리켰는지는 확인 불가하나, 최소 이 2곳은 여전히 stale.

### 확인해서 문제 없었던 것 (이번 재검증 통과)
- **AC-01~18 커버리지**: 06 §9 매트릭스에 18개 전부 최소 1개 케이스로 커버됨(갭 없음) — 재확인 완료.
- **06 자체 산술**: API 그룹 소계(AUTH 15 + PW 9 + CONTACT 28 + CATEGORY 17 + SCREEN 1 = 70) 실제 나열 개수와 정확히 일치(각 그룹 ID를 직접 세어 대조). CATEGORY는 이전 16→17 오류가 실제로 정정되어 17로 확인됨. E2E 그룹 소계(7+6+12+8+5=38), ISO(6), PERSIST(4), UNIT(12), 총계(130, 단위 제외 118) 전부 실제 나열과 일치 — 산술 불일치 없음.
- **DB 재연결 설계**: TRD v1.4 §4-4 `pool_pre_ping=True` 코드와 tech-architecture.md §1/§5 결정 기록(2026-07-16 항목)의 근거 서술(NFR-01, `pg-lab` 재시작 시나리오)이 문구 수준까지 거의 동일 — 정합.
- **비밀번호 재설정 스키마**: TRD §8-1 `FindPasswordIn`(username만)/`FindPasswordOut`(username echo, id 없음)/`ResetPasswordIn`(username+new_password)/`MessageOut`이 01 §3-5 산문 설명(요청/응답 예시) 및 03 §4 FN-003 표 설명과 필드명·값 전부 일치.
- **FR-01 409 문구**: 01 §3-1 `{"detail": "이미 사용 중인 아이디입니다"}`와 06 `TC-API-AUTH-02`의 기대값이 완전히 동일한 리터럴.
- 04 v1.4 §5 UC-09(line 161)·06 v1.2(근거 문서 표, §1 역할 안내)의 02 인용은 v1.7로 정확히 갱신되어 있음(2차 감사 발견 #2 계열 재발 없음, 이 두 곳은 이번엔 문제 없음).
- 03 문서는 02 버전을 인용하는 곳 자체가 없어(grep 0건) 해당 없음.
- service-concept.md 안의 "화면정의서 v1.3" 등은 전부 날짜 붙은 결정 이력/브리프 기록(역사적 서술)이라 갱신 대상 아님(오탐 아님).

### 패턴 메모 (반복 재발 중)
2차 감사(v1.5→v1.6 갱신 때 01·02 stale) → 이번 5차(v1.6→v1.7 갱신 때 01·tech-architecture.md stale). 02 화면정의서가 버전업될 때마다 "02를 인용하는 모든 문서"를 전수 grep하지 않고 일부만 갱신하는 실수가 매 라운드 재발하는 중. 다음에 02가 다시 버전업되면 첫 번째로 `grep -rn "화면정의서.*v1\." docs/planning/*.md docs/planning/tech-architecture.md`로 전수 확인할 것을 제안.
