# harness-auditor 감사 로그

로그가 5개를 넘으면 가장 오래된 항목부터 삭제(git history에 전체 보존됨).

---

## 2026-08-26 (98차) — 사용자 명시 요청: mercari B범위 최종 확인 라운드 — brand-guide.md 신규 "10. 인터랙션 정의"(interaction-designer) + graphic-assets.md 신규 "SCR-004/005/006 오브제 보강" 절(graphic-designer) append 검증 — B 범위

**결과: HIGH 1건 신규 발견(icon-craft-guide.md의 "면색 없음 — 예외 없음" Basic 트랙 규칙을 graphic-assets.md 신규 Selection Indicator Selected 상태가 정면 위반, 같은 절의 Unselected 상태는 같은 규칙을 명시적으로 인용해 준수하고 있어 자기모순). MEDIUM 1건(Choice Card / Choice Card - Comparison Pair 네이밍 혼동 위험, ID 미기재로 동일 컴포넌트 여부 확인 불가). LOW 2건. "정정 포인터가 스스로 잘못된 이력을 주장하는" 기존 반복 패턴은 이번 신규 두 절(10절, SCR-004/005/006절)엔 없음 — 둘 다 순수 신규 관찰 기록이라 재정정 이력 자체가 없음.**

### 신규 발견
1. **[HIGH]** `docs/design/mercari/graphic-assets.md` "SCR-004/005/006 오브제 보강" > "1. Selection Indicator — 16×16 (Basic/Utility 트랙)"의 **Selected 상태**는 "Ellipse 16×16, fill `#3182F6`(`color/primary`), stroke 없음"으로 브랜드색 솔리드 원 채움을 쓴다. 그런데 `docs/harness/design-team/icon-craft-guide.md` 37번째 줄은 Basic/Utility 트랙 규칙으로 "면색(fill)을 넣지 않는다 — 순수 스트로크(outline)만 사용한다. **브랜드색이든 다른 색이든 면을 채우지 않는다**"를 예외 없이 못박고 있다. 같은 Selection Indicator의 **Unselected 상태**(바로 다음 줄)는 "fill 없음(`fills: []`, 완전 투명 — Basic 트랙 '면색 없음' 원칙 그대로)"라고 이 규칙을 명시적으로 인용해 스스로 판단 기준을 밝혔음에도, 정작 Selected 상태는 같은 트랙 라벨을 단 채 그 원칙을 어겼다 — 이미 확립된 아이콘(`chevron-right`, Basic 트랙, `fills: []`로 명시 확인됨)과도 대비된다. 예외를 만들려는 의도적 서술(왜 Selected만 예외인지 설명)도 전혀 없다. 확인 필요 — Selection Indicator를 Visual/Feature 트랙으로 재분류하거나, Selected 상태의 craft를 stroke 기반으로 다시 그려야 하는지 판단 필요.
2. **[MEDIUM]** `docs/design/mercari/brand-guide.md` 10-3이 다루는 `Choice Card - Comparison Pair`(SCR-004, "설문UI" 페이지)는 노드 ID가 전혀 제시되지 않아, 9절(design-systems 라운드)이 이미 "Choice Card 마스터 내부에서 INSTANCE로 존재"한다고 서술한 `Choice Card`(SCR-002, "UI 디자인" 페이지, `187:x` 대역)와 동일 컴포넌트인지 이름만 비슷한 별개 컴포넌트인지 문서만으로 판별 불가능하다. 동일 컴포넌트라면 "이미 컴포넌트 마스터+INSTANCE로 존재" vs "raw FRAME, variant INSTANCE 아님"이 정면 충돌한다. 각기 다른 페이지·화면 계열(SCR-002 vs SCR-004)에 속해 별개 컴포넌트일 가능성이 높지만, 이름의 높은 유사성과 ID 부재로 확신할 수 없음 — 확인 필요.

### 확인됨(문제 없음)
- **참조 무결성**: 10-1이 인용한 `component-state-guide.md` 2번("버튼류: Default/Hover/Press/Focus/Disabled")과 10-1·10-2·10-3이 인용한 `motion-timing-guide.md` 1번("화면 전환 200~400ms")·3번("설명하지 못하는 움직임은 뺀다")을 원문 대조 — 절 번호·문구 모두 정확히 일치.
- **노드 ID 상호 참조**: `258:169`("기타" Reason List Item)가 graphic-assets.md(Selection Indicator 대상 목록)와 brand-guide.md 10-2(모달 트리거) 양쪽에서 동일 용도로 정확히 일치. `258:152`(shield-check 인스턴스)·`258:160/165`·`258:161/166/170` 등 신규 노드 ID가 두 문서·기존 섹션 어디와도 충돌하지 않음(grep 재확인).
- **색상/토큰 일치**: Selection Indicator·Celebration Badge·Confirmation Badge가 쓰는 `color/primary`(`#3182F6`)·`color/bg-surface`(백색)·`color/border-neutral`(`#E2E6EC`)·`color/bg-accent-tint`(`#E8F3FF`) 전부 brand-guide.md 1절 색상표 hex와 정확히 일치, 새 hex 발명 없음.
- **트랙 판정 근거 인용**: `docs/harness/design-team/icon-craft-guide.md`의 트랙 판정 기준·이모지 정책 원문과 graphic-assets.md의 인용이 일치(문구 자체 왜곡 없음 — 단, 적용 결과는 위 HIGH #1 참고).
- **append-only**: 두 파일 모두 신규 절이 문서 맨 끝(9절 뒤 10절, 기존 "이어지는 라운드" 절 뒤 신규 절)에 추가됐고 "근거" 절도 기존 항목을 편집하지 않고 새 불릿만 추가하는 형태 — 96·97차가 지적했던 "고쳐서 되돌리는" 유형의 재편집 흔적 없음.
- **정정 포인터 자기서술 문제 재발 여부**: 10절·SCR-004/005/006 절 둘 다 순수 신규 관찰·설계 기록이라 "정정"·"재정정" 표현 자체가 등장하지 않음 — 94~97차에 반복됐던 "편집 안 했다고 말했지만 실제로는 편집함" 유형 문제 이번 라운드엔 해당 없음.

### 낮은 확인 필요(참고성)
1. **[LOW]** 10-1이 CTA 컴포넌트셋을 `124:1148`(variant 값 `CTA Button`/`CTA Button Disabled`)로 서술하는 반면, graphic-assets.md "이어지는 라운드" 절은 같은 두 variant를 `124:1135`(Disabled)·`124:1136`(Active)라는 다른 ID와 "Active"/"Disabled"라는 다른 명칭으로 서술한다. 부모 컴포넌트셋 ID와 자식 variant ID가 다른 것 자체는 Figma 구조상 자연스러울 수 있고 명칭도 비형식적 서술일 가능성이 높아 확정 모순으로 보진 않으나, 두 문서만 보고는 같은 컴포넌트를 가리키는지 교차 검증할 수 없다.
2. **[LOW]** 10-1은 CTA 컴포넌트셋에 Hover·Focus 상태도 없다는 것을 스스로 확인해놓고("State 축이 전혀 없다"), `component-state-guide.md` 2번의 "버튼류는 예외 없이 5개 상태(Default/Hover/Press/Focus/Disabled)를 항상 갖춘다"는 규칙에 비춰 Press만 design-systems에 요청하고 Hover/Focus는 왜 이번 요청 범위에서 빠졌는지 문서에 설명이 없다. 인터랙션 정의 브리프 스코프상 의도된 축소일 가능성이 높으나(디자인 QA 영역과 겹침), 명시적 사유는 없음.

### 확인 안 함(범위 밖)
- 83차 LOW #5·#6, 89차 LOW(mercari brand-guide.md 근거절이 agent-memory 캡 대상 로그 인용)는 이번 라운드 변경 범위와 무관해 재확인하지 않음 — 계속 미해결 상태로만 기록.
- `graphic-assets.md` "정리 결과 요약" 표(94차 LOW #1)가 여전히 CTA 불일치를 미해결처럼 적어두는 문제는 변경 없음 — 신규 이슈 아님.
- B 범위 버전 인용 정합성(`docs/planning/**`)은 이번 라운드에서 해당 문서 변경이 전혀 없어 재점검 생략.

### 패턴 메모(누적, 갱신)
- **신규 패턴(98차): 신규 오브제를 만들 때 "이 상태는 원칙을 지켰다"고 스스로 명시적으로 인용하는 습관(Unselected 상태의 '면색 없음 원칙 그대로')이 오히려 바로 옆 형제 상태(Selected)의 위반을 더 도드라지게 만든다 — 한쪽 상태만 규칙을 인용하고 다른 쪽은 조용히 어기면, 작성자가 규칙을 몰랐던 게 아니라 알면서도 예외를 만들었다는 인상을 준다. 같은 오브제의 여러 state를 한 절에서 함께 서술할 때는 "이 절 전체가 같은 트랙 규칙을 따르는가"를 상태별로 한 번씩 점검할 필요가 있다.**

---

## 2026-08-26 (95차) — 사용자 명시 요청: 94차 발견 MEDIUM 3건에 대해 design-pl이 추가한 append-only 정정 포인터 3건 재검증 — `docs/design/mercari/graphic-assets.md`(1건)·`docs/design/mercari/brand-guide.md`(2건) — B 범위

**결과: 94차 MEDIUM #1·#3은 해소로 판단. MEDIUM #2는 요청받은 좁은 목적(토큰명 불일치 명시)은 달성했으나, 정정 문장 자체가 같은 문단 안의 다른 서술과 새 모순을 만듦(신규 MEDIUM 1건). #1 관련 LOW 1건 신규 발견(원인 서사 불일치).**

### 확인됨(해소)
- **94차 MEDIUM #1(graphic-assets.md만 읽으면 opacity 100%가 진실처럼 남음)**: 라인 37에 "brand-guide.md 9-3에서 실측 재확인 결과 opacity 0.3으로 정정됐다"는 포인터가 원 관찰 기록 바로 뒤에 추가됨 — graphic-assets.md만 읽는 독자도 이제 정정 사실과 최종값(토큰 `color/text-muted-35-on-surface-muted`, 합성 hex `#AAABB1`)을 알 수 있다. 목적 달성, 해소로 판단(단, 아래 LOW 참고).
- **94차 MEDIUM #3(신설 "Colors" 페이지 노드 ID 누락 + 스코프 근거 약함)**: brand-guide.md 9-4에 페이지 ID `233:143`(canvas 타입)·프레임 ID `234:143`이 추가됨 — 기존 `205:x`/`206:x` 노드 ID 대역과 겹치지 않아 충돌 없음(grep 재확인). 스코프 근거("9-2/9-3 변수 바인딩 작업의 문서화 산출물이라 별도 스코프 확장이 아니다")도 신설돼, 9절 헤더가 선언한 라운드 범위(변수 신설·컴포넌트 바인딩·스펙 시트)와 논리적으로 연결됨. 해소(이 근거가 "충분히 설득력 있는지"는 판단 대상 밖이라 사용자 확인 필요로 남김).

### 신규 발견
1. **[MEDIUM]** `docs/design/mercari/brand-guide.md` 9-2(라인 147-148)의 원 문장은 "Primitives+Semantic Colors" 세트가 "옵션시티별 `-on-white`/`-on-tint`/`-on-cta-disabled` 합성 hex까지... 이미 계산돼 있었음"이라고 서술해, 이 셋 모두를 실제 Figma 변수 컬렉션에서 관찰(발견)된 값처럼 단정했다. 그런데 바로 뒤에 이번에 추가된 정정 포인터는 "`-on-cta-disabled`는 실제로 존재하는 토큰 이름이 아니라 접미사 패턴을 보여주기 위한 예시였다"고 말해, 같은 문장이 열거한 세 항목 중 하나가 사실은 실제 관찰이 아니라 예시였음을 드러낸다. 이는 design-pl이 해결하려던 좁은 목적(9-3의 실제 토큰명과의 불일치 설명)은 달성했지만, 원 문장의 "이미 계산돼 있었음"이라는 서술 방식 자체의 신뢰성에 새 의문을 남긴다 — 나머지 두 항목(`-on-white`/`-on-tint`)도 실제 관찰인지 마찬가지로 예시적 설명인지 문서 어디에도 검증 근거가 없다(두 문서 전체에서 이 두 접미사는 이 한 문장에만 등장, grep 확인). 확인 필요 — 원 문장을 "옵션시티별 합성 hex 접미사 패턴(예: `-on-white`/`-on-tint` 등)이 이미 계산돼 있었음"처럼 예시 표현으로 다시 쓰거나, `-on-white`/`-on-tint`가 실제 토큰명이라면 그 근거를 별도로 명시할 필요가 있는지 design-pl이 판단할 사안.

### 낮은 확인 필요(참고성)
1. **[LOW]** `graphic-assets.md` 신설 포인터는 opacity 불일치를 "정정됐다"(실측 재확인 결과 이전 기록이 틀렸다는 뉘앙스)로 서술하는데, 정작 그 근거로 지목한 `brand-guide.md` 9-3 원문(라인 161, 이번 라운드에서 미수정)은 같은 현상을 "그 사이 누군가 opacity만 손댄 흔적"이라며 값 자체가 두 관찰 시점 사이에 바뀌었다는 다른 가설을 스스로 제시하고 있다 — "원래 기록이 잘못됐다"(측정 오류)와 "그 사이 실제 값이 변경됐다"(드리프트)는 서로 다른 설명이라, 새 포인터가 어느 쪽을 뜻하는지 문면상 불명확하다. 실질적 결론(현재 정답은 0.3/토큰 `#AAABB1`)에는 영향 없어 LOW로 유지.

### 확인 안 함(범위 밖)
- 83차 LOW #5·#6, 89차 LOW(mercari brand-guide.md 근거절이 agent-memory 캡 대상 로그 인용)는 이번 라운드 변경 범위와 무관해 재확인하지 않음 — 계속 미해결 상태로만 기록.
- `graphic-assets.md` "정리 결과 요약" 표(94차 LOW #1, 라인 62)가 여전히 CTA 불일치를 미해결처럼 적어두는 문제는 이번 라운드에서도 그대로(수정 대상 아니었음) — 신규 이슈 아님.
- B 범위 버전 인용 정합성(`docs/planning/**`)은 이번 라운드에서 해당 문서 변경이 전혀 없어 재점검 생략.

### 패턴 메모(누적, 갱신)
- **신규 패턴(95차): 좁은 불일치 하나를 해명하려고 넣은 정정 포인터가, 그 불일치를 감싸고 있던 원 문장의 "이미 다 확인된 사실"이라는 더 넓은 단정까지 무심코 흔들 수 있다 — 예시 3개 중 1개만 콕 집어 "사실은 예시였다"고 정정하면, 나머지 2개도 같은 종류의 신뢰도 의문을 새로 얻는다(정정 대상이 아니었는데도). 부분 정정을 append할 때는 "이 정정이 같은 문장/문단의 다른 이웃 항목들에 대해서도 똑같이 의문을 유발하지 않는가"를 함께 점검할 필요가 있다 — 91차("예외를 한쪽에만 반영")·94차("불일치를 한쪽 문서만 인지하고 다른 쪽에 반영 안 함")와 마찬가지로 "부분 수정의 파급 범위 과소평가" 계열이지만, 이번엔 수정 대상 문서 자신의 내부(같은 문단 안)에서 파급이 발생했다는 점이 다르다.**

---

## 2026-08-26 (96차) — 사용자 명시 요청: 95차 발견 2건(MEDIUM 1, LOW 1)에 대해 design-pl이 다시 고친 재정정 문장 재검증 — `docs/design/mercari/brand-guide.md`(9-2절)·`docs/design/mercari/graphic-assets.md`(opacity 포인터) — B 범위

**결과: 95차 MEDIUM·LOW 둘 다 해소 확인. 다만 두 파일이 "같은 유형의 재정정 요청"에 서로 다른 방식으로 대응해 새 MEDIUM 1건 발견 — brand-guide.md 9-2는 원 관찰 문장 자체를 편집한 것으로 보이는 반면, graphic-assets.md는 원 문장을 보존하고 포인터만 개정.**

### 확인됨(해소)
- **95차 MEDIUM(brand-guide.md 9-2, `-on-cta-disabled`만 예시로 밝혀져 `-on-white`/`-on-tint`의 실재 여부가 불명확해짐)**: 재정정 문장이 "원문이 예로 들었던 `-on-white`/`-on-tint`/`-on-cta-disabled`는... 실제로 존재하는 개별 토큰명을 전수 나열한 것이 아니라 접미사 패턴을 보여주는 예시였다"로 세 항목을 동일하게 예시로 재분류하고, 실제 토큰 목록은 9-4절 Colors 카탈로그(`234:143`)를 보라는 포인터를 추가함 — 목적 달성, 해소.
- **95차 LOW(graphic-assets.md opacity 포인터가 "정정됐다"고 단정해 brand-guide.md 9-3의 드리프트 가설과 결이 다름)**: "두 기록 중 어느 쪽이 측정 오류였는지, 혹은... 실제로 값이 바뀌었는지는 확정할 근거가 없다"로 재작성돼 brand-guide.md 9-3의 "그 사이 누군가 opacity만 손댄 흔적" 가설을 명시적으로 인용 — 두 문서가 같은 불확실성을 공유하는 형태로 정합. 해소.

### 신규 발견
1. **[MEDIUM]** brand-guide.md 9-2절의 재정정 방식이 자신이 선언한 append-only 원칙(`figma-file-organization.md` 2-3/2-4번, "새 라운드마다 새 섹션으로 추가, 덮어쓰지 않음")과 다르게 처리된 것으로 보인다. 94차/95차 로그가 인용한 원 문장("...옵션시티별 `-on-white`/`-on-tint`/`-on-cta-disabled` 합성 hex까지... 이미 계산돼 있었음")과 달리, 현재 base 문장은 "옵션시티별 합성 hex가 이미 계산돼 있었다"로 세 구체 접미사명이 빠져 있다 — 정정 포인터를 뒤에 추가하는 방식이 아니라 원 관찰 문장 자체를 편집한 것으로 보인다. 같은 재정정 요청을 받은 graphic-assets.md의 opacity 포인터는 원본 관찰 기록("불투명도 100%")을 그대로 남기고 포인터 텍스트만 재작성했다("이 문서의 원본 관찰 기록은 정정하지 않고 그대로 두며, 참고 포인터만 남긴다"고 명시) — 두 파일의 처리 방식이 대비된다. brand-guide.md 근거절(라인 186)도 94차 최초 정정에는 "순수 문서 append"라 명시했지만 95차발 재정정엔 그 표현을 반복하지 않아 방증이 된다. 부수 효과로, base 문장에서 세 접미사명이 사라지면서 정정 포인터 안의 "원문이 예로 들었던..." 구절이 그 이름들이 등장하는 유일한 자리가 됐다 — 이 문서만 읽는 독자는 "원문"이 정확히 무엇이었는지 되짚을 수 없다. git diff 도구 없이 인용문 대조로 추정한 것이라 확인 필요.

### 확인 안 함(범위 밖)
- 83차 LOW #5·#6, 89차 LOW(mercari brand-guide.md 근거절이 agent-memory 캡 대상 로그 인용)는 이번 라운드 변경 범위와 무관해 재확인하지 않음 — 계속 미해결 상태로만 기록.
- `graphic-assets.md` "정리 결과 요약" 표(94차 LOW #1)가 여전히 CTA 불일치를 미해결처럼 적어두는 문제는 이번 라운드에서도 변경 없음 — 신규 이슈 아님.
- B 범위 버전 인용 정합성(`docs/planning/**`)은 이번 라운드에서 해당 문서 변경이 전혀 없어 재점검 생략.

### 패턴 메모(누적, 갱신)
- **신규 패턴(96차): "정정 포인터"라는 장치 자체는 append-only를 지키기 위한 도구인데, 그 포인터를 다시 고쳐야 하는 후속 라운드(재정정)에서 "포인터 텍스트만 개정"과 "포인터가 감싸고 있던 원 관찰 문장까지 함께 편집"이 구분 없이 섞이면, 같은 하네스 안에서도 파일마다 append-only 준수 수준이 달라진다. git diff 없이는 감사자가 이 차이를 인용문 대조로만 추정할 수 있어 확인 비용이 크다 — 정정 포인터를 append할 때는 "원 문장 자체는 한 글자도 건드리지 않았다"를 작업자가 근거절에 명시적으로 확인해주는 습관이 이런 재발을 줄인다(94차 근거절의 "순수 문서 append" 명시가 좋은 선례).**

---

## 2026-08-26 (97차) — 사용자 명시 요청: 96차 발견 MEDIUM 1건(brand-guide.md 9-2가 원 관찰 문장을 편집한 것으로 보임) 후속 수정 재검증 — `docs/design/mercari/brand-guide.md`(9-2절) vs `docs/design/mercari/graphic-assets.md` append-only 준수 수준 비교 — B 범위

**결과: 96차가 지적한 "가시적 증상"(base 문장에서 세 접미사명이 사라짐)은 해소됐으나, 그 해소 방법 자체가 base 문장을 다시 편집(복원)하는 것이었어서 append-only 위반이 반복(2회째)됐고, 문서가 이 사실을 스스로 "원 문장은 편집하지 않았다"고 잘못 서술하는 새 문제가 생겼다. graphic-assets.md와의 append-only 준수 수준 격차는 이번에도 해소되지 않음 — 신규 MEDIUM 1건, LOW 1건.**

### 확인됨(가시적 증상만 해소, 방법론적으론 미해소 — 아래 신규 발견 참고)
- brand-guide.md 9-2(라인 148) base 문장이 `-on-white`/`-on-tint`/`-on-cta-disabled` 세 접미사명을 다시 포함하도록 복원됨 — 96차가 지적한 "이 이름들이 정정 포인터 안에만 남아 원문에서 추적 불가능"이라는 표면적 증상은 사라졌다. 정정 포인터 본문도 세 접미사를 동일하게 "예시"로 재분류하고 `-on-cta-disabled`만 9-3절 확인 결과 실재하지 않음, 나머지 둘은 미확인이라고 명확히 구분해 내용 자체는 논리적으로 일관됨. 복원된 base 문장이 참조하는 "토큰 아키텍처 가이드 6번 규칙"도 실재 문서(`docs/harness/design-team/token-architecture-guide.md` 6번, opacity 파생 대신 합성 hex를 독립 Primitive로 등록)와 정확히 대응해 근거는 유효함.

### 신규 발견
1. **[MEDIUM]** brand-guide.md 9-2 base 문장을 "최초 원문 그대로 복원"한 이번 조치 자체가, 96차가 지적했던 바로 그 규칙(append-only — 이미 있는 문장은 절대 편집하지 않고 새 포인터만 추가)을 또 위반한 것이다. base 문장은 이제 이 소동 전체에서 최소 두 번 편집됐다(94→96차 사이 어느 시점에 세 접미사명이 빠지도록 1차 편집, 이번 라운드에 원래대로 되돌리도록 2차 편집) — "고쳐서 되돌리는 것"도 여전히 기존 문장에 대한 편집 행위이지, append가 아니다. 그런데 문서 자체는 이 사실을 감춘다: 정정 포인터 라벨이 "[...재정정(2차) — 원 문장은 편집하지 않고 이 포인터만 다시 씀]"이라고 명시하고, 맨 끝 "근거" 절(라인 186)도 "최종 형태는 원 문장을 전혀 편집하지 않고 포인터 문장만으로... 구분한다"고 서술한다 — 그러나 96차 감사 로그가 그 시점에 실제로 인용한 base 문장("옵션시티별 합성 hex가 이미 계산돼 있었다", 세 접미사명 없음)과 지금 base 문장(세 접미사명 있음)을 대조하면 base 자체가 변경됐다는 사실이 명백하다. 즉 문서가 자신의 편집 이력을 스스로 부정확하게 서술하고 있다 — append-only가 지키려는 "추적 가능성"이 이번엔 문서의 자기 서술 자체에 의해 훼손됐다. 올바른 append-only 방식이었다면 96차 시점의 (실수로 축약된) base 문장을 그대로 두고, "이 괄호 안 나열은 이전 라운드에서 실수로 축약됐었다 — 원래는 `-on-white`/`-on-tint`/`-on-cta-disabled` 세 개였다"는 사실을 새 포인터로만 추가했어야 한다. 확인 필요.
2. **[LOW]** 정정 포인터 라벨이 "정정 포인터, 재정정(1차), 재정정(2차)" 세 이벤트만 나열하는데, 위 MEDIUM에서 지적한 이번 라운드의 base 복원 편집이 이 세 라벨 중 어느 것에 해당하는지(재정정(2차)를 재사용한 것인지, 라벨 자체가 갱신 안 된 것인지) 문서만으로는 판별할 수 없다 — git diff 없이 인용 대조로만 추정 가능해 확인 비용이 크다. 확인 필요(가능성 있음, 단정 못함).

### 비교 결론 — graphic-assets.md와의 append-only 준수 수준
- **여전히 불일치.** `docs/design/mercari/graphic-assets.md` 라인 36("불투명도 100%")은 94~96차 전 라운드에 걸쳐 원본 관찰 기록이 단 한 글자도 편집되지 않았고, 라인 37 포인터도 정확히 "이 문서의 원본 관찰 기록은 정정하지 않고 그대로 두며, 참고 포인터만 남긴다"고 사실대로 서술한다. 반면 brand-guide.md 9-2는 base 문장이 최소 2회 편집됐음에도 "편집하지 않았다"고 반대로 서술한다 — 두 파일의 실제 편집 이력은 여전히 다른 기준을 따르고 있고, 이번 라운드는 그 격차를 좁히지 못한 채 오히려 자기 서술의 정확성 문제를 하나 더 얹었다.

### 확인 안 함(범위 밖)
- 83차 LOW #5·#6, 89차 LOW(mercari brand-guide.md 근거절이 agent-memory 캡 대상 로그 인용)는 이번 라운드 변경 범위와 무관해 재확인하지 않음.
- `graphic-assets.md` "정리 결과 요약" 표(94차 LOW #1)가 여전히 CTA 불일치를 미해결처럼 적어두는 문제는 변경 없음 — 신규 이슈 아님.
- B 범위 버전 인용 정합성(`docs/planning/**`)은 이번 라운드에서 해당 문서 변경이 전혀 없어 재점검 생략.

### 패턴 메모(누적, 갱신)
- **신규 패턴(97차): append-only 위반을 "고쳐서 되돌리는" 방식으로 수정하면, 표면 증상(정보 누락)은 없어지지만 위반 행위 자체(기존 문장 편집)는 사라지지 않고 반복되며, 게다가 그 되돌리기 작업을 "편집 안 함"이라고 서술하면 문서의 자기 감사 능력이 실측값 자체보다 먼저 훼손된다. 96차 패턴 메모("부분 정정의 파급 범위 과소평가")의 다음 단계 실패 사례 — 이번엔 파급 범위를 과소평가한 게 아니라, 수정 방법 자체가 지켜야 할 규칙과 정면으로 다시 어긋났다. 향후 유사 상황(이미 실수로 편집된 base 문장을 발견)에서는 "원래대로 되돌리는 편집"이 아니라 "그 문장이 실수로 축약/변경됐었다는 사실을 새 포인터로 추가"하는 것이 유일하게 append-only와 양립하는 해법임을 명시적으로 상기할 필요가 있다.**

---

## 2026-08-27 (99차) — 사용자 명시 요청: 98차 HIGH 지적(Selection Indicator Selected 상태 면색 위반) 수정 결과 재검증 — `docs/design/mercari/graphic-assets.md` "[2026-08-27 결함 수정]" 절(graphic-designer) — B 범위

**결과: 98차 HIGH 1건은 해소 확인(면색 완전 제거, stroke-only 재설계 — 원본 서술은 편집하지 않고 새 절로 append). 다만 새 절의 파급 효과로 MEDIUM 2건 신규 발견(➊ 문서 맨 끝 "정리 결과 요약" 표의 Selected 색상 셀이 이 문서의 기존 관례와 다르게 원문 자체가 갱신된 것으로 보임, ➋ stroke-only로 바뀐 Selected 상태가 여전히 icon-craft-guide.md Basic/Utility 트랙의 "얇고 중립적인 스트로크" 요건과 별개로 충돌할 가능성). LOW 1건(Selected Ring 2px 신설로 같은 오브제 내 세 가지 스트로크 두께 혼재).**

### 확인됨(해소)
- **98차 HIGH(면색 위반)**: `docs/design/mercari/graphic-assets.md` "[2026-08-27 결함 수정]" 절에서 Selected 상태 외곽 Ellipse의 fill을 완전히 제거하고 stroke 2px `color/primary`만 남겼으며, 내부 체크 Vector도 fill 없이 stroke만(`color/primary`)으로 재설계했다고 명시 — "면색(fill)을 넣지 않는다" 규칙(`icon-craft-guide.md` 37번째 줄) 위반이 해소됐다. 8개 occurrence(`279:147`/`279:153`/`258:161`/`258:166`/`258:170`/`259:174`/`259:166`/`259:170`) 전부 `fills` 빈 배열 재확인까지 기록돼 있다.
- **append-only(narrative 절)**: 원래 "1. Selection Indicator" 절(fill 채움 서술)은 한 글자도 편집되지 않고 그대로 남아 있고, "[2026-08-27 결함 수정]"이 별도 새 헤딩으로 그 뒤에 추가됐다 — 94~98차에 걸쳐 이 문서가 지켜온 "원문 보존 + 포인터/신규 절 추가" 방식과 일치. 이 부분은 문제 없음.

### 신규 발견
1. **[MEDIUM]** 문서 맨 끝 "정리 결과 요약" 표(SCR-004/005/006 절)의 "Selection Indicator (Selected)" 행 "색상" 칸이 현재 "stroke만 `color/primary` 2px 링 + `color/primary` 체크(2026-08-27 수정, 과거 fill 채움 기록은 위 결함 수정 절 참고)"로 돼 있다. 이 문구가 표 셀 안에 직접 박혀 있어(별도 신규 행이나 각주가 아니라 기존 셀 내용을 대체한 형태로 보임), 같은 문서 상단의 다른 "정리 결과 요약" 표(chevron-right 항목)가 CTA Disabled 색상 불일치를 정정 후에도 "비활성 색상(#000000) 불일치는 CTA 컴포넌트 쪽 별도 이슈"라는 옛 문구를 전혀 손대지 않고 그대로 둔 것(94차 LOW #1이 이를 "append-only 제약상 표 자체는 못 고친다"고 명시적으로 확인한 선례)과 처리 방식이 다르다. git diff로 직접 대조하지 못해 단정할 수 없으나, 이 표 셀이 기존 셀 텍스트를 대체(원문 편집)한 것이라면 96·97차가 brand-guide.md에서 지적했던 "표/요약 문장을 고쳐서 되돌리는" 유형의 append-only 위반이, 그동안 "잘 지킨 쪽"으로 확인돼 온 이 문서(graphic-assets.md)에도 재발한 것일 수 있다. 확인 필요.
2. **[MEDIUM]** Selected 상태가 fill 위반은 해소했지만, `icon-craft-guide.md` Basic/Utility 트랙 규칙은 "면색을 넣지 않는다"뿐 아니라 그 앞 문장에서 "얇고 중립적인 스트로크만 사용한다"도 요구한다. 재설계된 Selected 상태의 stroke·체크 색은 둘 다 `color/primary`(브랜드 블루 `#3182F6`) — Unselected 상태의 중립색(`color/border-neutral`, 회색 계열)과 달리 브랜드색이다. "면색이든 다른 색이든 채우지 않는다"는 fill 조항은 명확히 지켰으나, 별개 조항인 "중립적" 스트로크 색 요건까지 충족했는지는 문서·가이드 어디에도 명시적으로 검토되지 않았다 — 선택 상태를 색으로 구분하는 것은 흔한 UI 관례라 의도된 예외일 가능성이 높으나, 가이드 문면상 명시적 근거가 없어 확인 필요.

### 낮은 확인 필요(참고성)
1. **[LOW]** 재설계된 Selected 상태는 외곽 Ring stroke 2px, 체크 stroke 1.7px를 쓰고, 같은 오브제의 Unselected 상태는 stroke 1.5px를 쓴다 — 세 값이 한 오브제 안에 공존한다. `icon-craft-guide.md` "세트 내 일관성" 절("같은 트랙끼리는 스트로크 두께... 동일하게 유지한다")은 트랙 내 아이콘 간 일관성을 말한 것이라 같은 아이콘의 서로 다른 state까지 적용되는지는 불명확하고, 상태 구분을 위한 의도적 차등일 가능성이 높아 LOW로 유지 — 확정 위반 아님, 확인 필요.

### 확인 안 함(범위 밖)
- 83차 LOW #5·#6, 89차 LOW, 94차 LOW #1(chevron-right 표), 98차 MEDIUM(Choice Card 네이밍)은 이번 라운드 변경 범위와 무관해 재확인하지 않음.
- B 범위 버전 인용 정합성(`docs/planning/**`)은 이번 라운드에서 해당 문서 변경이 전혀 없어 재점검 생략.

### 패턴 메모(누적, 갱신)
- **신규 패턴(99차): "결함 수정" 라운드에서 서술 절(narrative section)은 append-only를 잘 지키더라도, 문서 맨 끝의 "정리 결과 요약" 표처럼 여러 라운드에 걸쳐 계속 참고되는 요약표는 최신 정보 유지 압박(design-systems가 이 표를 보고 다음 작업을 한다는 실용적 필요) 때문에 원문 편집 유혹이 특히 크다 — 같은 문서 안에서도 "역사적 관찰 기록"(narrative, 잘 보존됨)과 "현재 상태 참고표"(summary table, 편집 유혹이 큼)가 다른 기준으로 다뤄질 위험이 있다. 96·97차가 brand-guide.md에서 지적한 "표/문장 재편집" 문제와 계열이 같지만, 이번엔 지금까지 "모범 사례"로 언급되던 graphic-assets.md에서 처음 그 위험 신호가 나타났다는 점이 다르다.**

---
