# harness-auditor 감사 로그

로그가 5개를 넘으면 가장 오래된 항목부터 삭제(git history에 전체 보존됨).

---

## 2026-08-22 (83차) — 사용자 명시 요청: 백오피스/통계 대시보드 완성도 예외 신설 3건 정합성 감사 — `figma-file-organization.md` 3-D 신설 + `screen-spec-pattern-guide.md` 신규절 + `stats-dashboard-design-reference.md` 신규 파일(+ `reset-checklist.md` A그룹 등록) — 보통 강도 (A 범위)

**결과: 신규 발견 6건(HIGH 2, MEDIUM 1, LOW 3). 세 문서 간 상호 참조 자체(파일 경로·절 제목)는 전부 정확 — 참조 무결성 위반은 "존재 확인이 안 되는 제3자"(`dataviz` 스킬)에서 발생.**

### 신규 발견
1. **[HIGH]** `figma-file-organization.md` 3-D번과 `stats-dashboard-design-reference.md` 둘 다 "차트 자체의 색상·마크·범례·인터랙션"·"시각화 자체의 완성도"를 `dataviz` **스킬**에 위임하는데, 이 하네스 어디에도(`​.claude/skills/*/SKILL.md` 6개 — design-concept-round, planning-kickoff-round, ai-image-content-round, report-pdf, review, summary — 전역 `~/.claude`까지 확인) `dataviz`라는 스킬·문서가 존재하지 않는다. `reset-checklist.md` A그룹 스킬 목록에도 없다. 두 문서가 "정본"이라고 지목한 대상이 실제로는 존재하지 않아, 지금 이 상태로는 차트 시각화 디테일을 다루는 근거 문서가 아예 없다. 확인 필요 — `dataviz` 스킬을 실제로 새로 만들 계획인지, 아니면 다른 기존 문서(예: 없음)로 참조를 정정해야 하는지.
2. **[HIGH]** `stats-dashboard-design-reference.md` 1번째 줄이 스스로를 "design-qa가 감사할 때 쓰는 검증 기준"이라 선언한다 — `docs/harness/design-team/figma-page-format-guide.md`·`icon-craft-guide.md`·`component-state-guide.md`·`token-architecture-guide.md`·`motion-timing-guide.md`도 전부 같은 방식으로 자신을 design-qa 검증 기준이라 밝히고 있고, 실제로 `.claude/agents/design-qa.md`의 "판단 기준"·"검토 우선순위" 목록에 이 5개가 전부 이름과 번호(6~12번)로 명시적으로 인용돼 있다. 그런데 새로 만든 `stats-dashboard-design-reference.md`는 이 목록 어디에도 인용되지 않았다 — 이 파일 하나만 "이미 존재하는 5형제와 같은 지위를 스스로 주장하지만 실제로 그 지위를 부여하는 문서(design-qa.md)에는 등록이 안 된" 상태. design-qa가 실제로 통계 대시보드 화면을 감사할 때 이 문서를 적용할지 불확실하다. 확인 필요.
3. **[MEDIUM]** `figma-file-organization.md`의 같은 섹션(3번) 안에서, 3-B번 5단계("전체 확장")가 "4-1번 승인을 받은 뒤에만, 추출된 시스템을 기준 삼아 docs/planning에 정의된 **나머지 화면 전체를 같은 완성도로 제작한다**"라고 규정하는데, 바로 다음에 신설된 3-D번은 "백오피스는 [프론트와] 그보다 낮은 완성도... 의도적으로 허용한다"고 말한다. docs/planning 전체 화면 목록에 백오피스가 포함돼 있다면, 3-B 5단계의 "나머지 화면 전체를 같은 완성도로"라는 무조건적 서술이 3-D가 만든 예외를 반영하지 못한 채 그대로 남아있는 것으로 읽힐 수 있다(3-B 5단계 문구 자체엔 "단, 백오피스 제외" 같은 단서가 없음). 확인 필요 — 3-B 5단계의 "나머지 화면 전체"가 애초에 프론트 화면만 가리키는 문맥(파일럿 선정이 3-B 1단계에서 "서비스 핵심 플로우 대표 화면"으로 이미 프론트 위주였음)이라 암묵적으로 안전한 것인지, 아니면 3-B 5단계에도 3-D 예외를 명시적으로 교차 인용해야 하는지는 판단 필요.
4. **[LOW, 가능성 있음]** `screen-spec-pattern-guide.md` 신규절과 `stats-dashboard-design-reference.md` 둘 다 예시 문구로 "SCR-007의 스타일은 **참여자용** 화면(§1)과 무관한..."/"이 화면의 스타일은 **참여자용** 화면과 무관한..."을 똑같이 쓴다. 그런데 이 하네스가 이미 정착시켜 쓰는 용어는 "프론트(고객용) 화면"(`figma-file-organization.md` 3-D, 방금 이 라운드에 같이 신설)이지 "참여자용"이 아니고, 이 프로젝트(연락처 관리 웹 앱) 자체에도 "참여자"라는 개념은 없다(`docs/planning`에 "참여자"·"SCR-007" 문자열 없음 확인). 다른 프로젝트(설문/이벤트 등 참가자가 있는 서비스)의 예시가 정정 없이 그대로 두 포터블 문서에 박제된 것일 가능성이 있다 — 이 하네스 자체의 "연락처" 프로젝트 고유 예시(이미 `reset-checklist.md` C-3에 등록된 것)와는 다른 종류의, 제3의 프로젝트발 도메인 특유 용어가 새로 유입된 사례일 수 있다. 확정 근거는 없어 가능성 있음으로만 보고 — 사용자가 원래 의도한 예시(실제로 참여자 개념이 있는 다른 프로젝트를 염두에 두고 일부러 쓴 것)일 수도 있다.
5. **[LOW]** `figma-file-organization.md` 3-D번은 `stats-dashboard-design-reference.md`나 `screen-spec-pattern-guide.md` 신규절을 전혀 역참조하지 않는다(단방향 참조 — 두 신규 문서는 3-D번을 인용하지만 3-D번은 이들을 모른다). 모순은 아니지만, 3-D번의 "시각화 자체의 완성도는 `dataviz` 스킬 참고"라는 괄호 안에 "화면 배치 원칙은 `stats-dashboard-design-reference.md` 참고"를 같이 남기지 않아 상호 참조가 한쪽만 완결돼 있다.
6. **[LOW]** `design-pl.md`·`ui-designer.md`는 `figma-file-organization.md`를 통째로 "따른다"는 포괄 참조만 갖고 있어 3-D번도 기술적으로는 상속되지만, `design-pl.md`가 3-B/2-4/3번처럼 다른 하위 절은 개별적으로 콕 집어 인용하는 기존 패턴과 비교하면 3-D번(그리고 `stats-dashboard-design-reference.md`)만 orchestration 문서에서 별도로 스포트라이트되지 않았다 — 실무 영향은 낮지만(포괄 참조로 이미 커버됨) 패턴 일관성 관점에서 참고.

### 확인됨(문제 없음)
- 세 문서의 명시적 상호 참조(파일 경로·절 번호·절 제목) 자체는 전부 실제 위치와 정확히 일치: `screen-spec-pattern-guide.md` 신규절 → `figma-file-organization.md` 3-D번(존재·내용 일치), `stats-dashboard-design-reference.md` → 같은 3-D번(존재·내용 일치) 및 `screen-spec-pattern-guide.md`의 "구성요소-동작 표"(4종 세트 3번 항목과 일치)·"디자인 언어가 의도적으로 분리된 화면" 절(제목 일치).
- `reset-checklist.md` A그룹의 `stats-dashboard-design-reference.md` 등록 문구("백오피스 안에서도 완성도 예외, 정보 위계·그리드 배치, 프론트로 스타일 역류 금지")가 실제 파일 내용(1~5번 절)과 대체로 일치 — "표 정렬·필터"(4번 절) 요약 누락은 있으나 모순은 아니고 사소함.
- 백오피스 용어("관리자용 별도 화면, 킥오프 체크리스트에서 스코프 확정")는 3-D번과 `planning-team/architecture-decision-guide.md`(78차에서 이미 확인된 문서) 사이에 일관되게 유지됨 — 신규 충돌 없음.
- 새 세 문서 모두 노드 ID·이 프로젝트 실제 컴포넌트명/색상 hex 하드코딩 없음(4번 항목의 "참여자용" 용어 건은 다른 종류의 leak 가능성으로 별도 표기).
- `component-state-guide.md`·`icon-craft-guide.md`·`token-architecture-guide.md` 등 기존 design-team 가이드와 `stats-dashboard-design-reference.md`는 다루는 층위(컴포넌트/토큰 vs 화면 레이아웃 배치)가 겹치지 않아 내용 충돌 없음.

### 패턴 메모(누적, 갱신)
- **신규 패턴(83차): 새 포터블 가이드 문서를 만들 때, 그 문서가 스스로 "이 문서는 OO 에이전트의 검증 기준/실행 근거다"라고 선언해도, 그 OO 에이전트 정의 파일(`design-qa.md` 등) 쪽에 실제 인용이 추가되지 않으면 선언은 있지만 집행력이 없는 상태로 남는다 — 기존 형제 문서들이 전부 양방향 등록(문서 자기소개 + 에이전트 쪽 명시적 인용)돼 있는 패턴이면, 새 문서도 같은 두 곳을 다 채웠는지 확인해야 한다.**
- **신규 패턴(83차): 하네스 문서가 아직 존재하지 않는 스킬/문서를 "정본"으로 지목하며 역할을 위임하는 문장은, 그 지목된 대상이 실제로 있는지 grep 한 번으로 확인해야 한다 — 없으면 위임이 아니라 책임 공백이 된다.**

---

## 2026-08-22 (84차) — 사용자 명시 요청: 83차 발견 4건(HIGH 2, MEDIUM 1, LOW 1) 후속 수정 확인 — `design-qa.md` 등록, `dataviz` 설명 보강, 3-B/3-D 교차 인용, "SCR-007"/"참여자용" 예시 정정 — 짧은 확인 (A 범위)

**결과: 4건 모두 해소 확인. 신규 발견 없음(83차 LOW #5·#6은 이번 확인 대상에서 제외 — 미해결로 남아있으나 사용자가 지정한 4건 범위 밖).**

### 확인됨(해소)
- **83차 HIGH #2(stats-dashboard-design-reference.md가 design-qa.md에 미등록)**: `.claude/agents/design-qa.md`의 판단기준 목록(line 26)과 검토우선순위 목록(line 45, 13번)에 각각 "통계/데이터 대시보드 준수 여부"가 추가돼 형제 문서(figma-page-format-guide 등)와 동일한 양방향 등록 패턴을 갖추게 됨 — 해소.
- **83차 HIGH #1(`dataviz` 스킬 미존재)**: `figma-file-organization.md` 3-D번과 `stats-dashboard-design-reference.md` 5번째 줄 모두 `dataviz`를 "Claude Code 내장 번들 스킬(프로젝트 파일 아님)"로 명시해 두 문서 표현이 서로 일치함 — 원래 지적("프로젝트 안에 해당 스킬 파일이 없다")은 해소. 단, "dataviz"가 실제 Claude Code 내장 스킬로 존재하는지 자체는 텍스트 정합성 감사 범위 밖(외부 사실 검증)이라 별도 확인은 하지 않았음 — 두 문서가 서로 모순 없이 같은 주장을 하고 있다는 점만 확인.
- **83차 MEDIUM(3-B 5단계 vs 3-D 무조건 충돌)**: `figma-file-organization.md` 3-B 5단계에 "단, 백오피스 화면이 있다면 3-D번의 완성도 예외(대시보드 제외 낮은 완성도 허용)를 적용한다" 문장이 추가돼 3-D 예외를 명시적으로 교차 인용함 — 해소.
- **83차 LOW #4("SCR-007"/"참여자용" 예시)**: `screen-spec-pattern-guide.md`(line 30)·`stats-dashboard-design-reference.md`(line 25) 예시 문구가 모두 "SCR-xxx"·"프론트(고객용) 화면"으로 정정됨. 하네스 전체에서 "SCR-007"·"참여자용" 문자열 재검색 결과 잔존 없음 — 해소.

### 확인 안 함(범위 밖, 여전히 미해결 가능성)
- 83차 LOW #5(3-D번의 단방향 참조 — 신규 두 문서를 역참조하지 않음)·LOW #6(design-pl.md/ui-designer.md가 3-D를 개별 스포트라이트하지 않음)은 이번 라운드 수정 대상에 포함되지 않아 확인하지 않았음 — 여전히 그대로일 가능성 있음(심각도 LOW, 급하지 않음).

### 패턴 메모(누적, 갱신)
- 이번 라운드는 지적된 4건 모두를 빠짐없이 반영한 사례 — 82차 패턴 메모("한 번에 관련 파일을 다 나열해 대조하면 재발을 막는다")와 일관된 결과.

---

## 2026-08-22 (85차) — 사용자 명시 요청: `stats-dashboard-design-reference.md` 6~16절 대거 신설 + 메타 콜아웃·근거 절 추가 + `reset-checklist.md` 등록 갱신 정합성 감사 — 신규 내용 많아 꼼꼼히 (A 범위)

**결과: 신규 발견 4건(HIGH 2, MEDIUM 2), 참고성 LOW 2건. 가장 중요한 발견은 13절(접근 제어)이 `architecture-decision-guide.md`의 "예외 없음" 절대 규칙을 규모 기준으로 몰래 좁히는 패턴 — 과거 실제 사고(design-systems.md FRAME 삭제 건)와 동일한 구조.**

### 신규 발견
1. **[HIGH]** `stats-dashboard-design-reference.md` 13절 — "파일럿·소규모(관리자 1인)라면 최소 비밀번호 게이트로 충분하다... (`architecture-decision-guide.md`의 "백오피스는 최소한 역할 기반 접근 제어가 항상 필수" 기준의 최소 구현 형태)"라는 서술이, 실제 `architecture-decision-guide.md` 9번 절 원문("규모와 무관하게 최소 기준 하나는 항상 지킨다: 고객이 관리자 전용 기능에 접근할 수 없도록 **역할(role) 기반** 접근 제어를 반드시 건다 — **화면이 하나뿐이라도 예외 없다**")과 정면으로 어긋난다. 원문은 "역할(role)"을 괄호로 명시하며 규모와 무관하게 예외를 두지 않는데, 신규 13절은 "비밀번호 게이트"(역할 개념이 아예 없는 공유 시크릿 방식)를 그 "최소 구현 형태"라고 재규정해 소규모라는 조건으로 몰래 예외를 만든다 — "역할 기반"과 "비밀번호 게이트"는 같은 메커니즘이 아니다. 확인 필요 — 사용자가 원래 "예외 없음"의 의도를 소규모까지 문자 그대로 적용할 뜻이었는지, 아니면 비밀번호 게이트도 실질적으로 고객 접근을 막으므로 허용 가능한 재해석인지.
2. **[HIGH]** `stats-dashboard-design-reference.md` 상단 "이 문서가 다루지 않는 것" — "차트 자체의 색상·**마크**·범례·인터랙션 같은 시각화 설계 디테일은 `dataviz` 스킬이 정본"이라고 스스로 경계를 그어놓고, 바로 10절에서 "두 값 비교엔 막대, 유의성 검정+신뢰구간엔 기준선이 있는 밴드, 짝지어진 데이터엔 2×2 그리드, 구성비엔 도넛/파이, 그룹 쏠림엔 중앙 기준선이 있는 분할 막대"처럼 통계 개념별 구체적 마크(차트 타입)를 직접 지정한다. 문장 끝에 "(구체적 마크·색·인터랙션은 dataviz 스킬 참고)"를 붙여 경계를 의식하고는 있지만, 막대/밴드/그리드/도넛/분할막대 자체가 이미 "마크" 선택이라 상단에서 스스로 선언한 제외 대상과 문자 그대로 겹친다 — 자기모순. 확인 필요 — "마크"의 의미를 "차트 패밀리 선택"과 "세부 렌더링(점 모양·선 스타일 등)"으로 나눠 경계를 다시 명확히 할지.
3. **[MEDIUM]** 체크리스트 16개 항목이 1~12·14~16절은 각각 하나씩 대응시키면서 **13절(접근 제어)에 대응하는 체크리스트 항목이 아예 없다**. 이 문서 1번째 줄이 스스로를 "design-qa가 감사할 때 쓰는 검증 기준"이라 선언하는데, 검증 기준(체크리스트)에서 접근 제어 항목이 빠지면 design-qa가 실제로 이 항목을 감사에 반영할 근거가 약해진다 — 위 HIGH #1과 별개로, 문서 내부에서 본문(13절)과 체크리스트 간 자체 불일치.
4. **[MEDIUM]** `.claude/agents/design-qa.md` 13번 판단기준("정보 위계·그리드 배치·표 설계·재사용 금지 경계, 2026-08-22 신설")이 84차 라운드 시점(구 1~5절)의 요약 그대로 남아있고, 이번 라운드에서 같은 파일에 대거 신설된 6~16절(목표흐름패널·유의성배지·접근제어 등)을 전혀 반영하지 못했다. 모순은 아니지만(옛 내용은 여전히 사실), 이번에 새로 생긴 절 다수(특히 HIGH #1의 접근 제어)가 design-qa 판단기준 설명에 등장하지 않아 실제 감사 시 누락될 소지가 있다.

### 확인됨(문제 없음/참고)
- `reset-checklist.md` A그룹의 `stats-dashboard-design-reference.md` 등록 설명(6~16절을 "목표흐름패널·유의성배지·터치용아코디언·no-peeking·시각화목적별선택·계산로직단일화·부분집합강조·단위표기·중첩토글위계 등"으로 요약)은 실제 문서 내용과 대체로 일치 — 11절(반응형)·13절(접근제어)은 요약에서 빠졌지만 "등"으로 비완전 열거를 이미 표시해 모순은 아님(83차 선례와 같은 수준의 사소한 누락).
- 조건부 표시("성공지표 트리가 있는 프로젝트", "유의성 검정 결과가 있는 경우", "사전 등록된 실험이 있는 경우")는 6·7·9절 제목과 체크리스트 항목 양쪽에 일관되게 붙어 있어 일반 원칙으로 오독될 소지는 낮음.
- 새 "근거" 절의 톤(다른 프로젝트에서 "100회 안팎의 시각 피드백을 거쳐, 2회 이상 반복 재발한 패턴만 추려 일반화")은 문서 상단 메타 콜아웃("여러 차례 시각 피드백을 거쳐 걸러진 판단 기준")과 서로 일치 — 톤 충돌 없음.
- 노드 ID·이 프로젝트(연락처 관리 앱) 고유 컴포넌트명/색상 hex는 새 절 어디에도 없음.

### 낮은 확인 필요(참고성)
- **[LOW, 가능성 있음]** "근거" 절이 출처로 특정 외부 프로젝트 식별자("01-ML_mercari_price_2608")를 그대로 노출한다 — 이 프로젝트 고유 데이터는 아니지만, 포터블 문서를 제3의 프로젝트로 복사했을 때 이 식별자는 아무 맥락도 주지 못하는 이물질로 남는다. 다른 포터블 문서들이 실제 사고를 "상세는 OO 참고"로 추상화하는 패턴과 결이 다르다.

### 패턴 메모(누적, 갱신)
- **신규 패턴(85차): "최소 구현 형태"·"완화된 버전" 같은 표현으로 절대 규칙(예외 없음)을 재서술할 때, 재서술된 메커니즘이 원 규칙이 명시한 핵심 기술 개념(이번엔 "역할 기반")을 실제로 충족하는지 별도로 확인해야 한다 — 이름만 이어받고 메커니즘이 바뀌면 "구현 형태"가 아니라 사실상 새로운 예외다.**
- **신규 패턴(85차): 문서 상단에 "이 문서가 다루지 않는 것" 경계를 선언한 뒤, 그 경계에 쓰인 단어(이번엔 "마크")를 본문에서 다시 구체적으로 사용하면, 경계 선언과 본문이 같은 단어를 다른 의미로 쓰고 있는 건지 확인이 필요하다.**

---

## 2026-08-22 (86차) — 사용자 명시 요청: 85차 발견 5건(HIGH 2, MEDIUM 2, LOW 1) 후속 수정 확인 — `stats-dashboard-design-reference.md` 13절 재작성·스코프 선언 재작성·체크리스트 추가, `design-qa.md` 13번 확장, "근거" 절 식별자 정정 — 범위 A 위주 꼼꼼히 (A 범위)

**결과: 5건 모두 해소 확인. 신규 발견 없음.**

### 확인됨(해소)
- **85차 HIGH #1(13절이 architecture-decision-guide.md의 "역할 기반·예외 없음"을 몰래 좁힘)**: 13절이 "요건(역할 기반 접근제어)은 규모 무관 유지, 낮출 수 있는 건 구현의 복잡도뿐"으로 재작성되고 "비밀번호 게이트는 role 개념이 없어 요건을 충족 못 한다"고 명시적으로 배제함. `architecture-decision-guide.md` 원문(역할 기반 접근 제어를 규모 무관 필수로, 화면이 하나뿐이라도 예외 없다고 규정한 절 + 바로 다음 문단에서 "그 이상"(API 계약 분리·인증 스킴)만 규모별로 다르다고 이미 구분해둔 서술)과 정확히 같은 구조(요건 vs 구현 복잡도)를 그대로 이어받음 — "역할별 권한 세분화 없이 단일 role 필드만 확인"이라는 완화도 원문의 "화면 1~2개면 기존 인증·API 체계에 역할 체크만 얹어도 충분"과 같은 수준이라 정합. 해소.
- **85차 HIGH #2(스코프 선언 "마크" 제외 vs 10절 "마크" 직접 지정 자기모순)**: 상단 스코프 문장이 "차트 '형태'가 적합한지(예시 포함)는 이 문서(10절)가 다루지만, 그 차트를 실제로 그릴 때의 색상·정확한 마크 스타일·범례·인터랙션 디테일은 dataviz 스킬"로 재작성돼, "어떤 차트 유형을 고르는가"(10절 소관)와 "그 마크를 어떻게 스타일링하는가"(dataviz 소관)를 명확히 분리함. 스코프 선언의 예시(신뢰구간 밴드, 2×2 그리드)가 10절 본문 예시와 문자 그대로 일치해 경계가 실제로 겹치지 않음을 재확인. 해소.
- **85차 MEDIUM(13절 대응 체크리스트 항목 부재)**: 체크리스트에 "최소한 역할(role) 기반 접근 제어가 걸려 있는가(비밀번호 게이트만으로 대체하지 않았는가)?" 항목 추가 확인(line 88). 해소.
- **85차 MEDIUM(design-qa.md 13번이 구 1~5절 요약에 머묾)**: 13번 판단기준 문장이 6~16절 다수(정보위계·그리드배치·표설계·재사용금지경계·접근제어·목표흐름패널·유의성배지·no-peeking·시각화목적별선택)를 나열하고 "등"으로 비완전 열거를 명시 — 83차 선례와 같은 수준의 허용 가능한 요약. 해소.
- **85차 LOW(근거 절의 프로젝트 폴더 식별자 노출)**: "근거" 절이 "01-ML_mercari_price_2608" 같은 구체 식별자 대신 "다른 프로젝트(AB테스트 결과 대시보드를 만든 별개 프로젝트)"로 일반화됨 — 포터블 문서 원칙에 부합. 해소.

### 확인 안 함(범위 밖)
- 83차 LOW #5(3-D 단방향 참조)·#6(orchestration 문서 개별 스포트라이트 없음)은 이번에도 사용자가 지정한 5건 범위 밖이라 재확인하지 않음 — 84차에 이어 계속 미해결 상태일 가능성.

### 패턴 메모(누적, 갱신)
- 이번 라운드도 지적된 5건 모두를 빠짐없이 반영 — 82·84차에 이어 "관련 파일/절을 한 번에 나열해 대조" 방식이 재발 방지에 계속 효과적임을 재확인.

---

## 2026-08-24 (87차) — 사용자 명시 요청: "바텀시트=스텝분해 신호" 신규 UX 판단 원칙 정합성 감사 — `screen-spec-pattern-guide.md` 신규절+체크리스트, `ux-designer.md` 판단기준 항목, `reset-checklist.md` 날짜 주석 — A 범위 위주 (B는 관련 근거 없음 확인)

**결과: HIGH/MEDIUM 신규 발견 없음. 세 파일 간 참조·요약이 전부 정확히 일치. LOW 2건(1건은 신규, 1건은 83차부터 이어진 미해결 항목 재확인)만 참고용으로 보고.**

### 확인됨(문제 없음)
- 신규절이 같은 문서의 "4종 세트는 예외 없이 필수" 절과 모순되지 않음 — 오히려 강화: 스텝을 나누면 "나뉜 스텝마다 4종 세트를 갖춘다"고 명시해 4종 세트 원칙의 적용 범위를 넓힐 뿐 좁히지 않음.
- 신규절이 "오버레이 포함"(바텀시트를 이미 오버레이 예시로 열거) 절과도 층위가 다름 — 오버레이 절은 "이미 확정된 디자인의 레이아웃을 문서화하는 방법"(사후), 신규절은 "기획·리뷰 단계 지시를 어떻게 해석할지"(사전) 규칙이라 서로 다른 시점을 다뤄 충돌 없음.
- `ux-designer.md` 새 판단기준 불릿의 요약("컨테이너 교체 아니라 스텝 분해 신호", "지금 당장 필요한 것 vs 다음 단계에서 볼 것")이 `screen-spec-pattern-guide.md` 신규절 본문과 표현만 다를 뿐 내용상 정확히 같은 주장 — 인용 왜곡 없음. `@docs/harness/planning-team/screen-spec-pattern-guide.md` 경로도 실제 파일 위치와 일치.
- `reset-checklist.md` line 28의 "2026-08-24 바텀시트=스텝분해 신호 절 추가" 날짜 주석이 실제로 그 날짜에 신설된 절 제목과 일치.
- `planning-writer.md`가 `screen-spec-pattern-guide.md`를 "4종 세트와 오버레이 포함 원칙" 수준으로 포괄 인용("따른다")하고 있어, 신규절을 개별로 다시 스포트라이트하지 않아도 실제로는 상속됨(83차에서 이미 확인된 "포괄 참조로 커버되는 패턴"과 동일 — 문제 아님).
- `docs/planning/**`·`docs/design/**`에 바텀시트 관련 기존 화면정의서/확정 디자인이 아직 없음을 확인(B 범위 충돌 대상 자체가 존재하지 않음) — "시트" 문자열이 걸리는 기존 항목은 전부 Figma "스펙 시트"(컴포넌트 문서화 프레임)이거나 엑셀 비유로, 바텀시트 UI와 무관.
- 노드 ID·이 프로젝트 고유 컴포넌트명/색상 hex 등 leak 없음 — 신규절 예시는 모두 일반화된 서술.

### 낮은 확인 필요(참고성)
1. **[LOW, 가능성 있음, 신규]** `screen-spec-pattern-guide.md` 1번째 줄이 이 문서를 "planning-writer가 화면정의서를 쓸 때 지키는 구조 패턴"이라고 스스로 planning-writer 전용으로 소개하는데, `ux-designer.md`는 이번에 "화면정의서 반영 방법은 이 문서를 따른다"고 적어 실제 문서 작성자가 아닌 ux-designer도 이 문서의 "반영 방법"까지 따르는 것처럼 읽힌다. ux-designer.md의 다른 항목들(예: 확정 디자인 흐름 불일치 발견 시 "임의로 고치지 않고 design-pl에게 보고")과 같은 패턴이라면 ux-designer는 스텝 분해 필요성을 "판단·제안"만 하고 실제 화면정의서 반영은 planning-writer가 한다는 역할 분담이 맞겠지만, 두 파일 어디에도 이 역할 분담이 명시적으로 적혀 있지 않다. 확인 필요 — ux-designer가 화면정의서를 직접 쓰는 일이 실제로 생기는지(ux-designer.md tools에 Write가 있어 기술적으로는 가능), 아니면 항상 planning-writer에게 넘기는지.
2. **[LOW, 이월, 재확인]** 83차 LOW #5·#6(figma-file-organization.md 3-D번이 관련 신규 문서들을 역참조하지 않음, `design-pl.md`가 3-D를 3-B/3-C처럼 개별 스포트라이트하지 않고 포괄 참조에만 의존)이 이번 라운드에도 그대로 남아있음을 재확인(`design-pl.md` line 57이 여전히 "3-B/3-C번"만 명시). 이번 라운드 변경 범위 밖이라 급하지 않음 — 계속 미해결 상태로만 기록.

### 패턴 메모(누적, 갱신)
- 이번 라운드는 "일반 원칙(하네스 가이드) 신설 → 상위 오케스트레이션 문서(에이전트 파일)에 요약 반영 → 리셋 체크리스트 날짜 주석 갱신"이 세 곳 모두 한 번에 정확히 맞물린 사례 — 84·86차처럼 "관련 파일을 한 번에 나열해 대조"가 여전히 유효.
- **신규 관찰(87차): 특정 가이드 문서가 스스로 "이 문서는 OO 역할 전용"이라고 1인칭으로 소속을 밝힌 뒤, 나중 라운드에서 다른 역할(이번엔 ux-designer)이 그 문서를 "반영 방법의 근거"로 인용하기 시작하면, 인용 자체는 틀리지 않아도 "누가 실제로 문서를 쓰는가"의 역할 경계가 암묵적으로 흐려질 수 있다 — 새로 인용을 추가할 때 그 문서의 원 소유자 선언과 새 인용자의 역할이 겹치지 않는지 확인하면 좋다.**

---
