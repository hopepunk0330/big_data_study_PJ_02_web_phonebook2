# design-prompter 메모리

이 파일은 design-prompter가 작업 시작 시 읽고, 작업 종료 시 기록을 남기는 메모리/로그입니다.

## 작업 로그

### 2026-08-24 — ui-designer 브리프(단독): mercari 1차 라운드 후속 — brand-designer가 교체한 신규 Concept B 톤 프레임(`28:2`, "콜라주 저널링")으로 B 대표화면(`21:2`/`21:3`)만 재제작
- **원본 요청**: 직전 라운드에서 brand-designer가 기존 B("네오팝 브루탈형", `8:4`)를 `❌ 미채택`으로 라벨링하고 신규 `28:2`("Concept B — 콜라주 저널링", 팔레트는 기존 5개 hex 그대로, 조형 원리는 대시 보더 티켓 스텁+소프트 디퓨즈 그림자(blur16/ink14%)+washi tape 스티커+잉크 스탬프 배지+"선택됨=코랄솔리드+소프트섀도/미선택=대시아웃라인" 쿠폰 은유)를 완성. 이번 요청은 ui-designer가 만든 기존 B 대표화면(`21:2`/`21:3`)을 이 신규 컨셉으로 다시 만드는 것. A(`20:x`)·C(`23:x`)는 무관, 절대 변경 금지.
- **핵심 판단**: (1) house rule(라벨링 먼저 → 옆에 신규 제작 → 원본 직접 수정 금지)을 그대로 적용 — `21:2`/`21:3`은 `❌ 미채택 — ` 라벨만 붙이고 내용 무수정. (2) 화면 구조·문구는 A/C와 완전히 동일해야 한다는 원칙을 재확인하기 위해 v1.23 원문(스텝A/B 정확한 와이어프레임, "1/2"·"2/2" 인디케이터가 스텝 축이 아니라 A안/B안 노출 순서 축이라는 점, 자동 전환)을 다시 인용해 컨셉 교체가 구조 재해석으로 번지지 않게 못박음. (3) `28:2`의 4가지 조형 원리(대시보더/소프트섀도/washi tape/잉크스탬프)와 쿠폰 은유(선택 버튼 상태 표현)를 SCR-002/003 실제 요소(상품카드/신뢰배지/스텝B 2지선다 버튼)에 정확히 매핑하도록 개별 지시 — "색만 바뀐 A/C 복제"가 되지 않도록 완료 기준에 "카드 처리 원리가 A/C와 명확히 달라야 함"을 명시. (4) 테스트 품목은 기존과 동일하게 헤드폰으로 통일.
- **구체화 포인트**: A/C 관련 프레임 6개(`8:3`/`20:2`/`20:3`/`8:5`/`23:2`/`23:3`) 완전 보존을 완료 기준에 명시적으로 나열, design-systems 미투입(원본 값 직접 사용) 재확인, 다른 프로젝트 폴더 읽기 전용 재확인, SCR-007 불가침 재확인, 사용자 확정 전 후속 파이프라인(design-systems/design-qa) 진행 금지를 완료 기준 마지막 항목으로 재확인.

### 2026-08-24 — brand-designer 브리프(단독): "mercari 가격 UX 실험" 프로젝트 참여자 앱 — Mode A 1차(톤/컨셉) 라운드, 대표화면 SCR-002/003 고정
- **원본 요청**: 이 프로젝트 자체 디자인팀의 기존 화면과 완전히 독립된 대안 시안을 처음부터 새로 만드는 라운드(Type A, 브랜드/톤 최초 확정). brand-designer는 기존 화면을 본 적 없고 봐서도 안 됨 — 오직 Figma "레퍼런스" 페이지와 `docs/planning/06_기능정의서_화면정의서_v1.21.md`만 근거로 함. 대표 화면은 사용자가 SCR-002/SCR-003(가격 제안, 바텀시트 내부 스텝A/B 구조)으로 직접 고정.
- **핵심 판단**: (1) 확정 요구사항(당근마켓·토스류 친근 톤, 카드형 상품, 히어로 타이포 가격, 카드형 2지선다, 스텝 인디케이터, 신뢰배지, 카운트업 연출, 바텀시트+내부 2단계 구조)은 절대 재해석 금지. (2) 3개 톤 프레임은 서로 다른 실제 트렌드에 대응시키되, 레퍼런스 무드와 톤에 실제로 맞는 후보로 좁혀 제시 — 뉴브루탈리즘처럼 톤과 상충하는 트렌드는 배제 권고. (3) 레퍼런스 페이지 실제 열람을 문서 서술 대신 근거로 삼도록 재확인. (4) 산출물 배치는 지정된 파일 안 "mercari 가격제안 Concepts" 페이지에 3개 프레임.
- **구체화 포인트**: 각 톤 프레임에 트렌드명·팔레트(원본 hex)·타이포 톤·카드/모서리/그림자 방향·일러스트 방향을 필수 필드로 명시, SCR-002/003의 정확한 와이어프레임·문구를 원문 그대로 인용, 시안 3개 확정 전까지 후속 작업 진행 금지를 명시적으로 못박음.

### 2026-08-24 — ui-designer 브리프(단독): mercari 1차 라운드 — brand-designer 톤 프레임 3개(`8:3`/`8:4`/`8:5`) 확정 대기 상태에서, SCR-002/003 대표 화면 3장 제작(`figma-file-organization.md` 2-1번 2항)
- **원본 요청**: design-pl이 brand-designer가 방금 완성한 3개 톤 프레임(A 미니멀 신뢰형/B 네오팝 브루탈형/C 벤토 다크 글로우형, 각 팔레트·타이포·카드처리 표로 전달)을 그대로 이어받아, ui-designer가 SCR-002/003(가격 제안 화면) 대표 화면을 컨셉당 1장씩 총 3장 만들도록 요청. 사용자가 이미 초안 브리프를 상당히 구체적으로 작성해 전달(할 일/하지 않을 것/완료 기준 포함) — design-prompter는 이를 검증·보강하는 역할로 투입됨.
- **핵심 판단**: (1) 요구사항 문서 경로를 실제로 Glob/Read로 재확인한 결과 v1.22가 이미 `old/`로 이동했고 **v1.23이 최신본**임을 발견 — 이 프로젝트 문서가 세션마다 버전업되는 반복 패턴(v1.20→v1.21→v1.22→v1.23)이 계속되고 있어, 브리프에 "매번 old/ 폴더 유무로 최신 버전을 다시 확인하라"는 경고를 명시적으로 추가. (2) v1.23 원문(220~250줄 "-1. UX 패턴 방향", 384~444줄 "SCR-002/SCR-003")을 직접 읽어 사용자 초안이 놓친 두 가지 구조적 디테일을 보강: ① 스텝 A→B 전환은 **자동(타이머)**이지 클릭 버튼이 아니다(v1.17/v1.18 근거) — 프레임에 스텝 사이 "다음" 버튼을 넣지 않도록 명시. ② "1/2"·"2/2" 진행 인디케이터는 스텝 A/B 축이 아니라 **A안/B안 노출 순서** 축이다(v1.17 명시) — 혼동해 별도 스텝 인디케이터를 만들지 않도록 명시. (3) 스텝 B의 2지선다(수용/거절)는 SCR-004의 A/B 좌우 비교 카드와 배치 원리가 다름(세로 스택 vs 좌우 비교)을 구분해 명시 — 두 "카드형 선택 버튼"을 같은 모양으로 오인하지 않게 함. (4) 테스트 품목 3종 중 실제 상품명이 확정된 것은 헤드폰(Beats by Dre Studio Headphones)뿐이라, 대표 화면의 테스트 품목을 이걸로 통일하도록 구체적으로 지정(나머지 2종은 "추후 확정"이라 목업에 부적합). (5) 각 컨셉의 카드 처리 표(보더/그림자/radius)를 게슈탈트 원리(근접성/피규어-그라운드 대비/명시적 그리드 분할)로 번역해 "색만 다르고 레이아웃 골격이 같은" 실패를 구조적으로 막음 — 특히 C(벤토)는 brand-designer가 정의한 "4개 벤토 셀 분리" 원칙을 SCR-002/003 실제 요소(이미지/가격/배지/진행표시)에 정확히 매핑하도록 지시.
- **구체화 포인트**: KRW 환산은 정확한 환율/반올림 규칙 확정 없이 3개 시안에 동일한 근사값만 일관 적용하면 충분하다고 스코프를 좁혀 불필요한 정밀도 요구를 막음(Simplicity First), 정확한 문구(스텝 A/B 와이어프레임 텍스트 전체)를 원문 그대로 브리프에 인용해 재해석 여지 차단, 비교표 판정 원칙(2개 이상 항목 동일 시 미완성) 재확인, `/Users/aydana/dev/portfolio/bigdata/01-ML_mercari price_2608` 읽기 전용·SCR-007 불가침·design-systems 미투입·시안 확정 전 후속 진행 금지를 하지 않을 것으로 재확인.

### 2026-08-24 — brand-designer 브리프(단독): mercari 1차 톤 라운드 재작업 — B 계보 병합(네오팝+콜라주 저널링→브루탈 콜라주 신규) + A/C 확장 프레임 추가
- **원본 요청**: 사용자가 페이지 "브랜드컨셉"(`8:2`)의 현재 상태(A=`8:3` 미니멀 신뢰형/네오팝B=`8:4` 이미 미채택/콜라주저널링B=`28:2` 아직 미라벨/C=`8:5` 벤토 다크)를 보고 두 가지를 동시에 요청: ① 네오팝(`8:4`, 굵은 잉크 아웃라인+하드 오프셋 그림자+플랫 컨페티)과 콜라주 저널링(`28:2`, washi tape+티켓스텁 대시보더+잉크스탬프+소프트 디퓨즈 그림자)을 팔레트 고정(코랄/옐로/바이올렛/크림/잉크 5개 hex)한 채 스타일 언어만 병합한 완전히 새로운 Concept B 톤 프레임을 `8:2`에 신규 제작 → 병합 후 `28:2`에 `❌ 미채택 —` 라벨만 붙임(내용 무수정, `8:4`는 그대로 둠). ② A(`8:3`)·C(`8:5`)는 절대 무수정, 그 옆에 레이아웃/정보위계를 한 단계 더 발전시킨 "확장" 톤 프레임을 각각 신규 제작(컬러 아이덴티티는 유지, A=블루 미니멀+절제된 배지/신뢰 콜아웃 박스 인사이트, C=다크히어로→라이트 바텀시트 전환+펄스 인디케이터+칩 인터랙션 디테일 인사이트). 다른 프로젝트 폴더의 원본 레퍼런스 이미지(읽기 전용) 열람은 선택.
- **핵심 판단**: (1) 이번 라운드는 신규 3원 시안 세트를 만드는 게 아니라 기존 A/B/C 트리오에 대한 지정된 개수(병합 B 1개 + A 확장 1개 + C 확장 1개)의 보강 제작이라, "시안 3개 확정 전까지 후속 금지" 문구는 design-pl로부터 그렇게 지시받지 않아 임의로 새로 추가하지 않음(스스로 시안 여부를 재판단하지 않는다는 원칙 준수). (2) house rule 2번 "미채택 시안 원본 보존"을 그대로 적용해 `28:2` 라벨링은 이름만 변경(내용 무수정), `8:4`는 이미 라벨링된 상태이므로 이번엔 손대지 않음을 재확인. (3) "장난스럽지만 손으로 꾸민 듯한 브루탈 콜라주"라는 추상적 톤 요구를, "하드 그림자+두꺼운 아웃라인=구조를 잡는 베이스 레이어" vs "washi tape/잉크스탬프/티켓스텁=그 위에 얹는 장식 레이어(소프트 그림자 전용)"라는 레이어 역할 분담 원칙으로 구체화해 "한 화면에 두 그림자 스타일이 공존해도 어수선하지 않고 의도적으로 보이게" 하는 실행 기준을 제시. (4) A/C 확장의 참고 인사이트(배지·콜아웃/펄스·칩)는 컬러를 베끼지 않고 우리 팔레트로 치환해서 반영하도록 명시(다른 프로젝트 레퍼런스 열람이 브랜드 컬러 오염으로 번지지 않게 하는 안전장치).
- **구체화 포인트**: 새 프레임 3개 모두 `8:2` 페이지 안, 기존 프레임과 겹치지 않는 위치 배치 + 네이밍에 "Concept B — 브루탈 콜라주"/"Concept A — 확장"/"Concept C — 확장" 계열임을 명시(최종 워딩은 brand-designer 재량), 완료 기준에 design-pl 보고 항목(신규 프레임 3개 노드 ID, `28:2` 라벨 텍스트 변경 실측 확인, `8:3`/`8:4`/`8:5` 무변화 근거) 명시, 이번 라운드는 브랜드 톤 확정 전 중간 보강이라 `docs/design/confirmed/`·`docs/design/brand-guide.md` 갱신 대상 아님을 재확인.

### 2026-08-24 — ui-designer 브리프(단독): mercari 1차 톤 라운드 후속 — 병합 B(`43:2`) 대표화면 신규 제작 + A 확장(`48:2`)/C 확장(`51:2`) 대표화면 신규 제작, `36:35`/`36:2`(콜라주 저널링 B 대표화면) 미채택 라벨링
- **원본 요청**: 직전 라운드에서 brand-designer가 완성한 톤 프레임 3종(병합 B `43:2`/A 확장 `48:2`/C 확장 `51:2`)에 대응하는 대표화면을 ui-designer가 신규 제작. 병합 B는 기존 콜라주 저널링 B 대표화면(`36:35`/`36:2`)을 대체하는 성격이라 라벨링까지 포함. A 확장·C 확장은 기존 `20:3`/`23:3` 옆에 순수 추가(대체 아님). C 확장에는 브랜드 톤 프레임에서 정의된 레이아웃 전환·펄스 인디케이터·칩 외에, 이번에 새로 지정된 기능 디테일 2가지(2안 미선택 시 "다음 보기" 버튼 disabled, 기존 "1/2·2/2" 텍스트 인디케이터를 대체하지 않고 보완하는 점 스텝 인디케이터)가 추가로 요구됨.
- **핵심 판단**: (1) house rule 2-4번(반응형 원칙)과 유사하게, 병합 B 라벨링은 "라벨링 먼저 → 옆에 신규 제작"이 아니라 이번엔 신규 제작이 먼저 명시되고 라벨링은 완료 후 처리로 구성됐지만, 실제 실행 순서는 house rule 3-B번 3항의 "수정 시작 전에 먼저 라벨링" 원칙과 어긋나지 않도록(신규 프레임 제작과 기존 프레임 라벨링은 서로 다른 프레임이라 순서 상관없음을 확인 후) 브리프에 순서를 명확히 못박음: 신규 제작 완료 후 `36:35`/`36:2` 라벨링, 내용 무수정. (2) `20:3`/`23:3`은 순수 확장(대체 아님)이므로 원본 무변화 확인이 완료 기준에 필수 — A/B/C 세 계열의 "무엇을 대체하고 무엇을 보존하는지"가 서로 다른 패턴이라 헷갈리지 않도록 각 작업 블록에 개별 명시. (3) C 확장의 신규 기능 디테일(disabled 버튼, 점 인디케이터)은 브랜드 톤 프레임에 없던 새 요구사항이므로, "기존 확정 요소(1/2·2/2)를 대체하지 않고 보완"이라는 제약을 명확히 해 재해석으로 기존 인디케이터가 사라지는 사고를 예방. (4) 3개 신규 화면 모두 각 톤 프레임의 원본 5색 hex만 사용(design-systems 미투입 재확인)하도록 개별 명시.
- **구체화 포인트**: 병합 B의 구조 레이어(하드 그림자+아웃라인)/장식 레이어(washi tape·잉크스탬프·티켓스텁+소프트 그림자) 역할 분담을 SCR-002/003 실제 요소에 매핑 지시, A 확장의 정보 위계(콘텐츠>배지>콜아웃)를 크기·채도 대비로 구체화, C 확장의 disabled 버튼 처리를 저채도·저대비·클릭불가 시각 언어로 구체화, 완료 기준에 design-pl 보고 항목 5가지(신규 노드 ID, 라벨링 재조회 확인, 원본 무변화 근거, disabled/점 인디케이터 반영 자체점검, 5색 hex 준수 자체점검) 명시, 이번 라운드도 브랜드 톤 확정 전 보강이라 확정 스펙 문서(`docs/design/confirmed/`) 대상 아님 재확인.
</content>
