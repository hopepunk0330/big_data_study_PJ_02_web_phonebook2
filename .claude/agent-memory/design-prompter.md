# design-prompter 메모리

이 파일은 design-prompter가 작업 시작 시 읽고, 작업 종료 시 기록을 남기는 메모리/로그입니다.

## 작업 로그

### 2026-07-14 — design-systems 브리프: NeoInput/CornerInput Content(Placeholder/Filled) 축 추가 + NeoSelect Content(Placeholder/Selected) 축 추가 + 옵션 hover 신설 (기본 상태 체크리스트 실전 적용 2호)
- **원본 요청**: 사용자가 "인풋과 셀렉인풋창은 Default/에러/플레이스홀더/포커스로 만들잖아? 꼭 명시시켜서 알아서 만들도록 해줘"라고 지적. 직전 라운드(26차)에서 Input=Error, Select=Open까지는 신설했으나 Placeholder(안내 문구 노출) 상태가 빠져 있었음 — `.claude/agents/design-systems.md` 체크리스트에 이미 Input=Default/Placeholder/Focus/Error/Disabled, Select=+Open이 명문화돼 있어 재정의 없이 그 체크리스트를 그대로 따르도록 지시. design-pl이 사용자 승인 받은 실사용 매트릭스(Focus×Error×Placeholder=Yes 조합만 제외, 근거: 포커스 중 에러 지연 표시 UX 관례)를 그대로 전달.
- **핵심 판단**: (1) design-system.md 0-10절/5절 표를 직접 대조해 현재 실제 베이스라인이 NeoInput/CornerInput=Focus×Error 2×2=4 variant, NeoSelect=State(Default/Open) 2 variant임을 먼저 재확인 — 사용자가 준 "현재 상태" 서술이 문서 실측과 정확히 일치함을 검증한 뒤 브리프에 인용. (2) **중요한 함정 발견**: 0-3절 기록에 CornerInput 기존 텍스트 노드가 "placeholder '윤아'"로 명시돼 있어, 현재 baseline 4 variant가 실제로는 이미 "Placeholder 의미"로 렌더링돼 있을 가능성이 있음 — 이번 매트릭스는 기존 4개를 그대로 Content=Filled로 매핑하므로, design-systems가 자동으로 "라벨만 바꾸면 끝"이라 오판하지 않도록 "0단계 선행 확인"으로 현재 텍스트 색상(ink/900인지 muted 계열인지)과 내용을 먼저 실측하고, Filled 의미(값 채움, ink/900)에 안 맞으면 이번 작업 범위 안에서 바로잡으라고 명시. (3) NeoSelect Open 옵션 hover는 0-10절 B에서 "elevation 판단"만 기록되고 실제 hover는 추가되지 않았음을 문서 대조로 확인 — 기존 Selected×Open 패널에도 지금까지 hover가 없었던 실제 갭이므로, 이번에 신규 Placeholder×Open뿐 아니라 기존(재매핑된) Selected×Open 옵션 패널에도 함께 hover를 적용하도록 범위를 넓혀 명시(신규 것만 만들고 기존 것을 방치하지 않게).
- **구체화 포인트**:
  1. 작업 A(NeoInput/CornerInput): 매트릭스 그대로 7 variant(Placeholder×Error×Focus=Yes/Yes/Yes 1건 제외) 확장 지시, 제외 근거를 design-system.md에도 기록하도록 명시. Placeholder 텍스트색은 `color/text-muted-subtle`(#888)/`color/text-muted`(#777) 중 확정 프레임(login `247:6666`/join `241:1552`/main-수정 모달 `248:8103`/NeoInput은 main 검색창) 실측으로 확정, 7-1절 §5 "placeholder 텍스트 대비 미달" RESOLVED 항목과 같은 값이어야 함을 교차 검증 조건으로 명시. Filled 텍스트는 기존 ink/900 재사용, 신규 보더/그림자 토큰 없음(Focus/Error 축 기존 바인딩 유지). CornerInput 코너 브래킷 부활 금지 재확인. placeholder 문구는 확정 프레임 재관찰로 채택(임의 작문 금지, 사용자가 언급한 레거시 `314:881`("이름 (1~5자)")은 확정 프레임과 대조 후 참고만).
  2. 작업 B(NeoSelect): Content(Placeholder/Selected)×State(Default/Open) 2×2=4 variant. Placeholder 문구는 사용자가 이미 확정한 "종류선택"을 재관찰 없이 그대로 채택. 옵션 hover는 신규 필수 요구사항으로 명시(틀만 만들지 말 것) — 9절 hover 블렌드 공식(브랜드색 배경 t=12%/흰 배경+아웃라인류 t=6%, 정확한 계산은 9절 재확인)을 재사용하도록 지시, NeoSelect 옵션 행은 흰 배경 기반이라 t=6% 계열이 유력하나 최종 판단은 design-systems.
  3. 스펙 시트: NeoInput/CornerInput 7칸 그리드(제외된 1칸은 빈 셀+제외 근거 라벨로 명시해 버그처럼 보이지 않게), NeoSelect는 4 Content×State + 옵션 hover 예시 포함.
  4. 문서화: design-system.md 신규 절(0-11절 예상) + 1-2/1-3/3/5/9 관련 절 갱신, Focus×Error×Placeholder 제외 근거와 hover 공식 재사용 근거를 명시적으로 기록하도록 지시. 화면정의서(SCR-900)는 결과 보고에 메모만, 문서 수정 금지. 공통 제약 재확인: 확정 8개 프레임 읽기 전용, 신규 raw hex 금지, 완료 전 design-systems 자체 재대조 필수.

### 2026-07-14 — 브리프 2건(재개 라운드): graphic-designer(Pixel/EyeOff 신설) → design-systems(NeoSelect hover 폭 버그 수정 + Pixel/EyeOff 등록 + Placeholder 문서화 마무리)
- **원본 요청**: 서버 문제로 중단됐던 라운드 재개. design-scanner가 현재 Figma 상태를 재확인한 결과: (1) NeoSelect(`387:13`) Open 옵션 패널 hover 배경이 패널 전체 폭을 안 채우는 버그 미착수, (2) Pixel/EyeOff 짝 아이콘 미착수(Pixel/Eye `281:405`만 존재), (3) NeoInput/CornerInput Placeholder variant(각 7개)와 NeoSelect Placeholder/Selected×Default/Open(4개)는 **Figma에는 이미 생성 완료**돼 있으나(직전 라운드 결과물) `docs/design/design-system.md`에는 아직 기록이 없는 문서화 갭. design-pl이 라우팅(graphic-designer 먼저, design-systems 다음)과 각 작업 내용을 이미 확정해 전달, design-prompter는 실행 가능한 형태로 구체화만 함.
- **핵심 판단**: (1) graphic-designer 브리프에서 "Graphic Assets" 페이지(`90:2`)가 사용자에 의해 이미 삭제된 상태임을 `docs/design/graphic-assets.md`로 먼저 확인하고, 정상 파이프라인(원화를 그 페이지에 먼저 그림)을 못 찾아 헤매지 않도록 "이번엔 예외적으로 Icons 페이지에 바로 raw 벡터를 그린다"고 명시 — 없는 페이지를 찾다가 시간 낭비하거나 잘못된 페이지에 그리는 실수 방지. (2) Pixel/Eye(`281:405`)가 `PixelEye`(`247:6814`)라는 raw 노드에서 design-systems가 나중에 `createComponentFromNode`로 컴포넌트화한 선례를 그대로 재사용하도록, graphic-designer는 raw 벡터만 만들고 **아직 Figma Component로 만들지 않는다**는 역할 경계(2-2번 "크래프트 vs 시스템 등록 분리")를 명시적으로 못박음 — 안 그러면 그리는 사람이 등록까지 하려다 컴포넌트화 규칙(등록 절차·문서 반영)을 놓칠 위험. (3) design-systems 브리프의 (C) Placeholder 작업은 "이미 Figma에 존재하니 다시 만들지 말라"를 최우선으로 강조 — design-scanner 보고를 신뢰하되 `get_metadata` 재확인은 여전히 요구(문서 갭이지 산출물 갭이 아님을 구분해서 전달, 중복 생산 방지).
- **구체화 포인트**:
  1. graphic-designer 브리프: Pixel/Eye 조형 관례(14×10, 잉크 #1a1a1a, 6블록 조합, 블록 방식 vs Close의 stroke 방식 구분)를 재확인 지시로 명시, "닫힌 눈" vs "슬래시" 두 방향 중 판단은 크래프트 재량으로 위임하되 근거 메모는 필수화. 이름은 기존 컨벤션(NoResult 같은 복합어 선례)에 비춰 `Pixel/EyeOff` 기본값 채택, 배치 간격은 기존 클러스터 실측값 재사용(새 값 발명 금지).
  2. design-systems 브리프 (A): 원인을 짐작하지 않고 `get_metadata`로 hover 배경 fill 노드와 텍스트 노드의 padding/폭 차이부터 실측하도록 지시, 수정 원칙은 "배경=행 전체 폭, 텍스트=기존 padding 값 그대로 재사용"으로 좁힘(새 padding 수치 발명 금지). Content=Placeholder(`387:12`)/Content=Selected(`401:869`) 두 variant 모두 적용 필수.
  3. design-systems 브리프 (B): graphic-designer 산출물을 그대로 이어받아 `createComponentFromNode`만 수행(별도 재작업 없음), graphic-assets.md/design-system.md 두 문서 모두 카운트 숫자(10종→11종)까지 정확히 갱신하도록 명시.
  4. design-systems 브리프 (C): 7-1절 §5 "placeholder 텍스트 대비 미달"(RESOLVED) 기존 항목과의 교차 검증을 재차 강조(직전 라운드 브리프에서 이미 요구했던 조건이 실제로 지켜졌는지 이번에 문서화 단계에서 재확인). Component Specs `388:746`을 새로 만들지 않고 기존 2칸 그리드를 4칸(2×2)으로 확장하도록 지시(2-5번 규칙 — 새 프레임 남발 금지). design-system.md 신규 절 번호는 실제 파일의 최신 상태에 맞춰 design-systems가 판단하도록 유연하게 열어둠(0-11 예상이라고만 안내).
  5. 공통 제약: 확정 8개 프레임 읽기 전용, Link(`341:3`) 텍스트는 사용자가 이미 수정해둔 값이므로 불가침, 이번 라운드는 신규 라벨링(❌ 미채택/❌ 폐기) 대상이 없음을 명시해 불필요한 라벨링 방지.

### 2026-07-15 — design-systems 브리프: TypeSelector/NeoInput/CornerInput/Sidebar Nav Item Focus 구조를 버튼류 패턴("State 열거형 안의 단일 값")로 소급 단순화 (직교 Focus 축 폐지)
- **원본 요청**: fileKey `zgGlMBwFglaDlaeyP4CkgR`. 이 4개 컴포넌트는 9-3절에서 `State`(Selected/Unselected 등)와 별도로 `Focus=No/Yes` 직교 축을 갖고 있어 조합 variant가 불어나 있었다(TypeSelector 16, NeoInput/CornerInput 각 7, Sidebar Nav Item 4). 사용자가 "주목적이 Focus가 아니다", 복잡하다는 이유로 버튼류(NeoBtn 등, 9-2절)처럼 State 열거형 하나의 값으로 통합해달라고 명시적으로 요청, 조합 소실(정보 손실, 예: TypeSelector Selected+Focus 4종 삭제)은 사용자가 감수. 목표 variant 수·삭제/유지 대상·이름 규칙까지 사용자가 이미 구체적으로 확정해 전달했으므로, design-prompter는 새 디자인 판단을 추가하지 않고 그 사양을 실행 순서·검증 절차·문서화 형식으로만 구조화.
- **핵심 판단**: (1) `.claude/agents/design-systems.md` 33~34행에 "Focus는 별도 축을 만들지 않는다, State 열거형 값 하나로 둔다"는 규칙이 **이미 2026-07-15에 명문화**돼 있음을 확인 — 이번 작업은 새 규칙 도입이 아니라 그 규칙을 아직 못 따르고 있던 4개 컴포넌트(9-3절 표의 정확히 그 4개)에 소급 적용하는 정리 작업이라는 점을 브리프 서두에 명시(design-systems가 "왜 갑자기 이런 요청이"라고 헤매지 않게). (2) 이번 라운드는 **새 시각 요소를 그리는 작업이 아니다** — 삭제 대상/유지 대상 모두 사용자가 "시각 변경 없음"이라고 세 번 명시했으므로, design-systems.md의 "Focus 값은 Default clone+ink 링 추가로만 만든다"는 생성 규칙이 이번엔 적용 대상이 아님(신규 Focus 생성이 아니라 기존 값 재구조화+삭제)을 명확히 구분해 브리프에 적음 — 안 그러면 design-systems가 불필요하게 새로 그리려 시도할 위험. (3) 정확한 삭제 대상 variant의 top-level node ID를 문서(design-system.md 0-9/0-10/0-11/0-12절)에서 교차 확인한 결과, Sidebar Nav Item(`287:17` 유지 대상)만 명확한 ID가 있고 TypeSelector Selected+Focus 4종/NeoInput·CornerInput Focus=Yes 조합들은 일부 텍스트 자식 노드 ID(예: NeoInput `398:887`)만 문서화돼 있을 뿐 variant 최상위 프레임 ID는 없음 — design-prompter가 실제로 보지 못한 ID를 추측해 브리프에 박아넣지 않고, "get_metadata로 각 ComponentSet 전체 variant를 속성값(Category/State/Focus/Content/Error) 기준으로 먼저 열거해 매핑표를 만들고, 그 매핑표의 속성값으로 대상을 특정하라"는 절차를 못박아 오귀속 위험을 차단.
- **구체화 포인트**:
  1. 공통 0단계: 삭제 대상마다 파일 전체 인스턴스 여부를 `use_figma` 읽기 전용으로 먼저 확인(2-4번 Detach 우선 규칙과 같은 원리를 "삭제"에도 적용) — 인스턴스 발견 시 삭제 중단, design-pl 보고 후 대기.
  2. TypeSelector(`257:28`): Category(4)×State(Selected/Unselected/Focus)(3)=12로 축소. Unselected+Focus=Yes 4개는 이름만 State=Focus로 정리(시각 무수정), Selected+Focus=Yes 4개 삭제, Selected+Focus=No→Selected/Unselected+Focus=No→Unselected 이름 정리.
  3. NeoInput(`288:12`)/CornerInput(`288:27`): Content×Error 기본 4(Filled×Error=No/Yes, Placeholder×Error=No/Yes, 전부 기존 Focus=No 값)+단일 Focus 1(기존 Placeholder×Error=No×Focus=Yes 룩 유지, "방금 탭 이동해 들어온 빈 필드" 뼈대로 채택)=5. 나머지 Focus=Yes 조합 전부 삭제.
  4. Sidebar Nav Item(`258:29`): State(Active/Inactive/Focus)(3)=3. Inactive+Focus=Yes(`287:17`, 투명 배경 때문에 3px ink OUTSIDE 스트로크로 특수 구현)는 이름만 정리하고 절대 재작업 금지, Active+Focus=Yes 1개 삭제.
  5. Component Specs 4개 시트(`343:1146`/`344:721`/`344:740`/`343:1106`) 그리드 재구성, description 필드 "Focus=No/Yes" 언급 정리.
  6. design-system.md는 반드시 전체 Read 후 append(Write 전체 덮어쓰기 금지, 과거 586→61줄 손상 사고 재확인 필요), 5절 표+9-3절 표(이번에 9-3절 전체가 사실상 대상 소멸 — 표 자체를 "레거시/해소" 처리하도록 안내) 갱신.
  7. 완료 후 design-qa가 4개 컴포넌트 스팟체크(variant 개수·이름·시각 회귀 없음) 진행 예정임을 다음 단계로 명시(design-prompter가 직접 design-qa를 부르지 않음).

### 2026-07-15 — 브리프 2건(병렬): design-systems(체크박스 등록 + 라디오/디바이더 선제적 판단) + ux-designer(로그인 체크박스 흐름 검토 + 8개 확정 프레임 재스캔) — 2-6번(선제적 기본 구성) 신설 규칙 첫 적용
- **원본 요청**: 사용자가 design-pl을 통해 승인("[메인 세션 확인] 사용자가 실제로 승인함"). fileKey `zgGlMBwFglaDlaeyP4CkgR`, 확정 8개 프레임(부모 섹션 `248:11689`)에 대한 커버리지 감사 라운드(2-4번)이면서, 오늘 신설된 2-6번(선제적 기본 구성) 규칙의 첫 실전 적용. 배경: 로그인 확정 프레임(`247:6666`)의 "☐ 로그인 상태 유지" 체크박스(`247:6822`)가 여러 라운드를 거치고도 정식 컴포넌트로 등록되지 않았던 걸 사용자가 재지적. design-pl이 두 워커(design-systems=쓰기, ux-designer=읽기 전용 병행 검토)를 병렬 호출할 예정이라 브리프 2개를 동시 작성.
- **핵심 판단**: (1) design-system.md 1~7절/0-15/0-16절을 직접 재확인해, "Focus는 State 열거형 값 하나, Default clone+ink 링 추가로만 만든다"는 기존 확정 규칙을 체크박스/라디오에도 예외 없이 적용하도록 재확인시킴(새 규칙 발명 아님, 기존 체크리스트 적용). (2) docs/planning 02(화면정의서) 직접 대조 결과, "종류" 선택은 이미 드롭다운(select)+TypeSelector 칩으로 구현돼 있고 라디오 그룹 형태의 단일 선택 UI가 문서 어디에도 없음을 확인 — 이 사전 조사 결과를 design-systems 브리프에 "참고 근거"로 제공하되, 최종 판단(라디오 필요/불필요)은 design-systems가 확정 프레임을 직접 스캔한 뒤 내리도록 강제하지 않고 열어둠(design-prompter가 결론을 대신 내리지 않는다는 원칙 준수). (3) Divider 판단 근거로 Contact Row(`351:299`)가 이미 자체 하단 구분선(`color/border-divider-warm`)을 갖고 있다는 기존 문서 사실을 짚어, "행 구분선"은 이미 흡수돼 있으니 "그 외 다른 자리에 독립 Divider가 필요한가"로 판단 프레임을 좁혀줌(무분별한 확장 방지, 2-6번의 "과하게 확장하지 않는다" 지침 반영). (4) 01_구현요구사항서(§1 데이터 모델)를 직접 확인한 결과 "로그인 상태 유지"에 대응하는 세션 연장/영구 쿠키 관련 FR이 문서에 안 보임을 발견 — 이건 design-systems가 아니라 ux-designer의 흐름 검토 영역이라 판단해 ux-designer 브리프의 1번 핵심 검토 포인트로 명시(체크박스가 실제 기능 스펙과 연결되는지 여부를 사실 확인만 하고 판단은 design-pl/사용자에게 넘기도록 범위를 좁힘 — ux-designer가 "삭제해야 한다"처럼 임의 결론을 내리지 않게).
- **구체화 포인트**:
  1. design-systems 브리프: 0(문서/체크리스트 선행 확인) → 1(체크박스 `247:6822` 실측+Default/Checked/Focus/Disabled 4 variant 등록, 텍스트는 컴포넌트 속성, 체크마크가 아이콘 크래프트급이면 즉시 중단+design-pl 보고) → 2(8개 프레임 전체 라디오 raw shape 스캔, 발견 시 동일 절차, 미발견 시 docs/planning 근거 판단+문서화) → 3(Divider 필요성 판단, Contact Row 기존 구분선과의 관계 근거로 좁혀서 판단) → 4(CornerInput `344:740` 라벨 정정, 단 0-16절 이후 실제 구조가 바뀌었을 수 있어 먼저 재확인 후 필요시만 수정) → 5(자체 재대조) → 6(design-system.md 전체 Read 후 append, 손상 사고 재경고). 신규 컴포넌트는 1번 규칙(컴포넌트당 전용 페이지)에 따라 COMPONENTS 구역에 새 페이지로 배치하고 8절 페이지 순서 표도 갱신하도록 명시.
  2. ux-designer 브리프: 1번(체크박스 흐름 논리 — 문서상 기능 근거 존재 여부 사실 확인) → 2번(8개 프레임 전체 재스캔, 단 "비밀번호 찾기→재설정" 건은 이미 해결된 사례이므로 재확인만 하고 새로 발견한 것처럼 중복 보고 금지 — 이 지침을 명시적으로 강조). 확정 프레임은 발견만 하고 수정하지 않음을 재확인, 시각 스펙(색상/크기)은 design-systems 몫이라 다루지 않도록 역할 경계 명시.
  3. 공통: 확정 8개 프레임 읽기 전용(design-systems/ux-designer 둘 다), 두 워커가 병렬 호출되지만 Figma 쓰기 작업은 design-systems만 하므로 충돌 없음을 서두에 명시.

### 2026-07-15 — design-systems 브리프 4건(순차 호출용, 1콜 1브리프): TypeSelector 스펙시트 clipsContent 버그 + Focus 링 "흰 갭+잉크 링" 2겹 공식 도입(버튼류→입력/선택/체크박스 확장) + Error 예외 + 문서 텍스트 전수 점검
- **원본 요청**: 사용자가 design-pl 경유 승인. 배경은 사용자 스크린샷으로 확인된 결함 — 기존 9-1절 Focus 공식(단일 ink `DROP_SHADOW` spread3)이 검은/진한 보더를 쓰는 컴포넌트에서 보더와 같은 색이라 링이 사실상 안 보임. "검은 링들은 통일해야 할 거 같다"는 사용자 코멘트. 이전 세션에 한 호출에 여러 작업을 몰아넣어 API 출력 토큰 초과로 응답이 여러 차례 끊긴 전례(design-pl 메모리 25번째 항목 "서브에이전트 출력 토큰 한도 초과 대응" 참고)가 있어, design-pl이 4개 브리프를 순차 호출용으로 분리 요청.
- **핵심 판단**: (1) design-system.md를 전체 Read해 최신 상태(0-18절까지, 12절까지)를 직접 대조 — TypeSelector 스펙 시트 노드 ID가 문서상 `343:1146`으로 기록돼 있는데 이번 인계 정보는 `450:714`를 지목해 불일치를 발견, 브리프 A에 "문서 기록과 인계 정보가 다르니 get_metadata로 Component Specs 페이지(`342:2`)에서 실제 'Spec — TypeSelector' 프레임을 먼저 재확인하라"는 검증 절차를 명시해 오귀속을 차단(0-15절에서 이미 스펙 시트 4개가 한 차례 전면 재구성됐던 이력이 있어 ID 드리프트 가능성이 실제로 있음). (2) 9-1절에 이미 문서화된 단일 ink 링 공식을 완전히 새 공식으로 교체하는 것이므로, 브리프 B에서 9-1절 문구 자체를 갱신하도록 명시(9-1절이 전체 컴포넌트의 공통 참조점이라 여기를 안 바꾸면 이후 컴포넌트가 옛 공식을 계속 베낄 위험). (3) 브리프 C는 "정확한 spread 조합값은 브리프 B에서 이미 스크린샷으로 검증·확정된 값을 9-1절에서 다시 읽어 재사용"하도록 지시해 같은 실험을 두 번 하지 않게 하되, 배경/보더 조합이 다른 컴포넌트(TypeSelector 연회색 보더, NeoInput 흰 배경)에는 반드시 스크린샷 재검증을 요구해 "값은 맞는데 실제로 안 보임" 사고(design-pl 메모리 기존 교훈)가 재발하지 않게 함. (4) TypeSelector 미선택 보더가 실측상 `#CCCCCC`(연회색, 1-3절/0-1절 기록)인데 인계 지시문은 "검은 보더 케이스"로 서술한 점을 발견 — design-systems가 실제 라이브 색을 재확인 없이 지시문 표현만 믿고 잘못된 전제로 작업하지 않도록 브리프 C에 명시적 검증 각주를 추가. (5) Sidebar Nav Item `287:17`은 이미 9-5절에서 별도 사유(완전 투명 배경)로 DROP_SHADOW 대신 3px ink OUTSIDE 스트로크로 특수 구현돼 있어, 이번 2겹 공식을 기계적으로 적용하면 기존 특수 처리를 깨뜨릴 위험 — "스트로크 2겹으로 재구현할지 예외 유지할지"를 design-systems 판단에 맡기되 근거 기록을 필수화(임의로 재작업하다 회귀시키지 않도록).
- **구체화 포인트**:
  1. 브리프 A(TypeSelector 스펙시트 clipsContent): 0-15절에 이미 "전 Cell/Row/Grid에 clipsContent=false 정정"이 기록돼 있으나 검증 스크린샷은 NeoInput/CornerInput만 언급되고 TypeSelector는 빠져 있음을 근거로, 회귀 또는 미완료 가능성이 있는 지점으로 특정해 전달. NeoInput/CornerInput 스펙시트(`344:721`/`344:740`)를 "이미 정상인 참조 사례"로 제시해 같은 방식 그대로 적용하도록 범위를 좁힘.
  2. 브리프 B(Focus 2겹 공식 — 버튼류 9-2절): 흰 갭+잉크 링의 정확한 spread 수치는 design-systems가 실제로 스크린샷 검증하며 확정하도록 열어두되(design-prompter가 수치를 대신 정하지 않음), "안쪽 흰색 DROP_SHADOW + 바깥쪽 ink DROP_SHADOW(흰 갭만큼 spread 추가)" 2-effect 스택 방식이라는 구현 메커니즘만 명시. 색 배경(Amber)과 검은 보더(Neutral) 양쪽 실제 스크린샷 검증을 완료 조건으로 명시(단순 raw effects 값 확인으로 끝내는 것 금지 — 과거 "값은 맞는데 안 보임" 사고 재발 방지).
  3. 브리프 C(Focus 2겹 공식 확장 — 0-15/0-16/0-17절 입력·선택·체크박스 + Error 예외): 일반 잉크 링 적용 대상(TypeSelector/NeoInput/CornerInput/Sidebar Nav Item/Checkbox)과 Error+Focus 빨간 링 예외(NeoInput/CornerInput `456:2`/`456:4`, 기존 `color/border-error` 재사용, 새 색 금지)를 분리해 명시. Sidebar Nav Item `287:17` 예외 처리는 판단 위임(스트로크 2겹 재구현 vs 예외 유지) + 근거 기록 필수.
  4. 브리프 D(문서/description 전수 점검): 이미 발견된 사례(Input 페이지 `100:3` "State=Error" 오기술 — 실제로는 Error가 별도 축)를 예시로 제시해 어떤 종류의 불일치를 찾아야 하는지 구체적 기준을 줌. 장황한 서술 대신 표/불릿 형식 강제(토큰 초과 재발 방지, design-pl 메모리의 "문서화 단계는 간결하게" 교훈 반영).
  5. 공통: 4개 브리프 전부에 "docs/design/design-system.md는 전체 Read 후 Edit로만, Write 전체 덮어쓰기 금지"(문서 손상 2회 사고 이력)와 "확정 8개 프레임(`248:11689` 하위) 읽기 전용" 재확인 문구 포함. 브리프는 순차 호출 전제라 각각 이전 브리프 완료를 전제로 자기완결적으로 작성.
