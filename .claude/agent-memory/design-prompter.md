# design-prompter 메모리

이 파일은 design-prompter가 작업 시작 시 읽고, 작업 종료 시 기록을 남기는 메모리/로그입니다.

## 작업 로그

### 2026-07-15 — 브리프 2건(병렬): design-systems(체크박스 등록 + 라디오/디바이더 선제적 판단) + ux-designer(로그인 체크박스 흐름 검토 + 8개 확정 프레임 재스캔) — 2-6번(선제적 기본 구성) 신설 규칙 첫 적용
- **원본 요청**: 사용자가 design-pl을 통해 승인. fileKey `zgGlMBwFglaDlaeyP4CkgR`, 확정 8개 프레임(부모 섹션 `248:11689`)에 대한 커버리지 감사 라운드이면서, 2-6번(선제적 기본 구성) 규칙의 첫 실전 적용. 로그인 확정 프레임(`247:6666`)의 "☐ 로그인 상태 유지" 체크박스(`247:6822`)가 여러 라운드를 거치고도 정식 컴포넌트로 등록되지 않았던 걸 재지적.
- **핵심 판단**: (1) "Focus는 State 열거형 값 하나, Default clone+ink 링 추가로만 만든다"는 기존 확정 규칙을 체크박스/라디오에도 예외 없이 적용. (2) docs/planning 02(화면정의서) 직접 대조 결과, "종류" 선택은 이미 드롭다운+TypeSelector 칩으로 구현돼 있고 라디오 그룹 형태가 문서에 없음을 확인 — 참고 근거로만 제공, 최종 판단은 design-systems가 직접. (3) Contact Row가 이미 자체 하단 구분선을 갖고 있다는 사실로 Divider 판단 프레임을 좁힘. (4) 01_구현요구사항서에 "로그인 상태 유지" 대응 FR이 안 보임을 발견해 ux-designer 흐름 검토 포인트로 명시(판단은 design-pl/사용자에게 넘김).
- **구체화 포인트**: design-systems 브리프 0(문서 선행확인)→1(체크박스 4 variant 등록)→2(라디오 raw shape 스캔)→3(Divider 필요성 판단)→4(CornerInput 라벨 정정)→5(자체 재대조)→6(design-system.md append). ux-designer 브리프 1(체크박스 흐름 논리 사실확인)→2(8개 프레임 재스캔, 중복보고 금지). 공통: 확정 8개 프레임 읽기 전용.

### 2026-07-15 — design-systems 브리프 4건(순차 호출용, 1콜 1브리프): TypeSelector 스펙시트 clipsContent 버그 + Focus 링 "흰 갭+잉크 링" 2겹 공식 도입(버튼류→입력/선택/체크박스 확장) + Error 예외 + 문서 텍스트 전수 점검
- **원본 요청**: 사용자가 design-pl 경유 승인. 배경은 사용자 스크린샷으로 확인된 결함 — 기존 9-1절 Focus 공식(단일 ink `DROP_SHADOW` spread3)이 검은/진한 보더를 쓰는 컴포넌트에서 보더와 같은 색이라 링이 사실상 안 보임. 이전 세션에 한 호출에 여러 작업을 몰아넣어 API 출력 토큰 초과로 응답이 여러 차례 끊긴 전례가 있어, design-pl이 4개 브리프를 순차 호출용으로 분리 요청.
- **핵심 판단**: (1) design-system.md를 전체 Read해 최신 상태를 직접 대조 — TypeSelector 스펙 시트 노드 ID가 문서상 `343:1146`으로 기록돼 있는데 인계 정보는 `450:714`를 지목해 불일치 발견, "get_metadata로 재확인하라"는 검증 절차 명시. (2) 9-1절 문구 자체를 새 공식으로 교체하도록 명시(공통 참조점이라 안 바꾸면 이후 컴포넌트가 옛 공식을 계속 베낌). (3) TypeSelector 미선택 보더가 실측상 `#CCCCCC`(연회색)인데 인계 지시문은 "검은 보더 케이스"로 서술 — 실제 라이브 색 재확인 없이 지시문 표현만 믿지 않도록 검증 각주 추가. (4) Sidebar Nav Item `287:17`은 특수 구현(3px ink OUTSIDE 스트로크)이라 기계적 적용 시 회귀 위험 — 판단은 위임, 근거 기록 필수화.
- **구체화 포인트**: 브리프 A(TypeSelector 스펙시트 clipsContent), 브리프 B(Focus 2겹 공식 — 버튼류, 색 배경+검은 보더 양쪽 스크린샷 검증 필수), 브리프 C(2겹 공식 확장 — 입력/선택/체크박스 + Error 예외, Sidebar Nav Item 예외 처리 판단 위임), 브리프 D(문서/description 전수 점검, 표/불릿 형식 강제). 공통: 4개 전부에 "Write 전체 덮어쓰기 금지"+"확정 8개 프레임 읽기 전용" 재확인 문구, 순차 호출 전제로 각각 자기완결적 작성.

### 2026-07-15 — design-systems 브리프 2단계(Stage 1/Stage 2): 확정 디자인 소스 교체(`248:11689`→`501:2505`) 반영 — 토큰 diff 실측 및 갱신(Stage 1) → 컴포넌트 리바인딩 + design-system.md 문서화(Stage 2)
- **원본 요청**: 메인 세션이 사용자 요청("컬러랑 이런거 조금 바꿨어. 최종아니니깐 확인해서 토큰과 스타일등 컴포넌트에 반영해줘")에 따라 Figma를 직접 확인. 기존에 `docs/design/design-system.md`가 참조하던 확정 디자인 섹션(구 `248:11689`)은 사용자가 이미 "❌ 미채택 — 확정 디자인 탈락"으로 개명해뒀고, 새 확정 디자인 섹션은 `501:2505`(파일럿 페이지 `222:524` 하위)에 동일 8개 프레임 구성이 새 nodeId로 존재. 스크린샷상 파란 배경(로그인/가입류)·짙은 네이비 사이드바 등 색 팔레트가 상당폭 바뀐 것으로 보이나 정확한 값은 미실측. "최종은 아니다"라고 밝혔지만 지금 시점 값을 그대로 반영해달라는 명시적 요청이라 승인 대기 없이 진행. 과거 출력 토큰 한도 초과로 design-systems 호출이 중단된 전례가 있어 브리프를 2단계로 분리 요청받음.
- **핵심 판단**: (1) 이건 AI 파일럿 피드백 라운드가 아니라 "이미 한 번 확정→추출을 거친 소스가 교체"된 경우라는 점을 서두에 명시해, design-systems가 2-4번을 처음부터 다시 도는 것으로 오인해 전면 재작업하지 않도록 범위를 "diff 갱신"으로 좁힘. (2) 기존 확정 스펙 문서(`docs/design/confirmed/user-confirmed-final-design.md`)는 구 `248:11689` 기준으로 쓰여 nodeId가 전부 낡았지만, spacing/radius/그림자/장식 모티프 같은 구조적 관례는 여전히 참고 기준선으로 유효할 가능성이 높아 "그대로 믿지 말고 새 8개 프레임과 항목별로 재대조"하는 조건부 참고 자료로 브리프에 포함시킴(색상만 바뀌었다고 예단하지 않게). (3) 사이드바 다크 네이비를 "같은 컴포넌트 색상 변경"과 "새 시각 패턴"으로 나눠 실제 자식 구조를 보고 판단하게 하고, 신규 컴포넌트 생성은 Stage 2로 미뤄 Stage 1은 판단·기록까지만 하도록 경계를 그음(임의 신규 컴포넌트 남발 방지). (4) Stage 1 산출물(diff 표)을 design-pl이 Stage 2 호출 시 그대로 붙여넣어 전달하는 구조로 설계해, Stage 2가 처음부터 재실측하지 않고 이어받게 함 — 단 실제 반영 시 각 값은 원본과 재대조하도록 자체 검증은 유지.
- **구체화 포인트**:
  1. Stage 1: 재개 확인(0단계) → 8개 신규 프레임(`501:6008`/`501:3042`/`501:3636`/`501:4218`/`501:4692`/`501:4940`/`501:5188`/`501:6548`) 읽기 전용 실측 → design-system.md 1~7절 baseline 파악 → confirmed 문서 조건부 참고 → diff 식별(색상 전면 대체 여부, 나머지 팔레트 불변 여부, spacing/radius/그림자도 점검) → 사이드바 (a)색상변경/(b)신규패턴 판단(근거만, 생성은 안 함) → Primitive→Semantic 토큰 갱신/추가(3계층 유지) → diff 요약표 산출로 종료(문서 미수정).
  2. Stage 2: Stage 1 diff 표 인계 전제 → 전 관련 컴포넌트 토큰 바인딩 리바인딩(raw hex 금지, 과거 Button Focus/NeoSelect hover 하드코딩 사고 재경고) → Component Specs 스펙 시트 + Colors 스와치 카탈로그 갱신(2-5번) → design-system.md 전체 Read 후 Edit로 새 절 추가(소스 교체 사실/diff 표/갱신 토큰·컴포넌트 목록, 표·불릿 위주 간결하게) → 자체 재대조 → 화면 조립·퍼블리싱·8개 프레임 수정 금지 재확인.
  3. **후속 갭 플래그(design-pl에 별도 보고, 브리프 범위 밖)**: `docs/design/confirmed/user-confirmed-final-design.md`가 여전히 구 nodeId를 참조하는 채로 남는다 — 이 문서 갱신은 2-3번 규칙상 brand-designer/ui-designer가 새 8개 프레임을 직접 재관찰해야 하는 작업이라 이번 design-systems 브리프 범위에 포함하지 않았음을 명시적으로 알림. 색이 완전히 확정되면(사용자가 "최종"이라 확정하면) 별도 라운드로 이 문서도 신규 관찰 기반으로 갱신 필요.

### 2026-07-16 — design-systems 브리프 4건(Stage1 + Stage2-a/b/c, 순차 호출용): Disabled 상태를 opacity 기반에서 명시적 회색 토큰(bg/line/text) 기반으로 전환, Button/Table Row Action/Checkbox 통일 + Input(NeoInput/CornerInput) 텍스트 예외
- **원본 요청**: fileKey `zgGlMBwFglaDlaeyP4CkgR`. 사용자가 "Disabled은 회색톤으로 만드는게 정석"이라며, 현재 opacity 기반(9-1/9-5절: 컨테이너 opacity=1, 배경/보더 opacity=0.5, 텍스트 opacity=0.85) Disabled를 명시적 색 토큰 3종(bg/line/text)으로 바꾸고 NeoBtn/Button/Table Row Action/Checkbox 4개 컴포넌트에 통일 적용하되, NeoInput/CornerInput은 "값을 보여주는 display" 역할이라 bg/line은 같은 토큰을 쓰되 텍스트만 Default(진한 ink/900) 유지하라고 확정 요청. 과거 유사 규모 라운드가 토큰 한도 초과로 3회 중단된 전례가 있어 Stage1(설계, 미적용)/Stage2(적용, a/b/c 세분)로 분리.
- **핵심 판단**: (1) design-system.md 9-1/9-4/9-5/0-17절을 직접 재확인해, 인계된 Checkbox 노드 ID("474:881이 ComponentSet")가 실제로는 페이지 ID이고 실제 ComponentSet은 `474:899`, State=Disabled variant가 `474:893`임을 발견 — Stage1 브리프에 "인계 ID를 그대로 믿지 말고 get_metadata로 재확인하라"는 검증 각주를 명시적으로 추가해 오귀속 방지. (2) 이 프로젝트가 지금까지 항상 "새 축을 만들지 않고 기존 State 열거형에 값 하나를 추가"해온 관례(0-15/0-16절)를 Stage1의 NeoInput/CornerInput Disabled 추가 방식 판단에 참고 근거로 명시. (3) FOUNDATIONS Colors 스와치는 0-21절에서 막 확립된 "별도 신규 섹션 없이 기존 Primitives/Semantic 그리드에 바로 편입" 관례를 그대로 따르도록 Stage2-c에 명시. (4) Table Row Action은 7-1절 §3(텍스트 대비 미달, 개선하지 않기로 기존 결정)이 있어 이번 토큰 교체로 값이 달라져도 일부러 다른 토큰을 쓰지 않고 통일 원칙을 그대로 지키게 각주 추가.
- **구체화 포인트**:
  1. Stage1(읽기전용): 기존 gray primitive 전수 조사(이미 알려진 gray/0,50,100,150,200,300,350,400,450,500,600,650 + ink/800,900 목록을 참고치로 제공하되 실측 재확인 지시) → 신규 semantic 3종 이름/hex 설계 → WCAG 계산(bg/border는 흰 배경 3:1, text-disabled는 실측 기록) → 5개 컴포넌트 정확한 대상 노드 ID 목록 + NeoInput/CornerInput 추가 방식(State 열거형 값 vs 별도 축) 결정, 근거 포함. Figma/문서 미수정.
  2. Stage2-a(NeoBtn/Button/Table Row Action/Checkbox): Stage1 산출물 인계 전제, 토큰 3종 Variable 생성 → 4개 컴포넌트 Disabled variant 전체 리바인딩(raw hex 금지, opacity 오버라이드 제거해 1로 복귀) → 자체 재대조.
  3. Stage2-b(NeoInput/CornerInput): Content=Filled 기반 clone으로 신규 Disabled variant 추가, bg/line만 토큰화하고 텍스트는 `color/ink/900` 그대로 — "text-disabled 토큰을 여기 바인딩하지 마라"를 가장 중요한 주의사항으로 강조.
  4. Stage2-c(문서화): NeoInput/CornerInput 스펙 시트 7칸으로 확장, Checkbox/버튼류는 인스턴스 기반이라 자동 반영 확인만, FOUNDATIONS Colors 기존 그리드 편입, design-system.md는 반드시 전체 Read 후 Edit(Write 금지) — 9-1절 문구 교체, 9-4/7-2절 해당 항목 RESOLVED 갱신, 새 절 표/불릿 요약, 5절 표 variant 개수 갱신.
  5. 4개 브리프 전부에 "이미 진행된 부분 있으면 이어받기" 재개 지침과 "확정 8개 프레임(`501:2505` 하위) 무관, 건드리지 않는다" 공통 제약 포함.

### 2026-07-16 — design-systems 브리프 6건(Stage A/B-1/B-2/C-1/C-2/D, 순차 호출용): 34차 전수 대조 불일치 15건(P1~P15) recon 재확인 후 design-pl 최종 판단을 실행 브리프로 구조화
- **원본 요청**: fileKey `zgGlMBwFglaDlaeyP4CkgR`. design-pl이 확정 8개 프레임(부모 섹션 `501:2505`) 전수 대조에서 나온 불일치 15개(P1~P15)를 Figma 라이브 재조사(읽기 전용)로 재검증한 뒤 항목별 최종 판단(REFLECT 11건/조치없음 4건 — 문서만 3건 P9·P13·P15, 재검증만 P3)을 완료해 전달. 이전 유사 규모 라운드가 stall(600초 무응답)로 중단된 전례가 있어 Stage를 잘게 쪼개달라는 명시적 요청.
- **핵심 판단**: (1) 라우팅·판단 자체(반영 여부)는 이미 design-pl이 확정했으므로 재론하지 않고 "무엇을 어떻게" 실행 브리프만 작성 — 역할 경계 준수. (2) P1(Style=Sky/Navy 신규)은 대상이 NeoBtn(Size 축 있음)인지 Button(Size 축 없음)인지 인계 정보에 없어, "프레임 버튼 치수 재실측→기존 두 ComponentSet 치수와 비교해 판단"이라는 자체 판별 절차를 브리프에 명시(임의 추측 금지). (3) P11(Amber 전체 State ink 보더)은 9-1절 Focus 순수성 원칙(Focus=Default clone)과 충돌 소지가 있어 "보더를 추가한 새 Default 기준으로 Focus도 그 보더를 유지한 채 링만 추가돼야 한다"고 명시적으로 못박음 — 안 그러면 Focus에서 보더가 빠지는 회귀 위험. (4) P14는 Toast 마스터가 title 단일 role인지 title+subtitle 2-role인지 미확인 상태라 Case A(재바인딩)/Case B(보류+플래그) 분기를 브리프에 직접 설계, 구조 확장을 단일 프레임 근거로 임의로 하지 않도록 방지. (5) Stage D에 design-system.md 새 절뿐 아니라 `.claude/agents/design-systems.md`의 "Text property 인스턴스 교체" 원칙과 같은 계열로 "색상만 다른 반복 사용은 컴포넌트 확장 대신 인스턴스 오버라이드" 일반 원칙을 명문화하도록 포함 — P12 관찰(로그인 흰 체크마크 오버라이드)을 근거로 재확인된 패턴.
- **구체화 포인트**:
  1. Stage A(작게): P12(Checkbox `474:888` stroke를 `color/border-divider-cool`→`color/ink/900` 재바인딩, login 인스턴스 `604:5953`/`604:5954`는 확정 프레임 내부라 관찰만) + P14(Toast `263:53` 구조 재확인 후 Case A/B 분기).
  2. Stage B-1: P4(NeoInput/NeoSelect `Shadow/Hard-2`, Focus·Disabled 제외) + P5(CountPill Active `Shadow/Hard-1`) + P6(Icon Button radius→0) + P7(Card radius→0) — 전부 기존 토큰/이펙트 재사용.
  3. Stage B-2: P8(Card AccentStrip-Top 하단 보더, 두께는 hairline/base/heavy 중 재실측 매칭) + P10(CornerInput placeholder 14→13, Filled도 13이면 함께) + P3(재검증만, stroke weight만 필요시 미세조정).
  4. Stage C-1: P2(RowActionButton Danger Default만 — 배경 `color/bg-cta-amber`, 보더 `color/ink/900`, 두께는 Neutral 보더 토큰과 비교 판단, 5개 프레임 전수 재대조).
  5. Stage C-2(최대 규모, 필요시 추가 분할 허용 명시): P1(대상 ComponentSet 실측 판별 후 Style=Sky/Navy Default만 추가, 나머지 State는 7-2절 TODO로 이관) + P11(Amber 전체 State에 2px ink 보더, Focus 원칙 충돌 방지 각주).
  6. Stage D: design-system.md 전체 Read 후 Edit로 P1~P15 전체 표(조치없음 항목도 전부 포함) + 신규 토큰 기존 그리드 편입(별도 신규 섹션 금지, 반복 재발 경고) + 관련 스펙 시트 갱신 + `.claude/agents/design-systems.md`에 인스턴스 오버라이드 일반 원칙 추가.
  7. 6개 브리프 전부 서두에 "재개 확인"(이미 반영됐으면 건너뛰기), 말미에 "자체 재대조"(boundVariable/hex 재조회), 확정 8개 프레임 절대 무수정 재확인 공통 포함.
