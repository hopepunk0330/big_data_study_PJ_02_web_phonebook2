# design-prompter 메모리

이 파일은 design-prompter가 작업 시작 시 읽고, 작업 종료 시 기록을 남기는 메모리/로그입니다.

## 작업 로그

### 2026-07-13 — ui-designer 브리프: 사용자 지적 시각 결함 4건 — 파일럿 3화면 "인스턴스 한정" 수정 (마스터 컴포넌트 미변경, design-systems/design-qa 이번 범위 아님)
- **원본 요청**: 사용자가 파일럿 3화면 스크린샷을 직접 보고 결함 4가지 지적. 파일럿이 아직 최종 확정 전이라 design-pl이 "이번엔 컴포넌트(design-systems)를 건드리지 않고 3개 화면 인스턴스만 수정"으로 범위를 명시적으로 좁힘 — 이전 라운드들과 반대 방향(보통 컴포넌트 먼저 고치고 화면 재바인딩)이라는 점이 이번 브리프의 핵심 특이점.
- **핵심 판단(문서 원문 재확인으로 수정된 부분)**: 결함3 "새 카테고리 추가 버튼 색"에서 원 요청은 "amber 복원"이라고 썼지만, confirmed `b-2-contacts-layout.md` 6절 원문을 직접 재확인한 결과 **사이드바 카테고리 추가 버튼은 amber가 아니라 teal(Primary)**이 정답(본문 "추가" 행 버튼이 amber). 기억/추정이 아니라 문서 원문을 근거로 이 착오를 브리프에서 정정해 전달 — "모호함은 여기서 끝낸다" 원칙 적용 사례.
- **구체화 포인트**:
  1. 결함1(Row Action Button 아이콘 전용→텍스트 복원): Table Row 6곳(참고치, get_metadata 재확인 우선)+사이드바 카테고리 관리 행 전부(정확한 개수 실측) 대상. 마스터 컴포넌트(`166:421`) 미변경 원칙상 인스턴스 detach 후 로컬로 아이콘 제거→순수 텍스트 라벨("수정"/"삭제")로 재구성 지시. 색상은 원 요청 표현("초록/빨강 계열")을 기존 시맨틱(수정=`color/text-accent` #0F7A6E AA 근거, 삭제=coral/500 대비 직접 실측 후 미달이면 로컬 한정 톤다운, 새 전역 토큰 생성 금지)에 매핑해 구체화. 보더/radius/padding은 confirmed 2·3·4절의 기존 소형 버튼 값(1px, radius/xs 5px, 8×4px) 재사용해 이질감 방지, 44×44 히트 규칙은 아이콘 전용 컴포넌트 전제였음을 지적해 텍스트 버전엔 그대로 적용 안 됨을 명시.
  2. 결함2(검색 아이콘 비율): Input의 `Show Search Icon`/`Icon` 프로퍼티로 들어간 Icon/Search 인스턴스 크기를 16×16 또는 20×20 정사각형 고정, constraints/오토레이아웃 sizing mode 점검까지 포함.
  3. 결함3(카테고리 추가 버튼 색): teal(Primary) 배경 복원이 정답임을 명시하고, 원인 진단(오버라이드로 fill 삭제됐는지 vs variant 오선택)을 먼저 하도록 지시.
  4. 결함4(Select caret 이질감): 새 아이콘 창작 금지, 기존 caret 벡터의 스트로크만 3px+ink 컬러로 로컬 조정하는 수준으로 범위 한정. Icons 페이지(96:7)에 대체 가능한 화살표가 이미 있으면 그걸 우선 사용.
  5. 원칙 재천명: 오버라이드 우선, 구조 변경 필요시에만 해당 인스턴스 detach. 수정 후 3화면 전부 get_screenshot 재확인 없이는 완료 보고 금지. 보고 형식은 4가지 각각 "무엇을 어떻게 고쳤는지+스크린샷 재확인 결과"로 명시. design-systems/design-qa는 이번 범위 아님을 브리프에 명시(혼동 방지).

### 2026-07-13 — 확정 스펙 문서 작성: 사용자 직접 확정 디자인 8개 프레임 (`docs/design/confirmed/user-confirmed-final-design.md`)
- **원본 요청**: 사용자가 Figma에 직접 만든 8개 프레임(main/main-수정/main-삭제/main-검색없음/Join/login/login-알림창/main-알림창, fileKey `zgGlMBwFglaDlaeyP4CkgR`)을 "확정 디자인 - 절대 원본 건들지 말것-"으로 최종 확정 — 기존 AI 파일럿(B-2 기반)을 대체. brand-designer가 이 8개를 직접 관찰해 `docs/design/brand-guide.md`를 전면 갱신한 뒤, design-prompter가 2-3번 규칙에 따라 확정 스펙 문서를 작성하는 작업(브리프 발전이 아니라 문서화 작업).
- **작업 성격**: 통상적인 워커 브리프가 아니라 2-3번 "확정 스펙 문서" 작성 — 여러 전문 분야(design-systems/ui-designer/graphic-designer/interaction-designer/ux-designer)가 동시에 참고하는 공유 악보이므로 brand-guide.md보다 더 촘촘하게, 8개 화면 각각에 스펙이 어떻게 적용되는지까지 재구조화.
- **핵심 판단**: (1) brand-guide.md는 토큰 카테고리별(컬러/타이포/보더 등)로 정리되어 있어 "화면별로 어떻게 조합되는지"가 빠져 있음 — 이 문서의 핵심 부가가치는 8개 화면 각각의 레이아웃 구조·상태를 새로 서술한 10절("화면별 상세 스펙")임. (2) brand-guide.md가 명시한 두 가지 "그대로 전달해야 할 참고사항"을 임의로 축약하지 않고 원문 취지 그대로 문서에 포함: ① 편집 모달 TypeSelector 칩 색상이 CatBadge 팔레트와 다른 불일치(design-systems가 정리 판단할 대상, design-prompter가 임의로 통일하지 않음) ② 하드 스티커 그림자와 소프트 블러 그림자를 별개 effect 토큰으로 분리해야 한다는 점. (3) 이전 b-2-contacts-layout.md와 이번 문서가 로고 워드마크 웨이트(Bold vs ExtraBold)·색상(고정 딥틸 vs 배경별 반전)에서 서로 다른 값을 기록하고 있음을 명시적으로 대조해 혼동 방지 — 최신 소스(이번 문서)가 우선임을 표기.
- **문서 구성**: 체크리스트(로고/퍼스낼리티/구조 3대 영역) → 확정 배경 → 8개 프레임 표 → 브랜드 트라이앵글 → 컬러(팔레트/CatBadge/불일치/상태색) → 타이포 → 보더·radius → 그림자(하드/소프트 분리) → 간격 리듬 → 로고 정확 스펙(배경별 반전 규칙 포함) → 장식 모티프(화면별 위치) → 아이콘 계층 → 화면별 상세 스펙 8절 → 알림 오버레이 의도 명시 → 텍스트 대비 → 워드마크 네이밍 미확정 → design-systems/ui-designer 안내(4개 핵심 포인트).
- **건드리지 않음**: 기존 `docs/design/confirmed/b-2-contacts-layout.md`는 삭제/수정하지 않고 그대로 유지(새 파일로 이번 라운드만 별도 기록).

### 2026-07-14 — design-systems 브리프: Link 텍스트 스타일/컴포넌트 신규 등록 + 스펙 시트 10개 신설(FOUNDATIONS "Component Specs" 신규 페이지)
- **원본 요청**: design-pl이 승인받은 두 작업을 하나의 브리프로 묶어 전달. (A) login 확정 프레임(`247:6666`)에 실제 쓰인 "로그인으로 돌아가기"류 보조 텍스트 링크가 정식 토큰/컴포넌트로 전혀 등록돼 있지 않음(design-system.md/brand-guide.md grep 0건 확인됨) — 신규 등록 필요. (B) 이미 State/Focus 축이 있는 기존 컴포넌트 9개(버튼류 5+Focus-only 4)에 `figma-file-organization.md` 2-5절 "스펙 시트 의무화" 규칙이 아직 적용 안 됨 — 신설 필요. SCR-004(비밀번호 찾기) 화면 조립은 이번 범위 아님(디자인 시스템 완성 후 별도 승인).
- **핵심 판단**: (1) 작업 A의 근거 인용문("적용 안 됨: 보조/중립 버튼... '로그인으로 돌아가기'", "로그인 CTA + '로그인으로 돌아가기'류 보조 텍스트 링크")이 brand-guide.md가 아니라 `docs/design/confirmed/user-confirmed-final-design.md` 5-1절/10-6절 원문과 정확히 일치함을 대조 확인 — 브리프에 인용 출처를 확정 스펙 문서로 명시(brand-guide.md 절 번호와 혼동 방지). (2) 색상 우선순위는 기존 뮤트 텍스트 토큰(`color/text-muted-strong` #555 / `text-muted` #777 / `text-muted-subtle` #888) 재사용을 먼저 검토하도록 지시 — 새 primitive는 실측값이 기존 어느 토큰과도 안 맞을 때만. (3) State 축은 최소 Default만 등록하고 hover/press 등 신규 인터랙션 설계는 이번 범위가 아님을 명시(실측 근거 없는 상태 추가 금지, 필요시 7절 알려진 갭에 후속 과제로 기록). (4) "Component Specs" 신규 페이지 삽입 위치는 design-system.md 8절 페이지 순서 표 기준 Icons(`96:7`) 바로 뒤 · "브랜드 컨셉 Concepts"(`34:2`) 바로 앞으로 명시. (5) 참고 레이아웃(`95-162` "Spacing & Radius" 프레임)은 레이아웃/타이포/정렬 스타일만 차용하고 그 안의 값 자체(폐기됨)는 참고 금지임을 강조.
- **구체화 포인트**:
  1. 작업 A: login 프레임 읽기 전용 재관찰 → 실측(hex/폰트/크기/밑줄/그림자없음) → `Body/Link` 텍스트 스타일(+필요시 얇은 Link 컴포넌트) 신규 등록 → design-system.md 1-5절(Text Styles)·5절(컴포넌트 표, 텍스트 스타일이면 해당 없음)에 반영, 어느 화면(login 현재 + SCR-004 예정)에 쓰이는지 명시.
  2. 작업 B: 대상 9개 컴포넌트(NeoBtn `259:126`/Button `259:609`/Icon Button `259:613`/Row Action Button `260:95`/Table Row Action `260:100`/Sidebar Nav Item `258:29`/TypeSelector `257:28`/NeoInput `288:12`/CornerInput `288:27`) 전체 variant 개수·축(design-system.md 5절/9-2절/9-3절 표 그대로) 브리프에 명시, 그리드 구성 예시(Style×Size를 행, State를 열 등) 제시하되 최종 배치는 design-systems 재량. 설명 문구는 design-system.md 5절 표의 기존 설명을 그대로 재사용하도록 각 컴포넌트별로 인용문 제공.
  3. 완료 기준: 변수/컴포넌트 등록뿐 아니라 스펙 시트 10개(9개+Link) 전부 "클릭 없이 전체 variant가 보이는 그리드+상태 라벨" 요건을 충족해야 완료로 인정. design-system.md 8절 페이지 순서 표 갱신 + 5절 컴포넌트 표에 스펙 시트 보유 각주 추가까지 문서화 지시.
  4. 원칙 재확인: 원본 확정 8개 프레임(`248:11689` 하위)은 읽기 전용, 기존 토큰/컴포넌트 값은 변경 금지(순수 추가), 이번 라운드는 신규 Link + 신규 "Component Specs" 문서화 페이지로 한정.

### 2026-07-14 — design-systems 브리프: Table Row 실제 컴포넌트 조립+스펙시트 / Legacy Table Row(103:7) 해제 위험성 검증 / Link 44×44·WCAG 3.12:1 문서 상태 정리 (4작업 묶음)
- **원본 요청**: design-pl이 사용자 승인 받은 4개 작업을 하나의 브리프로 묶어 전달. (1) Table Row 페이지(`103:3`)에 "Documentation" 스펙(103:4~103:6)만 있고 실제 조립 컴포넌트가 없는 갭을 메우기(레거시 `103:7`은 정식 소스 아님). (2) 이전 라운드 보류됐던 Legacy Table Row(`103:7`) 컴포넌트 해제를 "인스턴스 있다고 막을 근거 약함" 재검토 요청 — 위험성을 실측 검증 후 처리. (3) design-qa가 지적한 Link(`341:3`, 69×18) 44×44 터치 타겟 미달 미문서화 보완. (4) 사용자가 신뢰 형식으로 확정한 "Link WCAG 대비 3.12:1 무시하고 진행" 결정을 문서 상태(TODO→RESOLVED)에 반영.
- **핵심 판단(모호함 정정, 문서 원문 대조)**: 원 요청 텍스트가 "이름/전화번호/주소 텍스트(Body/Regular)"라고 셋을 동일 스타일로 뭉뚱그렸으나, confirmed `user-confirmed-final-design.md` 3절 타이포 표 원문을 대조한 결과 **행 1차 텍스트(이름)는 Noto Sans KR Bold**(design-system.md에 이미 등록된 `Body/Button`, Bold 14px 재사용 대상)이고 `Body/Regular`(Noto Sans KR Regular)는 "2·3차 행 텍스트"인 전화번호·주소 전용이다. 이 착오를 정정해 브리프에 반영하고, 최종 확인은 design-systems가 `214:349` 재관찰로 하도록 지시(design-prompter가 확정하지 않음).
- **추가 발견(갭)**: 테이블 행 구분선 `#ede6d8` 1px가 confirmed 2-1절 표에 관찰값으로는 있으나 design-system.md 1절 Primitives에 아직 토큰으로 등록돼 있지 않음 — 새 발명이 아니라 확정 소스에서 뒤늦게 추출하는 정당한 작업임을 명시하고, 이번 조립 작업의 일부로 신규 primitive 등록을 지시.
- **작업2 검증 절차 설계**: 실제 인스턴스 7개(`103:77` + 파일럿 페이지 `❌ 폐기 —` 프레임 내부 6개)에는 위험한 실험을 하지 않는다는 원칙 하에, "마스터 컴포넌트를 격리된 위치에 복제 → 그 복제본의 테스트 인스턴스 1개 생성 → 복제 마스터만 COMPONENT_SET→FRAME 전환 → 테스트 인스턴스가 깨지는지 스크린샷 대조 → 결과와 무관하게 테스트 산출물(복제 마스터+테스트 인스턴스) 전부 삭제" 5단계 절차를 구체적으로 설계해 브리프에 명시. 안전 확인 시에만 실제 `103:7` 전환 실행, 위험 확인 시 7-2절 기록+design-pl 경유 사용자 판단 위임까지만.
- **작업3/4 구분**: 3은 7-2절에 신규 한 줄만 추가(터치타겟, 원본 내재값이라 신규 결함 아님을 명시), 4는 이미 7-2절에 있는 문구를 7-1절로 "이동"(기존 5건과 동일한 문장 패턴 "N. **[항목명]** — [수치]. **사용자 확인 후 이번 프로젝트 범위에서 개선하지 않기로 결정 — 원본 확정 프레임 값 그대로 최종.**" 재사용)만 하는 순수 문서 상태 변경임을 명확히 분리해 지시 — 색상 값 자체는 절대 변경 금지 재확인.

### 2026-07-14 — design-systems 브리프: Legacy Table Row(103:7) 해제 순서 실수로 깨진 인스턴스 7개 복구 (undo 우선 시도 → 마스터 콘텐츠 기반 재구성)
- **원본 요청**: 직전 라운드(0-6절)에서 사용자가 인스턴스 깨짐을 알고 재승인해 `103:7`을 COMPONENT_SET→FRAME 전환(해제)했으나, **Detach Instance를 먼저 하지 않고 컴포넌트 정의를 먼저 지운 순서 오류**로 참조 중이던 인스턴스 7개(`103:77` 1개 완전 빈 박스 + 파일럿 `❌ 폐기 —` 프레임(`110:201`) 안 데이터 행 6개 `110:220~290`)가 전부 `childrenCount:0`으로 깨짐. 사용자가 원인(순서 역전)을 정확히 지적하며 복구를 지시. 마스터 콘텐츠 자체는 신규 FRAME `357:303`에 무손실 보존됨.
- **핵심 판단**: (1) design-system.md 0-5/0-6절에 이미 이 사고의 전체 경위(격리 테스트로 위험성 사전 실증→사용자 재승인→실제 전환→예상대로 깨짐)가 상세히 기록돼 있어, 브리프에서 이 배경을 새로 설명하지 않고 해당 절 인용으로 대체 — design-systems가 직접 그 절을 다시 읽고 시작하도록 지시. (2) 1단계로 Figma Plugin API에 스크립트 호출 가능한 undo 함수가 실제로 존재하는지(일반적으로 없다는 통념을 그대로 믿지 말고) `figma-use` 스킬/API 문서를 먼저 확인하도록 순서를 명시 — 확인 없이 "안 될 것"이라 단정하고 바로 2단계(재구성)로 건너뛰지 않게 못박음. (3) 103:7은 이미 목표 상태(해제 완료)이므로 다시 컴포넌트로 되돌리는 게 아니라는 점을 명확히 구분(사용자 지시 오독 방지 — "복구"가 "컴포넌트 롤백"으로 오해될 소지 차단).
- **구체화 포인트**:
  1. 재구성 절차를 노드별 5단계(위치/크기 기록 → 357:303 clone 배치 → 데이터 정확성 처리 → 스크린샷 개별 검증 → 부모 프레임 전체 재검증)로 구체화, 깨진 빈 노드 제거 vs 클론을 자식으로 삽입 중 "더 안전한 쪽을 판단해서 진행"하도록 최종 구조 선택은 design-systems 재량으로 위임.
  2. **데이터 정확성 문제를 두 그룹으로 명확히 분리**: `103:77`(컴포넌트 페이지 참고용 인스턴스)은 마스터 예시 데이터("윤아" 등) 그대로 복제해도 무방 — 실 서비스 데이터가 아니므로 무해. 반면 `110:220~290`(파일럿 폐기 프레임 안 6개 데이터 행)은 서로 다른 연락처를 표시했을 가능성이 있어, 복원 전 먼저 형제 노드/프레임 이름/description에 원본 데이터 단서가 남아있는지 확인하도록 지시하고, 단서가 없으면 "완벽 복원 불가"를 숨기지 말고 명시하며 최소한 시각적으로 깨지지 않은 상태(마스터 예시 또는 카테고리 순환 정도의 최소 다양성)까지만 복원하도록 한계를 인정하는 기준을 명시.
  3. 문서화 지시를 "은폐 금지" 원칙으로 강하게 명시 — 실수 발생 경위, undo 시도 결과, 재구성 방식, 최종 결과(완전 복원 vs 부분 복원)를 있는 그대로 design-system.md에 남기고, 향후 동일 실수 재발 방지를 위해 "컴포넌트 해제 전 반드시 Detach Instance 선행" 절차를 8절 또는 적절한 절에 표준 절차로 명문화하도록 지시.
  4. 공통 원칙(원본 확정 8개 프레임/정식 Contact Row 컴포넌트 불가침) 재확인, 최소 기준("빈 박스" 상태는 반드시 해소)을 명확한 완료 조건으로 제시.
</content>
