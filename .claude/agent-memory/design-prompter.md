# design-prompter 메모리

이 파일은 design-prompter가 작업 시작 시 읽고, 작업 종료 시 기록을 남기는 메모리/로그입니다.

## 작업 로그

### 2026-07-14 — design-systems 브리프: Legacy Table Row(103:7) 해제 순서 실수로 깨진 인스턴스 7개 복구 (undo 우선 시도 → 마스터 콘텐츠 기반 재구성)
- **원본 요청**: 직전 라운드(0-6절)에서 사용자가 인스턴스 깨짐을 알고 재승인해 `103:7`을 COMPONENT_SET→FRAME 전환(해제)했으나, **Detach Instance를 먼저 하지 않고 컴포넌트 정의를 먼저 지운 순서 오류**로 참조 중이던 인스턴스 7개(`103:77` 1개 완전 빈 박스 + 파일럿 `❌ 폐기 —` 프레임(`110:201`) 안 데이터 행 6개 `110:220~290`)가 전부 `childrenCount:0`으로 깨짐. 사용자가 원인(순서 역전)을 정확히 지적하며 복구를 지시. 마스터 콘텐츠 자체는 신규 FRAME `357:303`에 무손실 보존됨.
- **핵심 판단**: (1) design-system.md 0-5/0-6절에 이미 이 사고의 전체 경위(격리 테스트로 위험성 사전 실증→사용자 재승인→실제 전환→예상대로 깨짐)가 상세히 기록돼 있어, 브리프에서 이 배경을 새로 설명하지 않고 해당 절 인용으로 대체 — design-systems가 직접 그 절을 다시 읽고 시작하도록 지시. (2) 1단계로 Figma Plugin API에 스크립트 호출 가능한 undo 함수가 실제로 존재하는지(일반적으로 없다는 통념을 그대로 믿지 말고) `figma-use` 스킬/API 문서를 먼저 확인하도록 순서를 명시 — 확인 없이 "안 될 것"이라 단정하고 바로 2단계(재구성)로 건너뛰지 않게 못박음. (3) 103:7은 이미 목표 상태(해제 완료)이므로 다시 컴포넌트로 되돌리는 게 아니라는 점을 명확히 구분(사용자 지시 오독 방지 — "복구"가 "컴포넌트 롤백"으로 오해될 소지 차단).
- **구체화 포인트**:
  1. 재구성 절차를 노드별 5단계(위치/크기 기록 → 357:303 clone 배치 → 데이터 정확성 처리 → 스크린샷 개별 검증 → 부모 프레임 전체 재검증)로 구체화, 깨진 빈 노드 제거 vs 클론을 자식으로 삽입 중 "더 안전한 쪽을 판단해서 진행"하도록 최종 구조 선택은 design-systems 재량으로 위임.
  2. **데이터 정확성 문제를 두 그룹으로 명확히 분리**: `103:77`(컴포넌트 페이지 참고용 인스턴스)은 마스터 예시 데이터("윤아" 등) 그대로 복제해도 무방 — 실 서비스 데이터가 아니므로 무해. 반면 `110:220~290`(파일럿 폐기 프레임 안 6개 데이터 행)은 서로 다른 연락처를 표시했을 가능성이 있어, 복원 전 먼저 형제 노드/프레임 이름/description에 원본 데이터 단서가 남아있는지 확인하도록 지시하고, 단서가 없으면 "완벽 복원 불가"를 숨기지 말고 명시하며 최소한 시각적으로 깨지지 않은 상태(마스터 예시 또는 카테고리 순환 정도의 최소 다양성)까지만 복원하도록 한계를 인정하는 기준을 명시.
  3. 문서화 지시를 "은폐 금지" 원칙으로 강하게 명시 — 실수 발생 경위, undo 시도 결과, 재구성 방식, 최종 결과(완전 복원 vs 부분 복원)를 있는 그대로 design-system.md에 남기고, 향후 동일 실수 재발 방지를 위해 "컴포넌트 해제 전 반드시 Detach Instance 선행" 절차를 8절 또는 적절한 절에 표준 절차로 명문화하도록 지시.
  4. 공통 원칙(원본 확정 8개 프레임/정식 Contact Row 컴포넌트 불가침) 재확인, 최소 기준("빈 박스" 상태는 반드시 해소)을 명확한 완료 조건으로 제시.

### 2026-07-14 — design-systems 브리프: TypeSelector 색상을 CatBadge 기준으로 통일 (사용자가 "의도적으로 다른 팔레트" 판단을 뒤집음, 인스턴스 연결 리스크 선확인 필수)
- **원본 요청**: fileKey `zgGlMBwFglaDlaeyP4CkgR`. CatBadge(`256:17`, main 테이블 행 배지)와 TypeSelector(`257:28`, main-수정 `248:8103` 편집 모달 '종류' 칩)는 이전 라운드까지 "의도적으로 다른 팔레트"(TypeSelector는 원본 확정 프레임 재현용 legacy)로 description에 명시돼 있었음. 사용자가 이 판단을 뒤집어 "TypeSelector가 사실 CatBadge와 연관돼 있다 — CatBadge 기준으로 맞춰라, 디자인 확정에도 반영하겠다"고 지시. 이건 새 컨셉 판단이 아니라 사용자가 이미 확정한 값(CatBadge 팔레트)으로 다른 컴포넌트를 재바인딩하는 유지보수 작업 — **design-prompter는 색상 값을 새로 판단하지 않고 사용자가 준 실측표를 그대로 절차화**.
- **핵심 판단(작업 순서 설계)**: (1) 실행 순서에서 **0번(인스턴스 연결 리스크 확인)을 다른 어떤 단계보다 먼저**로 못박음 — 과거 Legacy Table Row(`103:7`) 사고(순서 역전으로 인스턴스 7개 파손, 직전 라운드에서 복구)와 같은 유형의 위험이 여기도 있는지(마스터 컴포넌트 색을 바꾸면 확정 프레임 `main-수정`이 같이 바뀌는 인스턴스 관계인지)를 먼저 읽기 전용으로 확인해야 함. 인스턴스로 연결돼 있으면 **detach 여부를 design-systems가 스스로 판단하지 않고 즉시 design-pl에게 보고 후 대기**하도록 명시 — "확정 프레임 절대 불가침" 원칙과 정면 충돌할 수 있는 상황이므로 판단 권한을 상위로 넘김. (2) 친구 카테고리는 이미 CatBadge와 일치한다는 사용자 실측 결과를 그대로 반영해 "재확인만, 변경 없음"으로 범위를 좁힘 — 4개 카테고리를 전부 똑같이 취급하지 않음. (3) "기타" 카테고리는 사용자가 명확한 답을 안 주고 "통일할지 판단 필요"로 남겨뒀으므로, design-systems가 임의 판단하지 않도록 "명확한 무채색 의도 증거가 없으면 기본적으로 CatBadge 보라 토큰으로 통일, 증거가 있으면 예외로 design-pl에게 보고"라는 판단 기준을 브리프에 명시(디자이너가 자의적으로 결정하지 않게 가드레일 제공).
- **구체화 포인트**:
  1. 0번: `main-수정`(`248:8103`) 종류 칩을 get_design_context/get_metadata로 읽기 전용 관찰 → TypeSelector(`257:28`) 인스턴스 여부 확인 → 인스턴스면 즉시 중단+보고(nodeId, 증거, 원칙 충돌 명시), 아니면 1번 진행.
  2. 1번: 새 raw hex 생성 금지, CatBadge가 이미 쓰는 기존 semantic 토큰(`color/category-{friend|family|company|other}-*` 계열, 정확한 변수명은 CatBadge 코드에서 재확인)에 재바인딩. 친구=재확인만, 가족(버그성 초록 `#27ae60`→ 핑크 `#ff5a76`/`#ffe4e8`/`#a8003b`), 회사(주황 `#e6800a`→ 틸 `#17a398`/`#d8fff5`/`#0a4f49`), 기타(무채색→ 보라 `#9b72cf`/`#ede0ff`/`#4b0d9c`, 예외 시 증거와 함께 보고). Unselected 상태·확정 프레임 8개(`248:11689` 하위)는 손대지 않음(읽기 전용).
  3. 2번: Component Specs 페이지 TypeSelector 스펙 시트(2-5번 규칙) 갱신.
  4. 3번: `docs/design/design-system.md`와 Figma 컴포넌트 description(TypeSelector `257:28`, CatBadge `256:17`) 둘 다 "의도적으로 다른 팔레트" 문구를 "2026-07-14 사용자가 판단 뒤집음 — CatBadge와 통일"로 갱신(코드만 바꾸고 문서를 옛 문구로 방치 금지).
  5. 4번: 0번에서 멈춘 경우와 끝까지 진행한 경우를 분리해 보고 형식 지정.

### 2026-07-14 — design-systems 브리프: NeoInput/CornerInput Error variant 신설 + NeoSelect Open variant 신설 (기본 상태 체크리스트 실전 적용 1호)
- **원본 요청**: 사용자가 Figma에서 레거시 `[Legacy B-2] Input/Error/Default`(`314:881`, 0-3절에서 FRAME `314:879`로 전환된 컴포넌트의 자식)와 `[Legacy B-2] Select/Open`(`314:896`, FRAME `314:893`의 자식)을 직접 열어보고, 정식 NeoInput(`288:12`)/CornerInput(`288:27`)에 Error 상태가 없고 NeoSelect(`261:660`)에 Open 상태가 없는 갭을 지적. 사용자가 "인풋 오류, 셀렉박스 오픈 컴포넌트를 만드는 것을 규칙으로 해야한다"고 명시적으로 확정해, 메인 세션이 이미 `.claude/agents/design-systems.md`에 "컴포넌트 유형별 기본 상태 체크리스트"(Input=Error, Select=Open 포함)를 명문화 완료 — 이번 브리프는 그 체크리스트의 첫 실전 적용 사례.
- **핵심 판단**: (1) 체크리스트 규칙 자체는 design-systems 에이전트 정의 파일에 이미 있으므로 design-prompter가 또 명문화하지 않고 "그 체크리스트를 따라 실행하라"는 지시만 브리프에 담음(중복 문서화 금지). (2) 레거시 참고 노드(`314:881`/`314:896`)가 실제로는 과거 COMPONENT_SET→FRAME 전환(0-3절)으로 이제 FRAME(`314:879`/`314:893`)의 자식 노드가 되어 있을 가능성이 높다는 점을 design-system.md 0-3절 대조로 미리 짚어, design-systems가 정확한 부모-자식 구조부터 `get_metadata`로 재확인하고 시작하도록 지시(엉뚱한 노드 참조 방지). (3) Error 색은 새 원시값 생성 금지, 기존 coral 계열(1-1절 `color/coral/500`/`coral/900`, 1-2절 `color/background-error`) 중 alias 재사용을 우선 검토하되, 보더/텍스트용 semantic이 현재 design-system.md에 없음을 grep으로 먼저 확인시키고 없으면 최소 alias만 신규 추가하도록 범위를 못박음. (4) Open 구조는 레거시의 "트리거+3개 옵션 패널" 레이아웃만 참고하고 색/보더/그림자는 NeoSelect 기존 토큰으로 재해석하도록 지시, elevation 적용 여부는 판단 후 반드시 근거를 문서에 남기도록 요구(판단만 하고 기록 누락 금지).
- **구체화 포인트**:
  1. 작업 A(NeoInput/CornerInput Error): A0 레거시 구조 참고(스타일 베끼기 금지)+기존 coral semantic 전수 확인 → A1 State=Error를 Focus와 독립/조합 축 중 무엇으로 설계할지 판단 기준 제시(화면정의서 시나리오 확인은 열람만, 문서 수정 금지) → A2 CornerInput은 0-3절에서 이미 제거한 CornerBracket을 절대 되살리지 않는다는 제약 재확인 → A3 신규 Error 텍스트/보더 색 WCAG 대비 필수 재계산(coral/500 vs coral/900 중 통과하는 쪽 채택 검토) → A4 스펙 시트(`344:721`/`344:740`) 갱신 → A5 design-system.md 1-2/1-3/3/5/7-2절 반영.
  2. 작업 B(NeoSelect Open): B0 레거시 구조(트리거+가족/친구/기타 옵션 3개) 참고+NeoSelect 기존 토큰 재확인 → B1 옵션 텍스트는 컴포넌트 프로퍼티로 노출(하드코딩 금지) → B2 elevation 적용 여부 판단+근거 필수 기록(적용/비적용 둘 다 이유를 남겨야 완료) → B3 Component Specs에 NeoSelect 스펙 시트 신규 생성(Default+Open) → B4 design-system.md 5절 "단일"→"Default/Open" 갱신.
  3. 범위 제한: 이번 승인 범위는 Input=Error, Select=Open만 — Select의 Focus/Disabled/Error 등 체크리스트상 나머지 상태는 이번에 임의로 확장하지 않고 필요 시 7-2절 후속 과제로만 기록(범위 확대는 별도 design-pl 승인 필요).
  4. 공통 제약 재확인: 확정 8개 프레임(`248:11689` 하위)·레거시 두 FRAME(`314:879`/`314:893`)과 그 자식 노드 전부 읽기 전용, 새 raw hex 생성 금지(기존 coral primitive alias만), 완료 전 design-systems 자체 재대조 필수, 화면정의서(SCR-900 등)는 열람만 하고 수정 금지(필요성 판단 시 결과 보고에 메모만).

### 2026-07-14 — design-systems 브리프: NeoInput/CornerInput Content(Placeholder/Filled) 축 추가 + NeoSelect Content(Placeholder/Selected) 축 추가 + 옵션 hover 신설 (기본 상태 체크리스트 실전 적용 2호)
- **원본 요청**: 사용자가 "인풋과 셀렉인풋창은 Default/에러/플레이스홀더/포커스로 만들잖아? 꼭 명시시켜서 알아서 만들도록 해줘"라고 지적. 직전 라운드(26차)에서 Input=Error, Select=Open까지는 신설했으나 Placeholder(안내 문구 노출) 상태가 빠져 있었음 — `.claude/agents/design-systems.md` 체크리스트에 이미 Input=Default/Placeholder/Focus/Error/Disabled, Select=+Open이 명문화돼 있어 재정의 없이 그 체크리스트를 그대로 따르도록 지시. design-pl이 사용자 승인 받은 실사용 매트릭스(Focus×Error×Placeholder=Yes 조합만 제외, 근거: 포커스 중 에러 지연 표시 UX 관례)를 그대로 전달.
- **핵심 판단**: (1) design-system.md 0-10절/5절 표를 직접 대조해 현재 실제 베이스라인이 NeoInput/CornerInput=Focus×Error 2×2=4 variant, NeoSelect=State(Default/Open) 2 variant임을 먼저 재확인 — 사용자가 준 "현재 상태" 서술이 문서 실측과 정확히 일치함을 검증한 뒤 브리프에 인용. (2) **중요한 함정 발견**: 0-3절 기록에 CornerInput 기존 텍스트 노드가 "placeholder '윤아'"로 명시돼 있어, 현재 baseline 4 variant가 실제로는 이미 "Placeholder 의미"로 렌더링돼 있을 가능성이 있음 — 이번 매트릭스는 기존 4개를 그대로 Content=Filled로 매핑하므로, design-systems가 자동으로 "라벨만 바꾸면 끝"이라 오판하지 않도록 "0단계 선행 확인"으로 현재 텍스트 색상(ink/900인지 muted 계열인지)과 내용을 먼저 실측하고, Filled 의미(값 채움, ink/900)에 안 맞으면 이번 작업 범위 안에서 바로잡으라고 명시. (3) NeoSelect Open 옵션 hover는 0-10절 B에서 "elevation 판단"만 기록되고 실제 hover는 추가되지 않았음을 문서 대조로 확인 — 기존 Selected×Open 패널에도 지금까지 hover가 없었던 실제 갭이므로, 이번에 신규 Placeholder×Open뿐 아니라 기존(재매핑된) Selected×Open 옵션 패널에도 함께 hover를 적용하도록 범위를 넓혀 명시(신규 것만 만들고 기존 것을 방치하지 않게).
- **구체화 포인트**:
  1. 작업 A(NeoInput/CornerInput): 매트릭스 그대로 7 variant(Placeholder×Error×Focus=Yes/Yes/Yes 1건 제외) 확장 지시, 제외 근거를 design-system.md에도 기록하도록 명시. Placeholder 텍스트색은 `color/text-muted-subtle`(#888)/`color/text-muted`(#777) 중 확정 프레임(login `247:6666`/join `241:1552`/main-수정 모달 `248:8103`/NeoInput은 main 검색창) 실측으로 확정, 7-1절 §5 "placeholder 텍스트 대비 미달" RESOLVED 항목과 같은 값이어야 함을 교차 검증 조건으로 명시. Filled 텍스트는 기존 ink/900 재사용, 신규 보더/그림자 토큰 없음(Focus/Error 축 기존 바인딩 유지). CornerInput 코너 브래킷 부활 금지 재확인. placeholder 문구는 확정 프레임 재관찰로 채택(임의 작문 금지, 사용자가 언급한 레거시 `314:881`("이름 (1~5자)")은 확정 프레임과 대조 후 참고만).
  2. 작업 B(NeoSelect): Content(Placeholder/Selected)×State(Default/Open) 2×2=4 variant. Placeholder 문구는 사용자가 이미 확정한 "종류선택"을 재관찰 없이 그대로 채택. 옵션 hover는 신규 필수 요구사항으로 명시(틀만 만들지 말 것) — 9절 hover 블렌드 공식(브랜드색 배경 t=12%/흰 배경+아웃라인류 t=6%, 정확한 계산은 9절 재확인)을 재사용하도록 지시, NeoSelect 옵션 행은 흰 배경 기반이라 t=6% 계열이 유력하나 최종 판단은 design-systems.
  3. 스펙 시트: NeoInput/CornerInput 7칸 그리드(제외된 1칸은 빈 셀+제외 근거 라벨로 명시해 버그처럼 보이지 않게), NeoSelect는 4 Content×State + 옵션 hover 예시 포함.
  4. 문서화: design-system.md 신규 절(0-11절 예상) + 1-2/1-3/3/5/9 관련 절 갱신, Focus×Error×Placeholder 제외 근거와 hover 공식 재사용 근거를 명시적으로 기록하도록 지시. 화면정의서(SCR-900)는 결과 보고에 메모만, 문서 수정 금지.
  5. 공통 제약 재확인: 확정 8개 프레임 읽기 전용, 신규 raw hex 금지, 완료 전 design-systems 자체 재대조 필수.

### 2026-07-14 — 브리프 2건(재개 라운드): graphic-designer(Pixel/EyeOff 신설) → design-systems(NeoSelect hover 폭 버그 수정 + Pixel/EyeOff 등록 + Placeholder 문서화 마무리)
- **원본 요청**: 서버 문제로 중단됐던 라운드 재개. design-scanner가 현재 Figma 상태를 재확인한 결과: (1) NeoSelect(`387:13`) Open 옵션 패널 hover 배경이 패널 전체 폭을 안 채우는 버그 미착수, (2) Pixel/EyeOff 짝 아이콘 미착수(Pixel/Eye `281:405`만 존재), (3) NeoInput/CornerInput Placeholder variant(각 7개)와 NeoSelect Placeholder/Selected×Default/Open(4개)는 **Figma에는 이미 생성 완료**돼 있으나(직전 라운드 결과물) `docs/design/design-system.md`에는 아직 기록이 없는 문서화 갭. design-pl이 라우팅(graphic-designer 먼저, design-systems 다음)과 각 작업 내용을 이미 확정해 전달, design-prompter는 실행 가능한 형태로 구체화만 함.
- **핵심 판단**: (1) graphic-designer 브리프에서 "Graphic Assets" 페이지(`90:2`)가 사용자에 의해 이미 삭제된 상태임을 `docs/design/graphic-assets.md`로 먼저 확인하고, 정상 파이프라인(원화를 그 페이지에 먼저 그림)을 못 찾아 헤매지 않도록 "이번엔 예외적으로 Icons 페이지에 바로 raw 벡터를 그린다"고 명시 — 없는 페이지를 찾다가 시간 낭비하거나 잘못된 페이지에 그리는 실수 방지. (2) Pixel/Eye(`281:405`)가 `PixelEye`(`247:6814`)라는 raw 노드에서 design-systems가 나중에 `createComponentFromNode`로 컴포넌트화한 선례를 그대로 재사용하도록, graphic-designer는 raw 벡터만 만들고 **아직 Figma Component로 만들지 않는다**는 역할 경계(2-2번 "크래프트 vs 시스템 등록 분리")를 명시적으로 못박음 — 안 그러면 그리는 사람이 등록까지 하려다 컴포넌트화 규칙(등록 절차·문서 반영)을 놓칠 위험. (3) design-systems 브리프의 (C) Placeholder 작업은 "이미 Figma에 존재하니 다시 만들지 말라"를 최우선으로 강조 — design-scanner 보고를 신뢰하되 `get_metadata` 재확인은 여전히 요구(문서 갭이지 산출물 갭이 아님을 구분해서 전달, 중복 생산 방지).
- **구체화 포인트**:
  1. graphic-designer 브리프: Pixel/Eye 조형 관례(14×10, 잉크 #1a1a1a, 6블록 조합, 블록 방식 vs Close의 stroke 방식 구분)를 재확인 지시로 명시, "닫힌 눈" vs "슬래시" 두 방향 중 판단은 크래프트 재량으로 위임하되 근거 메모는 필수화. 이름은 기존 컨벤션(NoResult 같은 복합어 선례)에 비춰 `Pixel/EyeOff` 기본값 채택, 배치 간격은 기존 클러스터 실측값 재사용(새 값 발명 금지).
  2. design-systems 브리프 (A): 원인을 짐작하지 않고 `get_metadata`로 hover 배경 fill 노드와 텍스트 노드의 padding/폭 차이부터 실측하도록 지시, 수정 원칙은 "배경=행 전체 폭, 텍스트=기존 padding 값 그대로 재사용"으로 좁힘(새 padding 수치 발명 금지). Content=Placeholder(`387:12`)/Content=Selected(`401:869`) 두 variant 모두 적용 필수.
  3. design-systems 브리프 (B): graphic-designer 산출물을 그대로 이어받아 `createComponentFromNode`만 수행(별도 재작업 없음), graphic-assets.md/design-system.md 두 문서 모두 카운트 숫자(10종→11종)까지 정확히 갱신하도록 명시.
  4. design-systems 브리프 (C): 7-1절 §5 "placeholder 텍스트 대비 미달"(RESOLVED) 기존 항목과의 교차 검증을 재차 강조(직전 라운드 브리프에서 이미 요구했던 조건이 실제로 지켜졌는지 이번에 문서화 단계에서 재확인). Component Specs `388:746`을 새로 만들지 않고 기존 2칸 그리드를 4칸(2×2)으로 확장하도록 지시(2-5번 규칙 — 새 프레임 남발 금지). design-system.md 신규 절 번호는 실제 파일의 최신 상태에 맞춰 design-systems가 판단하도록 유연하게 열어둠(0-11 예상이라고만 안내).
  5. 공통 제약: 확정 8개 프레임 읽기 전용, Link(`341:3`) 텍스트는 사용자가 이미 수정해둔 값이므로 불가침, 이번 라운드는 신규 라벨링(❌ 미채택/❌ 폐기) 대상이 없음을 명시해 불필요한 라벨링 방지.
