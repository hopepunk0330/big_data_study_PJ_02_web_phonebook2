# content-designer 메모리

이 파일은 content-designer가 작업 시작 시 읽고, 작업 종료 시 기록을 남기는 메모리/로그입니다.

## 작업 로그

- 2026-07-29: **NC1/NC3 골격 미분화 + NC2 완성도 격차 해소 라운드(6차, "3개 시안 비교" 신규 원칙 최초 적용)** — 5차 라운드가 "완전 재조립"으로 보고됐지만 실측 결과 NC1·NC3는 색상만 반전이고 섹션 구조·이미지 크기/위치가 사실상 동일했고, NC2는 스펙시트 골격뿐이라 완성도 격차가 컸다는 지적을 받음. **NC3(`176:2`) 구조 전환**: S2/S3/S5/S6/S7/S8/S9 7개 섹션을 "사진+스크림+텍스트 오버레이" 구조로 전환. **NC2(`171:2`) 완성도 격차 해소**: S3/S5를 매크로컷 확대+리더라인/성분히어로샷+큰 골드 넘버로 전환(카피에 실제 있는 사실만 사용, 클레임 발명 안 함). **3개 시안 비교표 게이트를 실제로 통과시킴**(5열 전부 실측값 기준). **신규 확인**: 세로 1만px+ 프레임은 `get_screenshot` 축소로 텍스트 판독 불가 — `clone()+temp.appendChild(child)`(y오프셋 필터)+`frame.screenshot({contentsOnly:true})`가 유일한 실용적 검증 경로. **전체 사용자 확정 대기 유지**. graphic-designer 요청 없음(전부 기본 도형).
- 2026-07-29: **New Concept 2 = "Victory Pop"(빅토리 팝) 완전 신규 조립 라운드(7차, "❌ 미채택 — New Concept 2 — Lab Notes"(`171:2`/`182:2`) 완전 반려 후 대체)** — design-prompter 브리프: 무드보드 6장(`49:8`/`49:21`/`49:18`/`49:24`/`49:25`/`49:33`) 실측 근거로 "하드엣지 블록 분할"(대각선/직각, 소프트 밴드 아님) 페르소나 신규 조립. **1단계 라벨링만 먼저 수행**(`171:2`/`182:2`에 `❌ 미채택 —` 접두어), 이후 완전 신규 프레임(`New Concept 2 — Victory Pop`, `201:5`, 1080×15360, x=11520) 처음부터 조립. **핵심 장치**: (1)대각선 하드엣지 웨지 3곳(S1 히어로/S4 피벗/S10 CTA, `figma.createVector()`+`vectorPaths` SVG path "M x y L x y Z" 형식 — 콤마 구분 "M0,0"은 파싱 에러, 공백 구분 "M 0 0" 필요), (2)수직 직각 스플릿 3곳(S2/S3/S6, 좌우 반전 지그재그 리듬), (3)초대형 고스트 넘버(S1 "3", S5 "01"/"02" — 카피에 있는 수치만 사용), (4)원형 배지 앵커링 3곳(블록 접합부에 걸쳐 배치), (5)브러시 커서브 단어 2곳(헤드라인당 딱 한 단어), (6)S7 before/after는 코너 삼각 플래그+중앙 하드바 오버레이(그라디언트 스크림과 차별화), (7)S8 초대형 스텝넘버+골드 헤어라인 컬럼 구분, (8)S9 글로시 그라디언트 엘립스. **사진 스케일**: Victory Pop을 NC1/NC3보다 다소 크게(640/460/480) 잡음 — "AIDA 히어로는 가장 과감해야" 원칙 우선 적용, design-pl 보고에 이 해석을 투명하게 남김. **버그 재확인**: 자동줄바꿈이 단어 중간을 끊는 문제 재발 — `characters=` 직접 재대입으로 의미단위 줄바꿈 교정. **z-order 기법**: 배경 뒤 삽입은 `root.children.findIndex()`+`root.insertChild(idx, node)`. graphic-designer 요청 없음(전부 기본 도형). **전체 사용자 확정 대기 유지**.
- 2026-07-29: **팔레트 교체+오브제 배치+가독성/폰트하한 전수점검 라운드(8차)** — (1) Victory Pop(`201:5`) 팔레트를 웜톤→딥네이비×아이스블루로 전면 교체(fill 63개, 골드 `#b8935b` 불변). (2) graphic-designer 15개 오브제를 21회 clone 배치(Slow Ritual 6/Victory Pop 7/Spotlight 8) — **버그**: 오브제 아이템 프레임 자체에 흰색 fill이 있어 clone 시 흰 박스가 같이 옴 → 매 clone마다 `clone.fills=[]`로 제거 필수. `contentBBox()`+`node.rescale(scale)`+`frame.x = targetX - bbox.minX*scale` 공식으로 정밀 배치, `insertChild(idx, clone)`로 배경 뒤 삽입. (3) 텍스트 가독성 전수점검: 실제 색상 대비 문제는 없었음, 유일한 이상은 폰트크기/박스폭 문제(4번에서 해결). (4) 폰트크기 하한 전수 스캔(`findAll`+`getStyledTextSegments(['fontSize'])`) 결과 서브카피 18개·캡션/라벨 40개 이상이 하한 미달 확인되어 전부 28px/24px로 상향. **버그**: `textAutoResize:'HEIGHT'`(고정폭) 필/뱃지 텍스트는 폰트만 키우면 줄바꿈 오버플로 발생 — `WIDTH_AND_HEIGHT`로 임시 전환해 타이트 폭 측정 후 `'HEIGHT'` 복귀+그 폭으로 `resize()`. 헤드라인은 원래 크기 유지. **전체 사용자 확정 대기 유지**.
- 2026-07-29: **최종 확정(Spotlight `176:2`) 후 마무리 라운드(9차) — 미채택 라벨링 + 잔여 노드 정리 + copy.md stale 서술 정정** — 사용자가 3안 Spotlight를 최종 확정, 1안 Slow Ritual(`162:2`)·2안 Victory Pop(`201:5`) 작업 중단. (1) 라벨링: `162:2`→`❌ 미채택 — New Concept 1 — Slow Ritual`, `201:5`→`❌ 미채택 — New Concept 2 — Victory Pop`. `176:2`는 미손대고 그대로 둠(2차 라운드에서 별도 처리 예정). (2) 잔여 노드(`74:2`/`74:35`/`74:65`, `136:3`~`136:6`, 검증용 임시 프레임 `248:2`) 스크린샷·좌표 실측 후 순수 중복/무관 잔재로 확인되어 삭제. (3) `copy.md` Section 1/4/10 "누끼컷" stale 서술을 "배경 포함 완성 사진"으로 정정(카피 본문은 손대지 않음). **`use_figma` 호출에서 이름변경+삭제를 한 스크립트에 묶으면 auto-mode가 차단** — 별도 호출로 분리하면 통과.
- 2026-07-29: **Spotlight 레이아웃 정밀화 2차 라운드(10차) — 변형1/2/3 3종 완성(그리드 분할형/대각선 크롭형)** — figma-file-organization.md 2-1-C 2단계 게이트의 2차(레이아웃 정밀화, 전체 섹션) 라운드. **1단계(관리)**: `176:2`→`New Concept 3 — Spotlight-1`, 그 요약카드 `182:35`→`...-1 — Summary Card`로 이름만 변경(내용 무수정). **2단계(신규 조립)**: 같은 페이지(`9:2`) 안 `176:2` 아래에 세로로 변형2(`255:2`, Grid Split, y=14180, h=9820)·변형3(`255:7`, Diagonal Crop, y=24200, h=9260) + 각 라벨 카드(`255:3`/`255:8`, 1~2문장) 신규 조립, 전체 10섹션 완성(placeholder 없음). 카피는 `copy.md` 그대로 재사용, 사진은 변형1(`176:2`)의 기존 이미지 노드에서 `imageHash`를 그대로 재조회해 재사용(재업로드 없음). **변형2(Grid Split)**: 사진/텍스트를 640:440 좌우 그리드 컬럼으로 물리적 분리, 섹션마다 사진 위치 좌/우 교차(zigzag), 원본 가로형 사진(05/08/09)은 상단 전체폭 스트립+하단 캡션 그리드(2col 성분뱃지/3col 스텝) 처리, 골드 세로 헤어라인으로 그리드 경계 표시. **변형3(Diagonal Crop)**: `figma.createVector()`+`vectorPaths`로 사선 패럴렐로그램(웨지) 사진 크롭, 좌/우 지그재그 10섹션 전체 적용, 대각선 경계에 골드 헤어라인, 텍스트는 좁은 안전지대(304px)에 배치. **신규 버그(중요)**: `figma.createVector()`로 `vectorPaths`를 절대좌표로 지정한 뒤 **추가로 `node.x`/`node.y`를 직접 대입하면, Figma가 그 값을 "기존 위치에 더하는 오프셋"이 아니라 그 시점 bbox-min 기준 절대좌표로 재해석해 도형 전체가 의도치 않게 밀림**(실제로 400px 밀려 사진이 텍스트 영역을 덮는 사고 발생, 스크린샷으로 발견). 같은 노드에 `vectorPaths`를 재대입하면 기존 x/y 위에 다시 얹혀 오프셋이 배가됨(한 번은 400 밀림, "고치려고" 재대입하니 두 배로 밀림). **해결 원칙**: 웨지를 만들 때는 path 좌표 자체에 최종 절대좌표(프레임 로컬 기준 실제 y값 포함)를 전부 넣고, 생성 후 `node.x`/`node.y`를 **절대 건드리지 않는다** — 위치를 바꿔야 하면 노드를 삭제하고 올바른 절대좌표로 새로 만든다(패치하지 않는다). **줄바꿈 버그**: `bodyTextPartial`(단일 문자열+부분강조, 자동줄바꿈 의존)로 만든 헤드라인이 좁은 컬럼(304px)에서 "사용은 간단합니\n다"처럼 단어 중간에서 줄바뀜 — 헤드라인은 항상 `headline()`류(라인 배열+명시적 `\n`)로 만들어야 함, `bodyTextPartial`은 텍스트 폭이 확실히 넉넉한 자리(전체폭 캡션존)에만 사용. **비교표 게이트 통과**(레이아웃 타입/이미지 크기·위치/텍스트 위치/장식 방식 5열 전부 변형1~3 실측 기준 실질적으로 다름 확인). graphic-designer 신규 요청 없음(기존 Spotlight 5종 오브제를 변형당 6회씩 clone 재사용, 3~5개 기준 충족). **전체 사용자 확정 대기 유지**(design-qa·정식화 진행 안 함).

## 참고 (버그/패턴 아카이브, 로그 트리밍으로 요약만 유지)
- 이전 라운드(4차, 브랜드 페르소나 3종 최초 이식)에서의 판단: 3개 페르소나는 섹션1·10(감성적 히어로/클로징 컷)에서 무드가 실제로 갈려 공용 사진 재사용 어려움, 섹션4(포뮬러/텍스처 매크로컷)는 공용 1장으로 충분.
