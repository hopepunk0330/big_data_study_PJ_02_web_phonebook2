# tech-architect 메모리

이 파일은 tech-architect의 최근 작업 로그(휘발성, 5개 캡)만 남깁니다. **기술 아키텍처 결정사항 자체(현재 확정 상태)는 여기 없습니다 — `docs/planning/tech-architecture.md`가 그 소스 오브 트루스입니다.** 작업 시작 시 이 로그가 아니라 그 문서를 먼저 읽으세요.

## 작업 로그

### 2026-07-16 — DB 커넥션 재연결 설계 브리프 (`pool_pre_ping=True`, TRD v1.3 → v1.4)
- 배경: 06 테스트계획서 §7 `TC-PERSIST-02`(DB 컨테이너 `pg-lab` 재시작 후 서버 재기동 없이 즉시 재요청 → 데이터 그대로 조회, AC-16 근거)를 통과시키려면 SQLAlchemy 커넥션 풀이 죽은 커넥션을 자동 재연결해야 하는데, 05 TRD §4-4 `database.py` 예시(`engine = create_engine(DATABASE_URL)`)·tech-architecture.md §1 어디에도 재연결 설계가 없어 01 문서 NFR-01(500 금지) 위반 소지가 있었음. 사용자가 "pool_pre_ping=True 같은 SQLAlchemy 표준 방식으로 지시해도 될까"를 구체적으로 확인 후 승인.
- 판단: `create_engine(DATABASE_URL, pool_pre_ping=True)` 한 줄(옵션 추가)만 surgical하게 변경 — 커넥션 체크아웃 직전 헬스체크·자동 재연결이 애플리케이션 코드 밖(풀 내부)에서 일어나므로 `get_db()`의 rollback 예외 처리는 손대지 않음. TRD 본문에는 06/`TC-PERSIST-02`를 직접 인용하지 않기로 판단(TRD는 00~04만 근거 문서로 인용하는 기존 관례 — 06이 05를 인용하는 방향만 있고 반대 방향 전례 없음, 05 §10이 `test_persistence.py`를 언급하면서도 06을 인용 안 한 것과 동일 패턴). "DB 재시작" 시나리오는 일반 서술로만 설명.
- `tech-architecture.md`는 이번에 직접 갱신(§1 표에 "DB 커넥션 복원력" 행 신설 + §5 결정 이력에 `TC-PERSIST-02` 인용 포함한 판단 배경 추가 — 그 문서는 내부 판단 로그라 인용 제약이 TRD와 다름). TRD(05 문서) v1.3→v1.4 실제 반영은 planning-writer 몫 — 브리프는 스크래치패드 `db-reconnect-brief.md`에 코드 diff(277행 1줄), NFR-01 연결 문단, 변경 이력 v1.4 문구, 함께 손볼 곳 체크리스트까지 그대로 옮겨쓸 수 있는 수준으로 작성.

### 2026-07-16 — SCR-002 "레이아웃(와이어프레임)" 신설 브리프 + tech-architecture.md §4 stale 버전 인용(v1.7→v1.8) 직접 정정
- 배경: 02 화면정의서 SCR-002(219~234행)에 팀 표준 4종 세트 중 "레이아웃(와이어프레임)" 서브섹션이 통째로 빠져 있음(SCR-001/004에는 있음). 원본 PDF(v1.0) 그림은 "인라인 편집" 시절 그림이라 복원 대상이 아니고, 확정 Figma 디자인(`docs/design/confirmed/user-confirmed-final-design.md` §10-1, `main` 프레임 `214:349`) 근거로 새로 설계해야 한다는 요청.
- 핵심 판단: SCR-002(연락처, 본문)와 SCR-003(카테고리, 좌측 사이드바)이 물리적으로 한 화면(§2 화면 목록에 이미 "SCR-002와 같은 화면 안"으로 명시)이므로, 와이어프레임은 SCR-002 절에 통짜로 하나만 넣고 SCR-003 절에는 "레이아웃은 SCR-002 참고" 한 줄만 추가 — 같은 그림을 두 곳에 복제하면 이 문서가 반복적으로 겪은 "stale 복제" 위험(버전 인용 정정 이력 참고)과 동일한 리스크가 생기기 때문. 좌우 2단 구조는 세로 구분선(`│┬┼┴┤`)으로 표현. 삭제/수정 모달은 별도 확정 프레임(`main-수정`/`main-삭제`)이 있고 표에 이미 텍스트로 설명돼 있어 이 "기본 상태" 와이어프레임에는 그리지 않음. 와이어프레임의 모든 요소는 SCR-002 표(추가/검색/전체/수정 모달 오픈/삭제/로그아웃)·SCR-003 표(카테고리 추가/수정/삭제)와 1:1 대응시키고 표에 없는 동작(예: 카테고리 nav 클릭 시 필터링)은 임의로 추가하지 않음.
- `tech-architecture.md` §4 "배경" 문단이 "화면정의서 v1.7"을 인용 중이었는데 이번 라운드에서 02 문서가 v1.8로 오를 예정이라 harness가 사전 지적 — 그 한 줄만 직접 정정(v1.7→v1.8), §5 결정 이력에도 정정 이력 1줄 추가. 02 md 자체는 건드리지 않음(planning-writer 몫) — 브리프(초안+정돈된 최종본 두 버전, 표 대응 체크리스트 포함)는 세션 스크래치패드 `scr-002-wireframe-brief.md`에 작성.

### 2026-07-16 — tech-architecture.md §4 stale 버전 인용(v1.8→v1.9) 기계적 정정
- 배경: 같은 라운드에서 planning-writer가 02 화면정의서를 v1.9로 올리는 작업 병행(SCR-002 카테고리 nav에 동적 목록 안내 + 카운트가 `GET /contacts` 클라이언트 집계 파생값이라는 명시 추가 — 순수 문서 보완, SCR-004의 "2단계 UI 흐름" 내용 자체는 무관). planning-pl이 직접 지시한 기계적 정정 작업(새 아키텍처/API 판단 없음).
- §4 "배경" 문단의 "화면정의서 v1.8"을 "v1.9"로 정정(1줄), §5 결정 이력에 동일 형식의 정정 이력 1줄 추가 — v1.7→v1.8 정정 때와 완전히 동일한 패턴. 다른 절·다른 인용은 건드리지 않음(surgical). 직접 `tech-architecture.md` Write로 갱신 완료, 02 md 자체는 손대지 않음(planning-writer 몫).

### 2026-07-16 — SCR-900/SCR-002 오버레이(모달·배너·토스트) 와이어프레임 브리프 + tech-architecture.md §4 stale 버전 인용(v1.9→v1.10) 직접 정정
- 배경: planning-writer.md에 신규 규칙 반영("화면"은 팝업/모달/얼럿/토스트/배너도 포함) 후, 사용자가 SCR-900(공통 메시지/오류표시)에 와이어프레임 서브섹션 자체가 없고 SCR-002의 수정/삭제 모달도 "모달 오픈" 텍스트 한 줄로만 서술돼 있는 것을 지적.
- SCR-900: "목적" 불릿 신설 + 에러 배너(login-알림창 `501:5188`)/성공 토스트(main-알림창 `501:6548`) 미니 와이어프레임 2개(각각 별도 그림 — 부모 화면·폭·위치가 달라 하나로 합치면 동시 출현처럼 오인될 위험, 단선박스=기본 레이아웃/이중선박스=절대위치 오버레이로 표기 구분) + 기존 "상황/화면 동작" 2열 표는 그대로 유지(사용자 동작이 아니라 API 응답 수신이 트리거라 5열 표준의 '요소'·'사용자동작'·'호출API' 열이 원천적으로 공백일 수밖에 없다는 근거 문장을 표 위에 추가) 판단.
- SCR-002: 수정 모달(main-수정 `501:3042`, CornerInput 4필드+TypeSelector 칩)·삭제 확인 모달(main-삭제 `501:3636`, 연락처 요약박스+경고박스) 미니 와이어프레임 2개를 기존 "레이아웃(와이어프레임)" 서브섹션 안에 소제목으로 이어붙이기로 판단(SCR-004의 기존 1/2단계 멀티 와이어프레임 배치 전례를 따름 — 구성요소-동작 표 옆에는 넣지 않음, 마크다운 표 셀에 코드블록 불가). 삭제 모달 경고 박스 카피·요약박스 값은 확정 디자인에 정확한 텍스트가 없어 예시임을 명시.
- 부가 발견(직접 처리 안 함, 보고만): SCR-003에 다른 화면엔 다 있는 "목적" 불릿이 없음. SCR-900 Mermaid의 로그인 시도 401 예외 케이스가 Q2/Q3 어디에도 명시적으로 안 걸리고 End4로 빠지는 구조적 공백.
- tech-architecture.md §4 "배경" 문단의 "화면정의서 v1.9" 인용이 02 문서가 이번 라운드에서 v1.10으로 오른 것과 어긋나 직접 정정(v1.9→v1.10), §5 결정 이력에도 1줄 추가 — 이전 정정들과 동일 패턴.
- 브리프는 스크래치패드 `scr900-scr002-modal-wireframe-brief.md`에 ASCII 그림·삽입 위치·근거까지 그대로 옮겨쓸 수 있는 수준으로 작성. 02 md 자체는 갱신하지 않음(planning-writer 몫).

### 2026-07-17 — tech-architecture.md §4 stale 버전 인용(v1.14→v1.15) 기계적 정정
- 배경: planning-writer가 02 화면정의서를 v1.14→v1.15로 개정(SCR-900 Mermaid 401 분기 구조적 공백 정정 + SCR-003 "목적" 불릿 신설 — 둘 다 사용자 승인된 순수 문서 정합성 보완, 아키텍처 판단 없음). 그 여파로 §4 "배경" 문단이 v1.14를 계속 인용하던 것을 planning-writer가 보고, planning-pl이 정정 지시.
- grep 결과 파일 내 "v1.14" 인용은 §4 배경 문단(살아있는 canonical 서술) 1곳뿐이었고, §5 결정 이력 안의 "v1.13→v1.14로 정정했다"는 과거 기록 문구는 그 자체가 이력이라 대상에서 제외(§5는 과거 결정 보존 원칙). §4의 1줄만 v1.15로 정정, §5에 기존 스타일과 동일한 패턴으로 정정 이력 1줄 추가. 다른 절·다른 인용은 건드리지 않음(surgical). 새 아키텍처/API 판단 없음.

</content>
