# tech-architect 메모리

이 파일은 tech-architect의 최근 작업 로그(휘발성, 5개 캡)만 남깁니다. **기술 아키텍처 결정사항 자체(현재 확정 상태)는 여기 없습니다 — `docs/planning/tech-architecture.md`가 그 소스 오브 트루스입니다.** 작업 시작 시 이 로그가 아니라 그 문서를 먼저 읽으세요.

## 작업 로그

### 2026-07-17 — tech-architecture.md §4 stale 버전 인용(v1.14→v1.15) 기계적 정정
- 배경: planning-writer가 02 화면정의서를 v1.14→v1.15로 개정(SCR-900 Mermaid 401 분기 구조적 공백 정정 + SCR-003 "목적" 불릿 신설 — 둘 다 사용자 승인된 순수 문서 정합성 보완, 아키텍처 판단 없음). 그 여파로 §4 "배경" 문단이 v1.14를 계속 인용하던 것을 planning-writer가 보고, planning-pl이 정정 지시.
- grep 결과 파일 내 "v1.14" 인용은 §4 배경 문단(살아있는 canonical 서술) 1곳뿐이었고, §5 결정 이력 안의 "v1.13→v1.14로 정정했다"는 과거 기록 문구는 그 자체가 이력이라 대상에서 제외(§5는 과거 결정 보존 원칙). §4의 1줄만 v1.15로 정정, §5에 기존 스타일과 동일한 패턴으로 정정 이력 1줄 추가. 다른 절·다른 인용은 건드리지 않음(surgical). 새 아키텍처/API 판단 없음.

### 2026-07-18 — tech-architecture.md §4 stale 버전 인용(v1.15→v1.16) 기계적 정정
- 배경: 02 화면정의서가 v1.15→v1.16으로 승격(SCR-002 카테고리 nav 목록 클릭 시 필터링 기능 — dev-pl 팀 프론트엔드 구현 라운드에서 이미 확정·구현돼 baseline에 인라인으로만 반영돼 있던 것을 정식 changelog로 이관하는 순수 버전 정합화 라운드, SCR-002 필터링 스펙 자체는 재검토 대상 아님).
- 전체 문서 수동 스캔(Grep 도구 미가용, Read 결과 전문 대조) 결과 "v1.15" 인용은 §4 배경 문단(살아있는 canonical 서술) 1곳뿐이었고, 사용자가 언급한 "86행"에는 실제로 버전 번호 인용 자체가 없어(그 줄은 "화면정의서 SCR-004가" — 버전 미표기) 정정 대상 아님. §5 결정 이력 안의 v1.14→v1.15 정정 기록은 과거 이력이라 대상에서 제외. §4의 1줄만 v1.16으로 정정, §5에 기존 스타일과 동일한 패턴으로 정정 이력 1줄 추가. 다른 절·다른 인용은 건드리지 않음(surgical). 새 아키텍처/API 판단 없음.

### 2026-07-18 — SCR-002 "카테고리 nav 클릭 시 필터링" 반영 브리프 (표/Mermaid/범례)
- 배경: dev-pl 팀이 사용자 결정에 따라 이미 구현·테스트 완료한 기능(app.js `selectCategoryFilter(li)`)을 02 화면정의서 v1.16 §4 SCR-002에 문서로 정확히 반영. 새 아키텍처/API 판단 아님 — `GET /contacts?category_id=` 계약은 01 문서 v1.12 §2 FR-06에 이미 confirmed.
- 판단: (1) 표 — 기존 310행 [전체]는 사이드바 nav "전체" 항목(dataset.categoryId="")과 동일 API·동일 결과이므로 "통합"(요소 칸 확장 + 검색창 비움·active 표시 추가), 특정 카테고리 클릭은 신규 행 1개로 분리(재클릭해도 해제 안 됨을 명문화). (2) Mermaid — 기존 D 분기 라벨만 "전체"까지 포괄하도록 확장, 신규 분기 L/L1/L2(GET /contacts?category_id={id} → 401/성공, C·D와 동일하게 "실패(401 제외)" 분기 없음 — 조회 전용이라 다른 실패 케이스 없음) 추가, classDef decision 목록에 L1 추가. (3) 범례(241~242행) — "표시 전용"을 "GET /categories로 채워짐(리스트 소스, 유지) + 클릭 시 GET /contacts?category_id=(§5 ⑤)"로 보완(완전 교체 아님 — 둘 다 사실이므로).
- tech-architecture.md §4 "배경" 인용 버전은 이미 v1.16으로 최신 확인됨(별도 정정 불필요). 브리프는 스크래치패드 `tech-architect-scr002-filter-brief.md`에 행 번호·정확한 대체 문구까지 작성. 02 md 자체는 갱신하지 않음(planning-writer 몫).

### 2026-07-18 — tech-architecture.md §4 stale 버전 인용(v1.16→v1.17) 기계적 정정
- 배경: 02 화면정의서가 v1.16→v1.17로 개정(SCR-002 카테고리 nav 필터링 관련 "구성 요소와 동작" 표·Mermaid 플로우차트·와이어프레임 범례 주석 3곳을 본문 서술과 정합화하는 라운드 — 바로 앞 항목에서 tech-architect가 전달한 브리프의 후속 반영, 새 아키텍처 판단 없음).
- 파일 전체 대조 결과 "v1.16" 인용은 §4 배경 문단(살아있는 canonical 서술) 1곳뿐이었고, §5 결정 이력 안의 v1.15→v1.16 정정 기록은 과거 이력이라 대상에서 제외. §4의 1줄만 v1.17로 정정, §5에 기존 스타일과 동일한 패턴으로 정정 이력 1줄 추가. 다른 절·다른 인용은 건드리지 않음(surgical). 새 아키텍처/API 판단 없음.

### 2026-07-18 — tech-architecture.md §4 stale 버전 인용(v1.17→v1.18) 기계적 정정
- 배경: 02 화면정의서가 v1.17→v1.18로 개정(harness-auditor가 발견한 §4 SCR-002 와이어프레임 범례 주석의 잘못된 절 인용 — "§5 ⑤"를 §4 SCR-002 본문 문단 인용으로 교체하는 순수 정정, 새 아키텍처 판단 없음).
- 파일 전체 대조 결과 "v1.17" 인용은 §4 배경 문단(살아있는 canonical 서술) 1곳뿐이었고, §5 결정 이력 안의 v1.16→v1.17 정정 기록은 과거 이력이라 대상에서 제외. §4의 1줄만 v1.18로 정정, §5에 기존 스타일과 동일한 패턴으로 정정 이력 1줄 추가. 다른 절·다른 인용은 건드리지 않음(surgical). 새 아키텍처/API 판단 없음.
</content>
