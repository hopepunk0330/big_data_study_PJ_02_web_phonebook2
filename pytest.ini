[pytest]
; playwright의 `page`/`context` 픽스처(및 test_auth.py의
; `page.context.request.post("/auth/logout")` 같은 상대경로 호출)가 base_url
; 없이는 "Invalid URL" 오류를 낸다. 테스트 파일 내용은 그대로 두고, 실행
; 설정만 여기서 고정한다(frontend-engineer, 서버 구동 전제하에 로컬 실행용).
addopts = --base-url=http://127.0.0.1:8000
