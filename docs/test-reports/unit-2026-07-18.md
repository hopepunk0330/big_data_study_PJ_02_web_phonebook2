# 단위 테스트 실행 보고서 — 2026-07-18 (프론트엔드 정합화 작업 완료 후 최종 회귀)

## 배경
- 오늘 하루 진행된 프론트엔드 정합화 작업(카테고리 모달, 아이콘, 로고, 배너 오버레이 통합, 사이드바
  스크롤, 테이블 컬럼/스크롤, 포커스 상태, 웹폰트 로딩 등)이 모두 완료된 뒤의 최종 마무리 회귀
  라운드. dev-pl 지시로 단위/통합 분리 절차를 그대로 반복 적용.
- 이번 라운드에서 새로 추가된 단위 테스트 케이스는 없음 — 프론트엔드 정합화 작업은 UI 레이아웃/
  스타일 성격이라 API 로직에는 영향이 없다는 전제로 **기존 76개 전체 회귀 여부만 확인**.

## 실행 정보
- 실행 명령: `.venv/bin/python -m pytest tests/test_auth.py tests/test_contacts.py tests/test_categories.py -k "test_tc_api or test_tc_iso" -v --durations=0`
- 백엔드: `127.0.0.1:8000` — 실행 전 `curl` 확인 결과 다운 상태였음(반복 패턴), `cd backend &&
  set -a && source ../.env && set +a && ../.venv/bin/uvicorn main:app --host 127.0.0.1 --port 8000`로
  재기동 후 `curl /auth/me` → 401 정상 확인 후 진행
- DB 컨테이너 `pg-lab`: 기동 확인(`docker ps`)
- 실행 일시: 2026-07-18(최종 라운드)

## 결과 요약
- **수집: 118개 중 76개 선택(나머지는 `test_tc_e2e_*`로 deselect)**
- **통과 76 / 실패 0**
- 소요 시간: **12.06s**

## 파일별 내역
| 파일 | 통과 | 실패 |
|---|---|---|
| tests/test_auth.py (AUTH 14 + PW 9 + SCREEN 1) | 24 | 0 |
| tests/test_contacts.py (CONTACT 27 + ISO 6) | 33 | 0 |
| tests/test_categories.py (CATEGORY 17 + 07b) | 19 | 0 |
| **합계** | **76** | **0** |

## 실패 케이스
없음(전부 GREEN).

## 비고
- 오늘 진행된 프론트엔드 정합화 작업은 전부 `static/` 쪽 UI/스타일/레이아웃 변경으로, 백엔드
  API·비즈니스 로직은 손대지 않아 단위 테스트 수치 변동이 없을 것으로 예상했고 실측으로도 확인됨
  (회귀 없음).
- 이 보고서는 `test_tc_e2e_*`(Playwright 브라우저 기반) 및 `tests/test_persistence.py`를 포함하지
  않는다 — 해당 결과는 `docs/test-reports/integration-2026-07-18.md` 참고.

## 이력(같은 날 이전 실행)
- 오늘 앞선 라운드("카테고리 nav 클릭 필터링 신규 기능 추가" 후 회귀 확인)에서도 동일하게 76개
  GREEN이었음 — 이번 최종 라운드까지 포함해 오늘 총 2회 실행 모두 76/76 GREEN, 회귀 없음.
- 전날(2026-07-17) 최종 라운드도 76개 GREEN이었음(`docs/test-reports/old/unit-2026-07-17.md`로
  이관) — 어제 이후 오늘 하루 전체 작업을 통틀어 단위 테스트 수치 변동 없음.
