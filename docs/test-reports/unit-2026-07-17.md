# 단위 테스트 실행 보고서 — 2026-07-17

## 실행 정보
- 실행 명령: `.venv/bin/python -m pytest tests/test_auth.py tests/test_contacts.py tests/test_categories.py -k "test_tc_api" -v --durations=0`
- 대상: `test_tc_api_*` 이름 패턴(API/로직 단위, `page`/브라우저 없이 `api_request`(APIRequestContext)만 사용)
- 백엔드: `127.0.0.1:8000` uvicorn (본 라운드 시작 시 다운되어 있어 QA가 `.venv/bin/uvicorn`으로 재기동 후 실행, DB 컨테이너 `pg-lab`은 이미 기동 중이었음)
- 실행 일시: 2026-07-17

## 결과 요약
- **수집: 114개 중 70개 선택(나머지 44개는 `test_tc_e2e_*`로 deselect)**
- **통과 70 / 실패 0**
- 소요 시간: **12.87s**

## 파일별 내역
| 파일 | 통과 | 실패 |
|---|---|---|
| tests/test_auth.py (AUTH 14 + PW 9 + SCREEN 1) | 24 | 0 |
| tests/test_contacts.py (CONTACT 27) | 27 | 0 |
| tests/test_categories.py (CATEGORY 17 + 07b) | 19 | 0 |
| **합계** | **70** | **0** |

## 실패 케이스
없음(전부 GREEN).

## 비고
- 가장 느린 케이스: `test_tc_api_category_07_name_empty_and_too_long`(0.57s), `test_tc_api_contact_21_patch_other_users_contact_returns_404`(0.57s) — 전부 정상 범위, 타임아웃/재시도 없음.
- 이전 라운드(2026-07-17 이전 로그)에서 발견됐던 `test_tc_api_category_07` 테스트 데이터 오타(89번 줄)는 이미 surgical fix로 해결되어 이번 실행에서도 계속 GREEN 유지.
- 이 보고서는 `test_tc_e2e_*`(Playwright 브라우저 기반) 및 `tests/test_persistence.py`(TC-PERSIST, 자체 서버 프로세스)를 포함하지 않는다 — 해당 결과는 `docs/test-reports/integration-2026-07-17.md` 참고.

## 지시문 패턴 밖 발견 사항 — dev-pl 보고 필요 (침묵하지 않고 명시)
`tests/test_contacts.py`에 `test_tc_iso_01`~`06`(6개, "ISO" = 사용자 간 데이터 격리 검증) 이
존재하는데, 이름이 `test_tc_api_*`도 `test_tc_e2e_*`도 아니어서 `-k "test_tc_api"` 패턴으로는
잡히지 않는다. 실제로는 `api_request`만 쓰고 `page`/브라우저를 전혀 쓰지 않는 **순수 API 단위
테스트**라 성격상 이 보고서에 속한다고 판단해 추가로 별도 실행함(케이스를 새로 만든 게 아니라
기존 06 문서 기반 테스트를 실행 범위에서 빠뜨리지 않기 위한 조치).

- 실행 명령: `.venv/bin/python -m pytest tests/test_contacts.py -k "test_tc_iso" -v`
- **통과 6 / 실패 0**, 소요 시간 2.49s

| 테스트 | 결과 |
|---|---|
| test_tc_iso_01_list_excludes_other_users_contacts | PASSED |
| test_tc_iso_02_search_by_other_users_name_returns_empty | PASSED |
| test_tc_iso_03_patch_other_users_contact_id_returns_404 | PASSED |
| test_tc_iso_04_delete_other_users_contact_id_returns_404_and_survives | PASSED |
| test_tc_iso_05_category_list_excludes_other_users_category | PASSED |
| test_tc_iso_06_create_contact_with_other_users_category_id_returns_404 | PASSED |

**정정된 최종 단위 테스트 합계: 70 + 6 = 76개, 전부 GREEN.** 다음 라운드부터는 `-k
"test_tc_api or test_tc_iso"`처럼 패턴을 확장하거나, 이 6개도 `test_tc_api_*`로 개명하는 것을
검토 요청.
