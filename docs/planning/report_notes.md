# 최종 산출물용 포인트 노트

대화 중 나온 "나중에 최종 보고서·제출 자료에 꼭 담자"는 아이디어를 그때그때 잊지 않게 모아두는 running 노트다. 정식 계획 문서(버전 관리 대상)가 아니라 계속 append되는 메모라서 버전 번호를 붙이지 않는다 — 실제로 `docs/test-reports/` 등 최종 산출물을 작성하는 시점에 이 목록을 훑어보며 반영 여부를 하나씩 확인하고 체크한다.

## 포인트 목록

(아직 없음 — 대화 중 최종 산출물에 담고 싶은 아이디어가 나오면 여기 체크박스로 추가한다.)

## 참고 — 이미 다른 곳에 확정 기록된 포인트 (여기 중복 기재 안 함)

Notion이나 다른 정식 계획 문서에 이미 반영된 결정은 여기 다시 적지 않는다 — 최종 산출물 작성 시 함께 확인할 참고 링크만 남긴다.

## 근거

이 노트 자체의 존재 이유는, 다른 프로젝트(01-ML_mercari_price_2608)의 `docs/harness/`를 비교 조사하던 중 그 프로젝트가 `docs/planning/report_notes.md`(같은 목적의 running 노트)를 신설한 것을 보고 사용자가 이 프로젝트에도 같은 습관을 지금 바로 적용하기로 결정한 것(2026-08-22). 개별 포인트의 근거는 각 항목 안에 인라인으로 남긴다.
