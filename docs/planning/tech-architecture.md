# 기술 아키텍처 (Tech Architecture) — 연락처 관리 웹 서비스 (2차 과제)

| 항목 | 내용 |
|---|---|
| 문서명 | 연락처 관리 웹 서비스(2차 과제) 기술 아키텍처 |
| 문서 유형 | Technical Architecture (living document) |
| 상태 | 진행 중 — 판단이 생길 때마다 이 파일을 덮어써서 최신 상태로 유지 |

**이 문서의 역할(안내)**: `docs/planning/service-concept.md`가 BM/스코프 판단의 소스 오브 트루스이듯, 이 문서는 **API 계약·DB 스키마·기술 스택 선택 근거**의 소스 오브 트루스다. `01_연락처관리_웹서비스_구현요구사항서`·`03_연락처관리_웹서비스_기능정의서`에 이미 반영된 확정 내용(v1.0 PDF)을 이곳에 canonical화해서 모으고, 이후 API/DB/스택 판단이 생길 때마다 이 문서를 갱신한다. 과거 판단은 하단 "5. 결정 이력"에 보존한다(휘발시키지 않음 — git history가 상세 diff를 추가로 보완).

---

## 1. 기술 스택 및 선택 근거

| 계층 | 선택 | 근거 |
|---|---|---|
| 언어/프레임워크 | Python 3.12+ / FastAPI 0.139.0 | 학습 목표(REST API 설계·자동 검증)에 부합, Pydantic 통합으로 형식 검증 계층이 자동화됨 |
| ORM | SQLAlchemy 2.0.51 (`Mapped[...]` + `mapped_column(...)` 문법) | 테이블 정의가 곧 설계도 역할, 학습 범위 내에서 실무 표준 ORM 패턴 경험 |
| DB | PostgreSQL 16 (Docker 컨테이너 `pg-lab`) | 로컬 개발 환경, 학습자 단독 검증에 맞는 최소 인프라 |
| 인증 | 세션 기반(쿠키, `secrets.token_hex(32)` + `sessions` 테이블) | JWT 등 추가 인프라 없이 "로그인·세션·데이터 격리"라는 핵심 학습 목표를 그대로 구현 가능 |
| 비밀번호 저장 | Argon2 해시(`pwdlib`) | 원문·복호화 가능 형태 저장 금지(NFR-03), 실무 표준 해시 알고리즘 |
| 배제한 인프라 | JWT, SMTP 메일 발송 | `service-concept.md` §2 "핵심 리소스" 참조 — 이번 과제 범위(로컬 개발, 학습자 단독 검증)를 넘는 무거운 리소스이므로 의도적으로 배제. 필요해지면 PRD §11 "향후 확장"으로 다룸 |

**이번 라운드에 재확인된 원칙**: 새 API를 추가할 때도 위 스택 범위를 벗어나지 않는 선에서 설계한다(예: 비밀번호 찾기 기능에 이메일 발송을 쓰지 않고 기존 인증 스택만으로 구현 — §4 참조).

## 1-1. 프로젝트 파일 구조 (canonical)

프로젝트 루트에는 `backend/`(Python 백엔드 코드)·`static/`(실제로 서빙되는 화면 코드)·`frontend/`(프론트엔드 작업 하네스 전용, 런타임 코드 없음)가 형제 폴더로 나란히 위치한다.

```
(프로젝트 루트)
├── backend/            # FastAPI 앱, DB 로직 — main.py, database.py, models.py, schemas.py, security.py, crud.py, routers/, requirements.txt
├── static/              # 실제 서빙되는 화면 코드 — index.html, app.js
├── frontend/             # frontend-engineer 작업 가이드(CLAUDE.md)만 존재, 런타임 코드 없음
└── docker-compose.yml    # PostgreSQL(pg-lab) 컨테이너 정의 — backend 전용이 아닌 프로젝트 공유 인프라이므로 루트에 위치
```

**설계 근거**
- `static/`을 `backend/` 하위가 아니라 프로젝트 루트의 독립 폴더로 둔 이유: "같은 오리진 유지 → CORS 미들웨어 불필요"라는 결론 자체는 맞지만, 그 결론이 "물리적으로 backend/ 안에 있어야 한다"는 뜻은 아니다. FastAPI `StaticFiles(directory=...)`는 파일시스템 경로와 무관하게 앱이 바인딩된 포트로 그대로 서빙하므로(`app.mount("/static", StaticFiles(directory="../static"))`), `static/`이 `backend/` 밖에 있어도 오리진은 여전히 `127.0.0.1:8000` 하나다. 오리진 판정 기준은 "요청에 응답하는 프로세스의 프로토콜·호스트·포트"이지 "서빙되는 파일의 디스크 경로"가 아니다.
- `frontend/`는 실제 화면 코드가 아니라 frontend-engineer가 `static/`을 편집할 때 참고하는 하네스 가이드(`frontend/CLAUDE.md`)의 자리다. 이 폴더 안의 파일이 런타임에 실행되거나 서빙되는 일은 없다.
- `docker-compose.yml`(PostgreSQL 컨테이너 정의)은 `backend/` 전용 설정이 아니라 프로젝트 전체가 공유하는 인프라이므로, `backend/`·`static/`·`frontend/`와 형제 레벨인 프로젝트 루트에 둔다. DB는 어느 한 코드 폴더에 종속된 리소스가 아니다.

**참고(해소, 2026-07-15)**: 이전 라운드까지 `03_연락처관리_웹서비스_기능정의서` §1-1은 `contact_app/`을 프로젝트 루트로 삼아 `main.py`·`routers/`·`static/`을 `backend/` 래퍼 없이 평평하게 배치하는 별도 트리를 갖고 있었다(이름 체계 차이, `contact_app/` vs `backend/`). 사용자 승인 하에 이번 라운드에서 03 §1-1 트리를 본 절 및 05 TRD §3과 동일한 `backend/`·`static/`·`frontend/` 형제 구조로 교체하도록 브리프를 전달했다 — 이름 체계만 통일하고 03의 함수 스펙(FN-001~015)은 변경하지 않는 범위. 03 문서 실제 갱신은 planning-writer 몫이며, 판단 근거는 세션 스크래치패드 `03-structure-brief.md`에 기록되어 있다.

## 2. API 설계 컨벤션 (canonical)

01 문서(v1.0)에 이미 확립된 관례이며, 신규 API도 반드시 이 관례를 따른다.

- **엔드포인트 = FR 1개당 API 1개**: `01_구현요구사항서` §2 FR표는 API 1개당 FR 번호 1개를 배정한다(예: 인증 관련 API 4개 = FR-01~04). 여러 API를 하나의 FR로 묶지 않는다.
- **오류 응답 통일**: 모든 오류는 `{"detail": "사람이 읽을 수 있는 한국어 안내"}` 형태로 통일(`HTTPException(status_code=..., detail=...)`). 화면은 어떤 오류든 `detail`만 꺼내 표시한다.
- **상태 코드 매트릭스**: 401(로그인 필요/인증 실패) · 404(대상 없음 — 남의 데이터도 403이 아니라 404) · 409(규칙 충돌: 중복, 사용 중) · 422(형식 위반, Pydantic 자동) · 500 금지(어떤 요청에도 발생하면 안 됨).
- **검증 2계층 구조**: ① 형식 검증(입력 그 자체만으로 판단 가능 — Pydantic이 자동으로 422) ② 데이터 검증(DB 조회가 필요 — 우리가 직접 코드 작성, 404/409).
- **데이터 격리 원칙**: 모든 조회 조건에 `user_id`를 포함시켜, 남의 데이터는 조회 자체가 안 되게 만든다(별도 권한 검사 코드 불필요).
- **입력용/출력용 스키마 분리**: 입력 스키마에 `id`를 두지 않고, 출력 스키마에 민감 정보(`password_hash` 등)를 두지 않는다.
- **비로그인 접근 허용 API는 명시적으로 구분**: 기본은 로그인 필수(FR-05~12)이며, 로그인 전에도 접근 가능한 API(FR-01/02/13, 그리고 이번에 추가되는 FR-14/15)는 01 문서 §2 "공통 규칙"에 예외로 명시한다.

## 3. DB 스키마 (canonical)

01 문서(v1.0) §1에서 확정된 테이블 4개 구조를 그대로 인용한다. 신규 API가 스키마 변경(컬럼 추가 등)을 요구하는 경우에만 이 절을 갱신한다 — **이번 라운드(FR-14/15)는 스키마 변경 없음**(기존 `users.password_hash` UPDATE, `sessions` 행 DELETE만 발생, 컬럼/테이블 추가 없음).

| 테이블 | PK | 주요 제약 | 근거 |
|---|---|---|---|
| `users` | `id` (자동 증가) | `username` UNIQUE·NOT NULL, `password_hash` NOT NULL(Argon2 해시만) | 로그인 계정의 본체. 식별자를 전화번호가 아닌 대리키(surrogate key)로 둔 것과 같은 원칙 — "한 번 정해지면 안 바뀌는 값"이 기본키여야 함 |
| `sessions` | `session_id`(64자, `secrets.token_hex(32)`) | `user_id` FK → `users.id`, NOT NULL | 로그인 상태를 DB에 두어 서버 재시작에도 로그인이 유지되게 함(1차 과제엔 없던 개념) |
| `categories` | `id` (자동 증가) | `user_id` FK → `users.id`, NOT NULL / UNIQUE(`user_id`, `name`) | 카테고리도 사용자별로 격리 — 전체 UNIQUE가 아니라 복합 UNIQUE인 이유는 "내 카테고리 안에서만 이름 중복 금지"가 올바른 규칙이기 때문 |
| `contacts` | `id` (자동 증가) | `user_id`/`category_id` FK, NOT NULL / UNIQUE(`user_id`, `phone`) | 전화번호를 기본키로 쓰지 않는 이유: 수정 기능으로 전화번호 자체가 바뀔 수 있음. 복합 UNIQUE인 이유: 서로 다른 사용자가 같은 번호를 각자 저장할 수 있어야 함(카테고리와 동일 원칙) |

**FK cascade 정책**: 01 문서 원본은 명시적 `ON DELETE CASCADE`를 규정하지 않고, 대신 애플리케이션 레이어에서 "사용 중인 카테고리는 삭제 거부"(409)로 고아 데이터를 방지한다(FR-12). 이는 DB 제약(FK)이 원천 차단하는 오류(500)를 친절한 409 안내로 바꾸기 위한 의도적 설계다 — cascade로 자동 삭제하면 사용자가 의도치 않게 연락처를 잃을 수 있어 이번 과제 성격(사용자 데이터 보호)에 맞지 않는다고 판단.

## 4. 이번 라운드 판단 — 비밀번호 찾기 API 2종 (FR-14, FR-15)

**배경**: 화면정의서 v1.3 SCR-004가 이미 정의한 2단계 UI 흐름(1단계 아이디 확인 → 2단계 비밀번호 재설정)에 대응하는 API 계약을 확정한다. 본인 확인 방식은 `service-concept.md` §3에서 "아이디만 확인 후 즉시 재설정"으로 이미 채택 완료된 상태이며, 이 절은 그 결정을 API/DB 계약 수준으로 구체화한 것이다.

**API 계약 확정**:

| FR | 엔드포인트 | 로그인 필요 | 요청 본문 | 성공 | 실패 |
|---|---|---|---|---|---|
| FR-14 | `POST /auth/find-password` | 불필요 | `{"username": "happyday"}` | 200 — `{"username": "happyday"}` | 404 아이디 없음 / 422 형식 위반 |
| FR-15 | `PATCH /auth/password` | 불필요 | `{"username": "happyday", "new_password": "newpass1234"}` | 200 — `{"message": "비밀번호가 변경되었습니다"}` | 404 아이디 없음 / 422 형식 위반 |

**설계 판단과 근거**:

1. **FR 분리(FR-14/FR-15로 별도 배정)**: 01 문서의 "API 1개당 FR 1개" 관례(§2 API 설계 컨벤션)를 그대로 따른 것. 하나로 합치면 "1단계는 조회, 2단계는 쓰기"라는 서로 다른 성격의 작업이 한 FR에 섞여 추적표(§10)의 의미가 흐려진다.
2. **`find-password` 응답에 `id`를 넣지 않음**: `GET /auth/me`(FR-04)는 이미 로그인된 사용자에게 자기 `id`를 알려주지만, `find-password`는 아직 인증되지 않은 상태에서 호출되므로 최소 정보 노출 원칙에 따라 입력받은 `username`을 그대로 echo하는 데 그친다. 클라이언트는 이미 입력값을 갖고 있으므로 `id`가 실질적으로 필요하지도 않다.
3. **`new_password` 확인란(비밀번호 재입력)은 서버로 보내지 않음**: 화면정의서 SCR-004가 "클라이언트가 사전에 두 값 일치를 검증하고, 불일치 시 API 호출 자체를 하지 않는다"고 규정했으므로, 서버 계약은 `new_password` 단일 필드만 받는다. 서버가 "확인용" 필드까지 검증하는 것은 이미 화면이 보장한 것을 중복 검증하는 것이라 불필요(Simplicity First).
4. **비밀번호 재설정 시 기존 세션 전체 무효화(신규 결정)**: `PATCH /auth/password` 처리 시 `users.password_hash`를 갱신하는 것과 같은 트랜잭션에서 해당 `user_id`의 `sessions` 행을 전부 삭제한다. 근거 — 비밀번호가 바뀌었다면 그 이전에 발급된 세션은 더 이상 "본인이 확인한 자격"을 대변하지 않으므로, 실무 보안 모범 사례상 강제 재로그인을 요구하는 것이 맞다. 추가 인프라 없이 기존 `sessions` 테이블 DELETE만으로 구현 가능해 이번 과제 스택 범위를 벗어나지 않는다. **이 결정은 `service-concept.md` §3이 명시한 트레이드오프("아이디만 알면 누구나 비밀번호를 바꿀 수 있다")를 뒤집지는 않지만, 그 트레이드오프의 피해 범위를 줄이는 보완책이다** — 공격자가 비밀번호를 바꿔도 원래 사용자의 기존 세션이 자동으로 함께 끊기므로 최소한 "몰래 세션을 계속 쓰는" 시나리오는 막는다.
5. **DB 영향 범위**: 스키마 변경 없음. FR-14는 `users.id`, `users.username` 조회(읽기 전용). FR-15는 `users.password_hash` UPDATE + `sessions` 테이블에서 해당 `user_id`의 전체 행 DELETE.
6. **형식 검증 재사용**: `username`은 `SignupIn.username`과 동일한 규칙(4~20자, `^[a-z0-9]+$`), `new_password`는 `SignupIn.password`와 동일한 규칙(4~20자)을 그대로 재사용한다 — 새 정규식을 만들지 않는다(Simplicity First, 기존 스키마 재사용).

**의도적으로 보류한 것(이번 라운드 범위 아님)**: PRD(04 문서) §2-2 N1 비목표 항목과 §11 향후 확장 표는 아직 이 결정과 어긋난 채로 남아 있다(`service-concept.md` §4에 기록됨). 이 불일치 해소는 이번 라운드(01/03 문서 반영) 범위가 아니며, 별도 라운드에서 PRD 갱신이 필요하다.

## 5. 결정 이력

- **2026-07-14 — 문서 최초 생성 + 비밀번호 찾기 API 2종(FR-14/FR-15) 계약 확정**: planning-pl의 브리프 요청(SCR-004를 01/03 문서에 정식 반영)을 계기로 신규 생성. §1~3(기술 스택·API 컨벤션·DB 스키마)은 `01_연락처관리_웹서비스_구현요구사항서_v1.0.pdf`·`03_연락처관리_웹서비스_기능정의서_v1.0.pdf`에 이미 확정되어 있던 내용을 canonical 문서로 옮겨 담은 것이며, 이 라운드에서 새로 판단한 것은 §4(FR-14/FR-15 API 계약, 특히 "비밀번호 재설정 시 세션 전체 무효화" 결정)뿐이다.
- **2026-07-15 — static/ 물리적 위치 정정(backend/ 중첩 → 프로젝트 루트 독립 폴더) + §1-1 프로젝트 파일 구조 신설**: TRD v1.0 §2/§3이 "같은 오리진 유지 → CORS 미들웨어 불필요"의 근거로 `static/`을 `backend/` 아래 물리적으로 중첩시켰던 것을 정정. 근거(같은 오리진 → CORS 불필요) 자체는 맞으나, "같은 오리진 유지 = 물리적 중첩 필수"라는 전제가 틀렸다 — FastAPI `StaticFiles(directory=...)` 마운트는 파일시스템 경로와 무관하게 앱이 바인딩된 포트로 그대로 서빙되므로, `static/`이 `backend/` 밖(프로젝트 루트)에 있어도 오리진은 여전히 하나(`127.0.0.1:8000`)다. 사용자 확정 승인 하에 신설한 §1-1에 `backend/`·`static/`·`frontend/` 형제 구조를 canonical화했다. `frontend/`는 이번에 처음 "하네스 전용, 런타임 코드 없음"으로 명문화됨. `docker-compose.yml`도 backend 전용이 아닌 프로젝트 공유 인프라로 판단해 프로젝트 루트 배치로 정리. TRD(05 문서) v1.0 → v1.1 반영은 planning-writer의 몫이며, 이 판단은 브리프(세션 스크래치패드 `trd-structure-brief.md`)로 전달했다.
- **2026-07-15 — 03_기능정의서 §1-1 이름 체계 통일 브리프(`contact_app/` → `backend/`·`static/`·`frontend/`)**: 사용자가 이번 라운드에서 이름 체계 통일을 명시적으로 승인. 03 §1-1의 옛 `contact_app/` 단일 루트 트리를 본 절(§1-1)·05 TRD §3과 동일한 프로젝트 루트 + `backend/`·`static/`·`frontend/` 형제 구조로 교체하는 브리프를 작성. 03은 "함수/파일 단위 기능정의" 문서이므로 05의 상세 주석으로 대체하지 않고, 03 원본의 파일별 설명 주석(예: `database.py # DB 연결: 엔진, DB세션 공장, get_db`)을 그대로 보존해 `backend/` 하위로 옮기는 방향으로 판단(Surgical Changes — 이번 작업은 이름 체계 통일이지 설명 스타일 교체가 아님). `frontend/`·`requirements.txt`는 03 원본에 대응하는 주석이 없던 신규 항목이라 05 TRD §3의 기존 문구를 그대로 채택(각주 포함). `docker-compose.yml`은 05 TRD §3 트리에도 포함되어 있지 않으므로(별도 §9-1에서만 다룸) 03 트리에도 포함하지 않았다. 03 md 자체는 갱신하지 않음(planning-writer 몫) — 브리프는 세션 스크래치패드 `03-structure-brief.md`로 전달했다.
